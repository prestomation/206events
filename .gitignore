output/
node_modules/
errorCount.txt
fatalErrorCount.txt
zeroEventCalendars.txt
newSourceParseErrors.txt
newZeroEventSources.txt
.DS_Store
*.zip
coverage/
.channel-workspace.json
__pycache__/
web/test-results/
web/playwright-report/
web/.playwright/
.env
.env.local
.env.*.local
.claude/worktrees/

# Build-time scratch copies of the coverage series (see build-calendars.yml).
# The committed baseline is docs/event-history.json; these are the restored
# cache and the fetched published copy that get merged into it.
cached-event-history.json
published-event-history.json
