# 2026-08-07 (re-confirmed 2026-08-09): ICS endpoint returns HTTP 200 with an
# empty (0 byte) body; the site still uses the All-in-One Event Calendar
# plugin and still renders events live on /events/, but the ai1ec_exporter
# export controller itself is broken upstream. No working alternative ICS URL
# found — plain http://, &xml=true, and standard WordPress/Tribe ?ical=1 URLs
# all also fail (redirect-to-empty or return HTML). The blog and its events
# calendar are NOT gone, so this source is intentionally kept (not disabled)
# pending either an upstream fix or a future HTML-scraping ripper. Non-fatal
# failure — see lib/calendar_ripper.ts external-fetch handling, which now
# reports "Empty response body" instead of a bare "null" for this case.
geo: null
name: west-seattle-blog
sourceRole: aggregator
friendlyname: West Seattle Blog
description: West Seattle Blog's community events calendar, aggregating happenings across West Seattle - trivia nights, markets, walking tours, festivals, watch parties, and community meetings.
icsUrl: https://westseattleblog.com/?plugin=all-in-one-event-calendar&controller=ai1ec_exporter_controller&action=export_events&no_html=true
infoUrl: https://westseattleblog.com/events/
tags:
  - West Seattle
  - Community
