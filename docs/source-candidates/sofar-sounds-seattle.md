---
name: "Sofar Sounds Seattle"
status: blocked
platform: Custom (React SPA)
url: https://www.sofarsounds.com/seattle
tags: [Music]
firstSeen: 2026-08-01
lastChecked: 2026-08-01
---

National secret-show platform with a Seattle city page (currently
booking pop-up shows at venues like Little Ben's Rooftop Bar). The
`/seattle` page is a client-rendered SPA shell (~1KB of HTML, no
server-rendered event data or discoverable JSON API endpoint) —
consistent with other JS-only sites already logged as unreachable from
this sandbox (no headless-browser fallback available here). Also a
multi-city aggregator rather than a Seattle-first-party org, so even if
scrapable it would be `sourceRole: aggregator`. Revisit if a public API
surfaces.
