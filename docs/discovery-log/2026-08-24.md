## Source discovery: implementing existing candidate (Tech)

Scanned the backlog of `status: candidate` files for a 🔥 high-confidence pick
that could be implemented without new investigation. Seattle Tech Forum
(`docs/source-candidates/seattle-tech-forum.md`, previously investigated
2026-08-17) was already flagged high-confidence: a plain ICS feed via Luma's
API (`https://api.lu.ma/ics/get?entity=calendar&id=cal-eQcMdYUbFxXxonI`), no
custom ripper code needed.

Also checked a few other keyword-matched candidates before picking this one:
- **King County Parks Events** (Socrata JSON API) — re-verified live; all 8
  upcoming events are at Marymoor Park (Redmond), Tolt MacDonald Park
  (Carnation), Petrovitsky Park (Renton/Fairwood), and Cottage Lake Park —
  none within Seattle city limits. Fails the Seattle-focused gate; not
  implemented.
- **Dog Yard Bar** — re-checked; the `/events` page is a regular Squarespace
  content page (type 10) embedding a Calendar Block widget, not a native
  Events Collection (`itemCount: 0` via `?format=json`). Doesn't fit the
  built-in Squarespace ripper type as the candidate note assumed; would need
  re-investigation of the Calendar Block widget's data source before this
  becomes viable.

- ✅ Added: Seattle Tech Forum — `sources/external/seattle-tech-forum.yaml`
  (ICS via Luma) — 90 VEVENTs confirmed live, 23 in the future — PR pending
- 💡 Candidate: none new this cycle (implementation-only run)
- ❌ Not Viable: none flipped this cycle — King County Parks Events and Dog
  Yard Bar findings above recorded in their existing candidate files instead
  of a status flip (still worth re-investigating, not dead ends)
- 💀 Dead source flagged: none new

## Source discovery: kids/family, social dance, new venues, science lectures, skate/roller rink, maker spaces, axe/pinball, immigrant-refugee orgs; stale-candidate re-checks

Ran `dead-sources.py` first — 21 zero-event calendars, 0 external
failures. 17 of the 21 are `seattle-food-trucks-*` per-pod calendars
(all zero simultaneously), plus `frye-art-museum-frye-art-museum`,
`queen-anne-historical-society-all-events`, `external-on-the-hiyu`,
`external-seattle-childrens-museum`, `external-seattledances`,
`external-urban-league-seattle` — all already tracked in prior cycles
except the food-truck pods and Queen Anne Historical Society.
`queen-anne-historical-society` is expected (its one live "Mount
Pleasant Cemetery Walking Tours" event was Aug 22, already past — normal
gap between programs, no action). The seattle-food-trucks zero-out is
suspicious: a live check of `seattlefoodtruck.com/api/events` shows real
events today for pods the build reports as empty (e.g. Starbucks
Center, 1551 Eastlake), so this looks like a real pipeline issue (not
"no bookings") — flagging for human/build-report follow-up rather than
fixing here (out of scope for source discovery; no 30-day pattern yet
to justify a `status: dead` candidate flag).

Rotated through kids/family calendars, social/partner dance (swing,
salsa, ballroom), newly-opened venues, science lecture series, roller
rink/skate nights, maker spaces, axe throwing/pinball leagues, and
immigrant/refugee cultural orgs. Nearly every strong hit was already
tracked or implemented (Reverie Ballroom, Southgate Roller Rink [7
recurring schedules], Flight Club, RailSpur, Salsa Con Todo, Town Hall
Seattle, Pacific Science Center, Seattle Makers, North End Makers,
Seattle Creates, Blade & Timber, Axe Kickers). "Wild Axe Throwing"
(found via a pinball-league search) didn't resolve to a real Seattle
address on further searching — likely not Seattle-based; not pursued.

Given 730+ tracked candidates, spent the rest of the cycle re-verifying
the oldest still-open `candidate`/`investigating` files instead of
chasing new leads in an increasingly saturated search space:

- ✅ **Added: Seattle CityClub** — Eventbrite (config-only,
  `sources/seattle_cityclub/ripper.yaml`) — nonpartisan civic nonprofit;
  now has 1 confirmed upcoming event ("Voter Guide Live with Seattle
  CityClub & KUOW", Sep 16, at The Collective Seattle) vs. 0 on the prior
  two checks (07-11, 07-22). `EVENTBRITE_TOKEN` is already a wired
  repo secret. PR opened; candidate held at `investigating` pending CI
  event-count confirmation, per the "never merge a source that returns 0
  events" rule.
- 🔄 Re-checked, no change (still 0 upcoming / same state, `lastChecked`
  bumped to today): NCNW - Seattle Section, Urban Community Councils of
  Seattle, CoCA Seattle, Uplift Climbing, Human People Beer, Kandelia.
- 🔄 Status fix: Bellevue Arts Fair, Redmond Arts Festival — flipped
  `candidate` → `notviable`. Both are entirely Eastside events (Bellevue
  Downtown / Redmond Town Center), not "primarily Seattle" per the
  geographic-scope rule; neither has structured event data either.
- ❌ Not Viable: none new this cycle beyond the two status fixes above.
- 💀 Dead source flagged: none new (food-truck pod anomaly noted above,
  not yet at the 30-day threshold for a formal flag).
