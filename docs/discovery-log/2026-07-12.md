# Discovery Log — 2026-07-12

## Social source discovery: r/SeattleEvents

- Feed: 25 posts (11 new since last run on 2026-07-09)
- ❌ Skipped (one-off): Sci-Fi Summer Takeover at Quality Flea Center — single Eventbrite event (organizer already known: Punk Rock Flea Market = notviable, CHASM = added)
- ❌ Skipped (already covered): Little Treat Tasting — Freeze Tag Events (sources/freeze-tag-events/)
- ❌ Skipped (already covered): Polish Festival Seattle — Seattle Center Festál (sources/seattle_center_festal/)
- ❌ Skipped (content site): "Seattle's summer calendar keeps rolling" — mynorthwest.com blog post
- ❌ Skipped (already candidate): Georgetown Garden Walk — docs/source-candidates/georgetown-garden-walk.md (status: notviable, updated lastChecked)
- ❌ Skipped (one-off): 35 Years of Speed: Sonic Art Exhibit — sonic35.iam8bit.com, one-off exhibit Sept 4-6 2026
- ❌ Skipped (already covered): PhinneyWood Garage Sale Day — Phinney Neighborhood Association (sources/external/phinney-neighborhood-association.yaml)
- ❌ Skipped (one-off): Mercer Island Summer Celebration — annual one-off festival, mercerisland.gov

**No new candidates this run.**

## Source discovery: implementing the queued candidate

- ❌ Not Viable: **Seattle Dance Fitness (SDF)** — the prior top candidate
  (queued 2026-07-11) turned out to be disqualified: the studio address
  behind both live events ("SDF Studio", 1501 N 200th St, Shoreline, WA
  98133) is in Shoreline, not Seattle proper, despite the "Seattle"
  branding. `docs/source-candidates/seattle-dance-fitness.md` updated to
  `notviable`.
- Re-checked all other open `candidate` entries (CoCA Seattle, Human
  People Beer, Phinney Books, Reconnect South Park, Reverie Ballroom) —
  still 0 upcoming events via `?format=json`. NCNW, Seattle CityClub,
  Seattle Downtown Community Council, Urban Community Councils of Seattle
  — still 0-1 upcoming events (below the "not a one-off" bar). Rat City
  Roller Derby — RSS shows exactly 1 upcoming event (a July 18 fundraiser,
  off-season) and the Tribe Events ICS export is still disabled; no
  status change.
- Searched fresh verticals (comedy calendars, small art galleries, board
  game cafes, new music venues, drag shows) — nearly everything found was
  already tracked (Gallery 110, Slip Gallery, Gray Sky Gallery, Mox
  Boarding House ×2, The Missing Piece, PlaytestNW, Next Level Lounge all
  already `added`/`notviable`). Common AREA / Common OBJECTS (Belltown
  arts nonprofit) investigated — its `/events` page is a Neon CRM
  donation-tool embed, not a public event calendar; no structured feed
  found, not pursued further (no dedicated candidate file — didn't clear
  the bar to file one).
- 🔧 **Fixed a real bug in the shared `EventbriteRipper` instead of
  landing a new source** — `docs/source-candidates/ballard-comedy-takeout.md`
  was `investigating` after a reverted PR (#838) hit a bug: organizers
  using Eventbrite's repeating-event ("series") feature only return the
  series parent (no concrete date) from the organizer events list,
  tripping the "no start time" `ParseError` for every occurrence. Fixed
  `lib/config/eventbrite.ts` to expand `is_series_parent` events via
  Eventbrite's `GET /series/{id}/events/` endpoint before parsing, with
  unit coverage in `lib/config/eventbrite.test.ts`. This benefits every
  Eventbrite source using repeating events, not just this one.
  Re-attempted `sources/ballard_comedy_takeout/ripper.yaml` and
  confirmed via CI that the fix works — the calendar built with **0
  errors** (the false ParseError is gone) — but the organizer's weekly
  open-mic series has since gone dormant/ended ("Event ended" on the
  Eventbrite page as of 2026-07-12, confirmed 0 live events via both
  the CI-authenticated and public API), so it's still 0 *events*. Per
  the never-merge-a-0-event-source rule, reverted the source addition
  again and left only the verified ripper fix; candidate reset to
  `investigating` for a future re-check once/if the organizer resumes.
  PR #924.