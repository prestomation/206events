## Source discovery: candidate implementation cycle (Community/neighborhood associations)

Picked up from the existing `candidate` queue rather than running fresh
searches this cycle; triaged several previously-flagged Community/venue
candidates before implementing the highest-confidence viable one.

- ✅ Added: Madrona Neighborhood Association — custom HTML `IRipper`
  (three dedicated per-page parsers: Trick or Treat 34th Ave, Monthly
  Meetings, Music In The Playfield) — 2 events confirmed live in a local
  `ONLY_SOURCE` build (the third page's dated Tuesdays had already passed
  for the season). See `docs/source-candidates/madrona-neighborhood-association.md`.
- 🔄 Status fix: Georgetown Coalition Recurring Events → `notviable`. Both
  recurring events on `/recurring-events` (Art Attack, Farmers Market) are
  already covered by `sources/recurring/georgetown-artwalk.yaml` and
  `sources/recurring/georgetown-farmers-market.yaml`; the seasonal annual
  events are covered elsewhere or carry no scrapable date on that page.
- 🔍 Investigating / re-confirmed unchanged: Poquitos Seattle, Phinney
  Books, Dog Yard Bar (still 0 upcoming events on their Squarespace
  collections), Clean Greens Farm & Market, NCNW Seattle Section,
  Innervisions Posters & Framing (still 0 upcoming Eventbrite events),
  Grey Drizzle Fibers (still 0 Seattle-proper events, Renton/Bellevue
  only), Kandelia (Modern Events Calendar RSS feed still empty), Seattle
  Yoga News (Tribe Events REST API still `total: 0`, ICS export still an
  empty body), King County Parks Events (still county-wide, no
  Seattle-proper events), 48 North SARC (still only 1 of 9 clubs in the
  feed is Seattle-based, no per-event location to filter on), SODO BIA
  (previously found behind a SiteGround JS-challenge — not re-tested this
  cycle).
- ❌ Not viable (new finding): King County Library System Events
  (BiblioCommons SPA, `/v2/events.json` returns the HTML shell not JSON —
  would need browser automation to find the real XHR endpoint), Evergreen
  Speedway (Monroe, WA — outside Seattle city limits, borderline even
  before the TicketGoat scraping question), Emerald City Softball (games
  occur at North SeaTac Park — outside Seattle — and the 2026 schedule is
  currently a PDF, not yet posted), GeekGirlCon (single annual convention,
  no ICS/API/ticketing platform found; would need a hand-maintained
  fixed-date entry re-verified every year).

## Source discovery: outdoors/sports (climbing, running, disc golf), food pop-ups, nightlife/markets sweep (2026-09-05, second cycle)

Ran `dead-sources.py` against production first — 9 zero-event calendars
(`eastlake-community-council-all-events`, `fremont-brewing-all-events`,
`marymoor-movies-all-events`, `roam-bar-roam-bar`, `tag-redmond`,
`external-el-centro-de-la-raza`, `external-on-the-hiyu`,
`external-seattledances`, `external-shunpike`) and 2 external failures
(`discover-magnolia`, `seattle-indies`, both HTTP 403). None crossed the
30-day-dead threshold on this single snapshot alone (no historical streak
data available in this environment beyond the current build) — leaving
as-is rather than flagging `dead` without stronger evidence; worth a
follow-up with `event-history.json` if they persist.

Searched outdoor recreation, beer/brewery, food festivals, holiday
markets, Eventbrite organizer discovery, new bars, night markets, drag
venues, running clubs, new art galleries, pop-up dinners, silent disco,
warehouse/gallery spaces, car shows, climbing gyms, disc golf leagues,
and community-center calendars. The overwhelming majority of hits are
already-covered sources (Volunteer Park Trust, Reuben's Brews, Fremont
Brewing, Washington Beer Blog, Columbia City Night Market, Seattle Night
Market, Capitol Hill Running Club, West Seattle Runner, Seattle Running
Club, Flying Lion Brewing, Fleet Feet Ballard, Gray Sky Gallery, Unicorn,
Julia's on Broadway/Le Faux, National Nordic Museum, Cannonball Arts,
Seafair — whose ICS feed already covers Greenwood Car Show, Ballard
Seafoodfest, and other Seafair-family street fairs, Edgeworks, Bouldering
Project, Vertical World, The Mountaineers) or already-tracked candidates
re-surfaced by the same searches (Seattle Women in Tech, Fast Fashion
Brewing, Greenlake Brewing, Assembly Art Fair, Silent Disco NW/Secret
Sunset Seattle, Silent Dance Alki, Seattle Pop-Up Menu, MixMix Socials) —
no change to any of their statuses this cycle.

- 🔄 Re-confirmed unchanged: Momentum Climbing (SoDo) — already tracked
  (`docs/source-candidates/momentum-climbing-sodo.md`, `notviable` since
  2026-08-07); this cycle's search resurfaced it via third-party mentions
  of monthly community nights, but the site still has no events post type
  or Tribe Events REST endpoint and no ICS/RSS. No change to the file.
- 🔍 No new implementable candidate found this cycle — DiscNW's Seattle
  leagues (Jackson Park Daybreak League, mixed team leagues) are
  registration-based team sign-ups on a Blackbaud/Personify platform
  rather than spectator event listings, and single-annual events found
  (BMWs & Brunch car meetup, Seattle Interactive Conference, Wekfest
  Seattle) don't clear the recurring-calendar bar used for previous
  `notviable` single-event fairs.

## Source discovery: candidate implementation cycle (WordPress "The Events Calendar" ICS pattern)

Picked up from the existing `candidate` queue again. Several previously-flagged
candidates share a URL shape (`/event/<slug>/<YYYY-MM-DD>/`) that identifies the
WordPress "The Events Calendar" plugin, which exposes a generic `?ical=1` export
regardless of how the candidate's `platform:` frontmatter was originally
labeled (often `Unknown`). Tested that export directly against four such
candidates — all four returned live `text/calendar` feeds with real future
`VEVENT`s.

- ✅ Added: FamilyWorks Seattle — external ICS
  (`https://www.familyworksseattle.org/events/?ical=1`) — 27 events confirmed
  in a local `ONLY_SOURCE=familyworks` build across 5 Seattle sites
  (Wallingford Food Bank, Family Resource Center/Roosevelt, Green Lake and
  Broadview SPL branches, Cedar Crossing). `geo: null` + `sourceRole: venue`
  (one org's own set of sites, per-event location strings geocoded via
  Nominatim). See `docs/source-candidates/familyworksseattle-org.md`.
- 💡 Candidate (confirmed live, not yet implemented): Sunset Hill Community
  Hall — `https://sunsethillcommunity.org/events/?ical=1`, 30 VEVENTs, single
  fixed venue (3003 NW 66th St, Ballard). Sits behind FamilyWorks in this
  cycle's one-source-per-run limit.
- 💡 Candidate (confirmed live, not yet implemented): Seattle Insight
  Meditation Center — `https://seattleinsight.org/events/?ical=1`, 30
  VEVENTs, primary venue at 4001 9th Ave NE plus some online/Kirkland-hybrid
  sessions; feed includes per-event `GEO:` coordinates.
- 🔍 Investigating: Stroum Jewish Community Center (SJCC) —
  `https://sjcc.org/events/?ical=1` is live with 30 VEVENTs, but the venue
  (3801 E Mercer Way) is on Mercer Island, outside Seattle city limits —
  same Seattle-focus gate that has held back King County Parks Events.
  Left as `candidate` rather than `notviable` pending a check of whether any
  individual events are Seattle-proper.
