## Source discovery: bookstores/literary, farmers markets, new venues, neighborhood (Beacon Hill/White Center), pop-up dinners/markets, science talks (2026-08-29)

Ran `dead-sources.py` first — 5 zero-event calendars
(`downtown-seattle-association-other-events`, `sam-sam-downtown`,
`sam-sam-asian-art`, `sam-sam-sculpture-park`, `tag-folk`) and 2 external
failures (`folkdance-page`: HTTP 403, `west-seattle-blog`: HTTP 503).
Cross-checked against the last two weeks of discovery logs: the three
`sam-sam-*` calendars and `frye-art-museum` have both appeared and
disappeared intermittently since mid-July (matches the previously-noted
Browserbase proxy-credit-exhaustion pattern, not a source problem).
`downtown-seattle-association-other-events`, `tag-folk`,
`folkdance-page`, and `west-seattle-blog` are first-time appearances in
the log with no prior history — not a 30-day pattern yet, so no `dead`
flags this cycle. Worth a look next time if `folkdance-page` (403) or
`west-seattle-blog` (503) persist.

Rotated searches across underused verticals from the skill's rotation
list: bookstore/author-reading events, Seattle fall festivals, farmers
market schedules, `site:seattle.gov calendar events`, newly-opened
venues, Beacon Hill community events, White Center events, pop-up
dinner series, science/lecture talks, and vintage/pop-up markets.

Given 890+ tracked candidate files, nearly every hit was already
implemented or tracked: Seattle City of Literature community calendar
(`added`, PR #317), Readers & Writers Seattle (`notviable` — not in
Seattle city limits), Flight Club Darts SLU (`notviable` — no public
calendar, booking-inquiry page only), RailSpur Seattle (`added`,
Eventbrite), every neighborhood farmers market surfaced (Queen Anne,
Central District, Fremont, Madison Park, SLU, Georgetown) already has a
`sources/recurring/*.yaml` entry or a tracked candidate file, both
seattle.gov calendars (`seattle-gov-city-wide`, `seattle-gov-parks-recreation`)
already tracked, and Beacon Hill Festival, White Center Now, Capitol Hill
Autonomous Swap Meet (CHASM), Punk Rock Flea Market, Seattle Local
Markets, Science in Seattle, and Seattle Aquarium Lightning Talks all
already have per-candidate files.

- ✅ Added: none this cycle — candidate-list-only run, no implementation
- 💡 Candidate: **Seattle Pop-Up Menu** (seattlepopupmenu.com) — Seattle
  food/beverage pop-up calendar — genuinely new, not previously tracked.
  Investigated and left as `candidate` rather than implemented: the
  visible calendar is an embedded Notion page
  (`heliotrope-cabin-23e.notion.site`) with no ICS/RSS/public API and a
  client-side-rendered SPA that a plain fetch can't read — not
  implementable with any current ripper type without a headless
  browser. See `docs/source-candidates/seattle-pop-up-menu.md`.
- ❌ Not Viable: none new this cycle (all not-viable hits were
  re-confirmations of existing records)
- 🔄 Status fix: none this cycle
- 💀 Dead source flagged: none new (zero-event/failure set matches the
  established intermittent pattern, not a 30-day threshold)
