## Source discovery: community, discovery-oriented searches, new-venue scan

Ran `skills/source-discovery/scripts/dead-sources.py` first — 17 zero-event
calendars (all `seattle-food-trucks-*` corporate-campus stops plus already
-tracked `charlies-queer-books`, `el-centro-de-la-raza`, `on-the-hiyu`,
`shunpike`) and 2 external 403s (`discover-magnolia`, `seattle-indies`), both
previously documented and re-checked with no new evidence — no new dead-source
flags this cycle.

Searched Seattle community-center calendars, neighborhood/block-party
listings, `site:eventbrite.com` organizer discovery, new 2026 venue openings
(via Seattle Times/Seattle Met/CHS Capitol Hill Seattle Blog/The Stranger),
immigrant & refugee community orgs, night markets, craft/vintage pop-up
markets, queer/LGBTQ events calendars, spoken word/poetry, and record store
day. Most hits (Seattle Center, Seattle.gov city-wide calendar, iLoveSeattle,
Columbia City Night Market, CID Night Market, Punk Rock Flea Market, Queer
Power Alliance, Western Washington Poets Network, Seattle Gay Scene) already
have `added`/`notviable`/`blocked` candidate files from prior runs.

- 💡 Candidate: **Queer Events Seattle** (`queereventsseattle.org`) — distinct
  org/domain from the already-added Queer Power Alliance. Squarespace
  confirmed working, but `?format=json` currently returns 0 upcoming events
  (1 past). Re-check for `upcoming > 0` in a future cycle.
- 💡 Candidate: **ThriftCon Seattle** (`tickets.thriftcon.co`) — annual
  vintage/thrift vendor convention at Seattle Convention Center (Sept 12,
  2026). Custom ticketing platform, no built-in ripper type, single dated
  event/year — 🔴 Low tier, needs a multi-year date pattern before a
  recurring entry makes sense.
- ❌ Not Viable: **Loot Table Café** (new Capitol Hill board game cafe,
  opened 2026, covered by CHS Capitol Hill Seattle Blog) — Webflow site, no
  events/calendar page exists; confirmed "player-led" format (guests book
  their own tables/campaigns) rather than a venue-run schedule.
- ❌ Not Viable: **PNW Neighborhood Markets** (`pnwneighborhoodmarkets.com`)
  — Squarespace confirmed, but the org's markets are overwhelmingly in
  Edmonds (fails Seattle-focus gate) and `?format=json` shows 0 upcoming
  events at time of check.

No new source implemented this cycle — every fresh lead across the searched
verticals either resolved to an existing `sources/`/`docs/source-candidates/`
entry, had 0 current upcoming events, or failed the Seattle-focus/structured
-data quality gates above. Candidate backlog remains heavily saturated
(918+ tracked files); today's four new/updated entries keep it current.

## Source discovery: backlog re-check + implementation

Swept the existing `status: candidate`/`investigating` backlog for anything
that flipped from 0 upcoming events to live since its last check (Squarespace
`?format=json`, Eventbrite organizer API, live sitemaps) before running fresh
searches. ~25 previously-0-event Squarespace/Eventbrite candidates re-checked
live today (dog-yard-bar, human-people-beer, coca-seattle, reverie-ballroom,
uplift-climbing, poquitos-seattle, phinney-books, reconnect-south-park,
asylum-collective, nudibranch-coffee, clean-greens-farm-market,
innervisions-posters, ncnw-seattle-section, urban-community-councils-of-seattle,
ballard-comedy-takeout, and others) — all still 0 upcoming events, no change.
Also re-checked the open `8-Bit Brass Band` PR (#760, blocked on the band's
own site never publishing a future-dated show — still 3+ days stale) and
found no viable new leads from fresh searches (Halcyon Brewing, Doris,
Picklewood, Lowlander Brewing, Flight Club Darts — all either 0 events or no
public calendar; Easy Street Records' DICE API still returns 403 from this
environment, same block as the last attempt).

- ✅ Added: **Glitter and Gold Studio** (SODO jewelry studio, monthly "Open
  Sapphire Mining Night") — `eventbrite` type, PR pending. Previously
  rejected as a `sources/recurring/` candidate because the date doesn't
  follow a fixed ordinal-weekday pattern; found the real Eventbrite
  organizer ID (`57832351373`) by reading it off the event page's embedded
  JSON (the organizer's own profile page 404s) — 1 live upcoming event
  confirmed via the public API mirror.
- 🔄 Status fix: **Queer Events Seattle**, **Reconnect South Park**,
  **Asylum Collective**, **Clean Greens Farm & Market**, **Ballard Comedy
  Takeout** and ~20 other candidates re-checked live today with no change;
  `lastChecked` bumped where noted in each file.
