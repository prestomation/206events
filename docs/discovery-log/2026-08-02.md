# 2026-08-02 Discovery Log

## Social source discovery: r/SeattleEvents

- Feed: 25 posts (0 new since last run)
- No new candidates this run.

## Source discovery: cideries, makerspaces, running clubs, historical societies, chess clubs

Ran `dead-sources.py` first — 5 zero-event calendars this snapshot
(`frye-art-museum`, `kremwerk`, `sync-seattle`, `urban-family-brewing`,
`tag-sand-point`), 0 external failures. All single-snapshot, none newly
meeting the 30+ consecutive day dead-source bar — nothing flagged as
newly `dead`.

Searched 5 verticals. As in recent cycles, almost every lead was already
covered by an existing source or already tracked in
`docs/source-candidates/` (515 files going in):

- ❌ Skipped (already covered): Seattle Chess Club, Seattle Running Club,
  Capitol Hill Running Club, Green Lake Running Group, West Seattle
  Runner, Seattle Frontrunners, Historic Seattle, Seattle Makers, North
  End Makerspace, Cider Summit Seattle — all already `added` sources or
  existing tracked candidate files, no status change
- ❌ Not viable (single annual event / third-party aggregator): City
  Fruit "Celebrates" cider tasting fundraiser (one dated fundraiser/year,
  no recurring calendar), Northwest Cider Association events page
  (multi-state aggregator, not a Seattle first-party org)

One genuinely new lead surfaced from the cidery vertical:

- ✅ Added: Press Then Press Cider Events — External ICS (published
  iCloud calendar linked from the shop's site) — 3 events (a monthly
  recurring pop-up at Watershed Pub in Northgate, plus Vashon Island
  Cider Fest and Cider Summit Seattle, the latter already covered
  elsewhere and left to cross-source dedup) — verified via
  `ONLY_SOURCE=press-then-press-cider npm run generate-calendars`
  (0 errors after adding a `KNOWN_VENUE_COORDS` entry for "watershed pub
  & kitchen" — Nominatim couldn't resolve the raw ICS address string).
  `docs/source-candidates/press-then-press-cider.md`

## Implement highest-confidence candidate

Re-checked every `status: candidate` file in `docs/source-candidates/`
(~30 files) for a live upcoming-event count. Most remain at 0 upcoming
events (Squarespace `upcoming: []` or Eventbrite `upcomingEventsTotal: 0`)
since their last check: `human-people-beer`, `phinney-books`,
`reverie-ballroom`, `uplift-climbing`, `wanderlust-nursery`,
`the-cuff-complex`, `poquitos-seattle`, `coca-seattle`,
`reconnect-south-park`, `ncnw-seattle-section`, `seattle-cityclub`,
`urban-community-councils-of-seattle`, `clean-greens-farm-market`. Also
ran a fresh round of ~10 web searches across comedy, brewery, live-music,
pottery/craft, board-game, and cultural-org verticals — every new lead
found (Suya Palace, Seatown Pottery, Blue Highway Games, Mox Boarding
House, Seattle Urban Native Nonprofits) was already covered by an
existing ripper or tracked candidate.

- ✅ Added: Queen Anne Historical Society — built-in `squarespace` type —
  `?format=json` now shows 1 upcoming event ("Mount Pleasant Cemetery
  Walking Tours", Aug 22, 2026), clearing the gate after several
  0-upcoming re-checks since 2026-07-10. `geo: null` (events rotate
  across neighborhood locations). Verified via `ONLY_SOURCE=queen-anne-historical-society
  npm run generate-calendars`: 1 live event, 0 errors.
- 💡 Candidate (thin, re-check next cycle): `grey-drizzle-fibers` now down
  to 1 upcoming event (North Bend, outside Seattle — the earlier Seattle
  Flock Fiber Festival listing has passed); `queen-anne-historical-society`
  itself was borderline at just 1 event but implemented anyway given the
  zero-code built-in Squarespace type and healthy multi-year event history.
- 🔍 Investigating: `fremont-chamber-of-commerce` — the previously-noted
  `business.fremont.com/calendar` URL now 404s on a plain `curl -I` HEAD
  request but returns 200 on GET (server quirk, not dead); live page
  currently shows only 2 unique upcoming events (APDA NW Optimism Walk,
  Fremont Health & Wellness Meet Up), still below the "few events" bar
  and still requires a custom GrowthZone scraper — left as `candidate`.
- 🔍 Investigating: "Seattle Math Museum" (found via r/SeattleEvents) —
  NeonCRM-hosted (`seattlemathmuseum.app.neoncrm.com`), client-rendered
  Vue portal with no discoverable public JSON/ICS endpoint from this
  environment. Not yet a per-candidate file; revisit if a future pass
  finds the underlying API.
- ❌ Not Viable: "Eastside Alchemy Market" (found via r/SeattleEvents) —
  Eastside-based pop-up market, outside Seattle proper.
