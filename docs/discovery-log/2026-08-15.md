## Source discovery: implementing an existing candidate (Folk Dance Page Calendar)

Scheduled implementation cycle — reviewed the outstanding `status:
candidate` queue in `docs/source-candidates/` (~98 files) for the
highest-confidence unimplemented lead rather than running a fresh
web-search rotation.

**folkdance-page** was the clear pick: a previously-confirmed live ICS
feed (`folkdance.page/index.ics?date=all&country=USA&city=Seattle`),
already flagged "Highest-confidence pick in the batch" in yesterday's
log. Re-verified live: 200 OK, 333 total `VEVENT`s, 18 with a future
`DTSTART`. Simple `sources/external/` ICS entry — `geo: null`
(location strings in the feed are just `"Seattle, USA"`, not per-venue
addresses), `sourceRole: aggregator` (republishes events from multiple
dance orgs — Skandia, TryContra, etc.), tags `Dance`/`Folk` (used the
canonical `Dance` spelling from `lib/config/tags.ts` rather than the
candidate note's `Dancing`, to avoid a near-duplicate tag).

The related `folkdance-trycontra` candidate (a narrower `styles=contra`
filter on the same upstream, already `notviable`) was left as-is — its
6 events are a strict subset of the general feed now covered here.

- ✅ Added: **Folk Dance Page Calendar** — external ICS — 16 near-term
  events across the `Dance`/`Folk` tag aggregates (333 total VEVENTs in
  the raw feed) — 0 errors — PR pending.
- 🔄 Status fix: Folk Dance Page Calendar — `candidate` → `added`.

Other high-confidence candidates noted in yesterday's log (King County
Parks Socrata API, 48 North SARC, Not Creepy Gathering, Saving Great
Animals, Savoy Swing, West Seattle Indivisible, NW Dance, Sound Theatre
Company, Trinity Nightclub's Eventbrite link) remain unimplemented —
left for a future cycle per the "one source per cycle" rule.

### Dead sources

Not re-run this cycle (checked 2026-08-14, no 30-day-stale sources
newly qualified since then).
