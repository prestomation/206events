## Source discovery: music venues, running clubs, comedy/nightlife, ICS discovery, Filipino/Vietnamese/Native/circus/spoken-word/drag/improv/disability community, Eventbrite organizers, fall-seasonal — plus a full sweep of the `investigating` backlog (2026-08-31)

Ran `dead-sources.py` first — 14 zero-event calendars, 0 external
failures. 13 of the 14 are `seattle-food-trucks-*` per-pod calendars
(all zero simultaneously); this matches a pipeline issue first flagged
in the 2026-08-24 discovery log (a live check of
`seattlefoodtruck.com/api/events` shows real current events for pods the
build reports as empty) and already noted there as out of scope for
source-discovery. Roughly a week old now, not yet a 30-day dead
pattern — no new `dead` flag. The remaining zero (`frye-art-museum`)
matches the established intermittent-gap pattern from prior logs.

Rotated across three search batches: (1) music venues, running clubs,
comedy/nightlife, and ICS/RSS discovery-oriented queries; (2)
underused verticals — Filipino, Vietnamese, Native American/Indigenous,
circus/aerial arts, spoken word, drag king shows, improv theater,
silent book clubs, language exchange, disability community; (3)
Eventbrite-organizer discovery and fall-seasonal (Oktoberfest,
markets). Given 894+ tracked candidate files, the overwhelming
majority of hits across all three batches were already implemented or
tracked: Clock-Out Lounge, Conor Byrne Pub, The Vera Project, Black
Lodge, El Corazon, Skylark Café, Comedy Underground, The Rendezvous,
Queer/Bar, Sync Seattle, Climate Pledge Arena, Woodland Park Zoo, all
five seattle.gov calendars, Green Lake Running Group, Kremwerk,
Seattle Urban Native Nonprofits, Disability Empowerment Center,
Vietnamese Cultural Center, Bandit Improv, Unexpected Productions,
United Indians/Daybreak Star, Seattle City of Literature, Urban Craft
Uprising, Joyeux Bazaar, New Tech Northwest, Seattle Uncorked, Seattle
Christmas Market.

Also swept the full `investigating`-status backlog (40 files) for any
that might now be reachable or worth a second look: retried
sgcaptcha-blocked leads (Seattle Indivisible, New Seattle Waterfront,
Meaningful Movies, Gingers Pet Rescue) and JS-rendered ones (Raygun
Lounge, Boneyard Seattle) — all still blocked/unreachable the same way
as their last check, no change in disposition. Corrected Boneyard
Seattle's platform note (it's Wix, not Squarespace). Re-checked two
low-volume `candidate` leads still sitting at 0/1 confirmed upcoming
events (Poquitos Seattle, Stone Way Cafe) — no change; identified Stone
Way Cafe's calendar platform ("Spot" widget, server-rendered with a
machine-readable date attribute) for a future implementation attempt
once volume picks up.

Eight genuinely new leads investigated to a disposition:

- **FANHS Greater Seattle Chapter** (fanhs-seattle.org) — `/events/` is
  a static prose page pointing to a blog category, not a dated
  calendar. `notviable`.
- **SANCA** (sancaseattle.org) — has a real, working Tribe Events ICS
  feed (6 VEVENTs, confirmed future dates), but every entry is a
  school-administrative date (closures, enrollment periods) — no
  audience-facing shows/performances. `notviable`.
- **Versatile Arts** (versatilearts.net) — WordPress "Seattle-Area
  Circus Events" hub; calendar page has no server-rendered event data,
  JS-rendered widget. `investigating`.
- **Emerald City Trapeze** (emeraldcitytrapeze.com) — real SoDo
  trapeze/aerial venue with recurring annual shows (Carnevolar), but
  only 1 confirmed future date at time of check. `candidate`, 🔴 low
  priority pending more volume.
- **November Project Seattle** — main site blocked by SiteGround
  captcha; Meetup page is client-rendered React with no scrapable data.
  `blocked`.
- **Joe's Bar & Grill** (Chinatown-ID comedy open mic) — no dedicated
  venue website, only third-party aggregator listings. `notviable`.
- **Progressive Entrepreneur** (Eventbrite) — turned out to be a
  franchised, identically-templated "[City] Biggest Business, Tech &
  Entrepreneur Networking Soiree" event replicated across many world
  cities — a generic networking-event mill, not a genuine Seattle org.
  `notviable`.
- **WomenHack Seattle** / **Startup Grind Seattle** (Eventbrite) — no
  `EVENTBRITE_TOKEN` available in this environment to query the API and
  confirm real Seattle-scoped events or resolve ambiguous organizer
  IDs (WomenHack has 3 candidate org IDs; Startup Grind's ID looks like
  the global parent account). Both left `investigating` for a future
  cycle with API access.

- ✅ Added: none this cycle — no candidate cleared the quality gate
  (sufficient confirmed event volume + reachable structured data)
- 💡 Candidate: Emerald City Trapeze — custom HTML — 🔴 low confidence
  (1 confirmed upcoming show) — https://emeraldcitytrapeze.com/shows/
- ❌ Not Viable: FANHS Greater Seattle Chapter — blog category page, no
  dated events; SANCA — feed exists but only carries admin/enrollment
  dates, no public events; Joe's Bar & Grill — no venue website;
  Progressive Entrepreneur — generic multi-city networking-event mill
- 🔄 Status fix: Boneyard Seattle — platform corrected Squarespace →
  Wix
- ⛔ Blocked: November Project Seattle — SiteGround captcha + Meetup
  client-rendered
- 🔍 Investigating: Versatile Arts, WomenHack Seattle, Startup Grind
  Seattle — all need a JS-capable fetch or API access to evaluate
  further
- 💀 Dead source flagged: none new (food-truck zero-out and
  frye-art-museum both match established non-30-day patterns)

## Source discovery: implementing existing candidate (dance)

Picked the highest-confidence unimplemented 💡 candidate from
`docs/source-candidates/` rather than running fresh searches this cycle:
**NW Dance** (`nwdance.net`), previously investigated 2026-08-14 as a
"Modern Events Calendar Lite" WordPress site with good event volume across
several Ballard-area halls, flagged to check for an ICS export before
resorting to HTML scraping.

No Tribe-Events-style `?ical=1`/`?mec-ical-export=1` endpoint exists for MEC,
but WordPress auto-registers a native RSS feed at `/events/feed/`, and MEC
extends it with structured `mec:startDate`/`mec:startHour`/`mec:endDate`/
`mec:endHour`/`mec:location`/`mec:cost` fields per item — no HTML scraping
needed after all. Implemented a custom `IRipper`
(`sources/nw_dance/ripper.ts`) that parses the feed directly. The feed's
`mec:location` is a bare venue name with no address, so the ripper maps the
two venues seen so far (Leif Erikson Hall, Sunset Hill Community Hall) to
full street addresses for precise geocoding, falling back to the bare venue
name for the geocoder to resolve on its own if a new venue appears.

Verified live via `ONLY_SOURCE=nw-dance npm run generate-calendars`: 10
events, 0 parse errors, 0 unresolvable geocodes.

- ✅ Added: NW Dance — custom RSS ripper — 10 events — PR pending
- 💡 Candidate: none new this cycle — candidate-implementation-only run
- ❌ Not Viable: none new this cycle
- 🔄 Status fix: NW Dance — `candidate` → `added`
- 💀 Dead source flagged: none new this cycle
