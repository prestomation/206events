# 2026-07-16 Discovery Log

## Social source discovery: r/SeattleEvents

- Feed: 25 posts (4 new since last run)
- ❌ Skipped (one-off): Luma event "the eldest daughters walk — seattle" — single community walk on July 25, organized by Cherry (women's health brand). Not a recurring source. — https://luma.com/pm7xkcjc (via [Reddit post](https://old.reddit.com/r/SeattleEvent/comments/1uxl94g/eldest_daughters_of_seattle_unite/))
- ❌ Skipped (no structured data): NARN (Northwest Animal Rights Network) — events page at https://narn.org/events/ is JS-rendered with no visible events in static HTML. No scrapable structured data. — via [Reddit post](https://old.reddit.com/r/SeattleEvent/comments/1ux7irh/karaoke_event_july_26th/))

No new candidates this run.

## Source discovery: re-checks + music/arts/books/comedy/gaming/community verticals

Re-checked several open `candidate` sources for newly-populated event calendars
(all still 0 upcoming events at time of check, no status change): Phinney
Books, Human People Beer, CoCA Seattle, Reconnect South Park, Queen Anne
Historical Society (Squarespace `?format=json`), NCNW Seattle Section,
Seattle CityClub, Urban Community Councils of Seattle, Clean Greens Farm &
Market, Seattle Street Food Festival (Eventbrite `__NEXT_DATA__`/organizer
API), Gasworks Brewing (still no events collection), Ballard Comedy Takeout
(Eventbrite organizer still 0 live events; the shared `EventbriteRipper`
series-expansion fix from 2026-07-12 remains in place and correct — it's
this specific organizer's series that's gone dormant, not the pipeline).

- ✅ Added: Charlie's Queer Books — custom JSON ripper (BookManager
  storefront API) — 17 events — `sources/charlies_queer_books/`. Resolved
  the missing `store_id` blocker from the 2026-07-10 investigation by
  finding the `store/getSettings` call in the site's JS bundle, which
  resolves the public `webstore_name` (`9932925`) to the real internal
  `store_id` (`1188985`); then `session/get` + `event/v2/list` (this store
  uses BookManager's v2 events API) return the dated event list. All calls
  are public/unauthenticated. Verified via
  `ONLY_SOURCE=charlies-queer-books npm run generate-calendars`.
- ❌ Not viable (checked, no change): PNW Axe Throwing (Squarespace
  `events-one` collection, `itemCount: 0`), Axe Kickers (Wix, no built-in
  ripper type), Blade & Timber (static/Netlify, no events data), Mox
  Boarding House / Meeples Games / Blue Highway Games (already tracked,
  `notviable`/`added` via recurring YAML), Needlehook (WordPress/Tribe
  Events fiber-arts calendar covering Western WA broadly — sampled 50 of
  4528 events, only ~14% in Seattle proper vs. Bellingham/Mount
  Vernon/Stanwood/etc.; fails the Seattle-focus gate), Greg Kucera Gallery
  (Squarespace `events-stacked` collection, `upcoming: []`, 19 past —
  logging as new candidate below rather than `notviable` per the "200 + 0
  events" rule), Sound Generations senior-services calendar (HTTP 403,
  blocked from this environment).
- 💡 Candidate: Greg Kucera Gallery — `https://gregkucera.com/events` —
  Squarespace confirmed, real `events-stacked` collection, but 0 upcoming
  events at time of check (19 past, most recently a First Thursday event).
  Re-check next cycle. Filed at `docs/source-candidates/greg-kucera-gallery.md`.
- ❌ Already covered (checked, no action): Filipino Community of Seattle,
  Friends of Little Saigon, Southgate Roller Rink / Roll Around Seatown,
  GO Latin Dance Seattle (Sueños de Salsa etc.), Century Ballroom, Swing
  Dance SCT, Club Comedy Seattle, Laughs Comedy Club, Third Place Books,
  Seattle City of Literature, CISC Seattle, Communities of Opportunity,
  Wing Luke Museum, National Nordic Museum, MOHAI.