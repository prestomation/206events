## Source discovery: implementing staged candidates

- ✅ Added: Jewish Federation of Greater Seattle — ICS external calendar — `sources/external/jewish-federation-of-greater-seattle.yaml`

## Source discovery: Comedy/trivia/community sweep, bookstores, Eventbrite organizers

Dead source check: `skills/source-discovery/scripts/dead-sources.py` (Python
`urllib` gets a `403` from the deployed site directly in this environment;
worked once fetched via `curl` — proxy env vars aren't picked up by bare
`urllib.request`, unlike `curl`). Fetched `https://206.events/build-errors.json`
with `curl` and ran the script against the local copy: **0 zero-event
calendars**; 2 external failures (`worksource-downtown-seattle`,
`worksource-north-seattle`, both `HTTP 403`) — both already tracked in
`pendingProxyVerification` (2 consecutive failures each, `outofband` rung,
`recommendation: verifying`), which is `skills/proxy-escalation` territory,
not a new dead-source flag.

Verticals searched: comedy open mic, trivia nights, Eventbrite Seattle
organizers, community centers/block parties, farmers markets, new Seattle
venues, bookstore author readings, ICS/subscribe feeds, night markets/craft
fairs, drag show venues.

- 💡 Candidate: **Queen Anne Book Company** — Custom HTML (Drupal/
  IndieCommerce) — `https://qabookco.com/events` — 4 confirmed upcoming
  events (book club + author events) — 🔥 High confidence
- 💡 Candidate: **Seattle Social Club** — Eventbrite (`organizerId:
  42795102333`) — 2 confirmed upcoming events (social mixers, board game
  night) — 🔥 High confidence
- 💡 Candidate: **Sync Seattle** — Eventbrite (`organizerId: 45161572473`)
  — 1 confirmed upcoming event (monthly "Vibes After 5" mixer for Black
  Seattle and allies) — 🟡 Medium confidence (low, but real, monthly volume)
- 💡 Candidate: **Phinney Books** — Squarespace — `upcoming: 0` events at
  time of check (only past events populated); kept as candidate to
  re-check next cycle rather than marked not-viable
- 🔄 Status fix (no new candidate — already covered): C-ID Night Market
  (Sept 26-27, 2026, Chinatown-International District) is run by CIDBIA,
  whose `/local-events` page is already scraped by `sources/cidbia`; not
  yet listed there (event is ~3 months out) but expected to surface
  through the existing aggregator once posted, no separate source needed.
- 🔄 Status fix (no new candidate — already covered): Phinney Neighborhood
  Association / PhinneyWood Winter Festival confirmed still live via the
  WordPress Tribe Events REST API (145 pages of events) — already covered
  by `sources/external/phinney-neighborhood-association.yaml` and
  `sources/recurring/phinneywood-winter-festival.yaml`.
- ❌ Not Viable (re-confirmed, no change): most comedy/trivia/drag leads
  from this sweep were already `added` or `notviable` in prior cycles —
  Emerald City Comedy Club, Club Sur, The Admiral Pub, Hopvine Pub (open
  mic + trivia), Unicorn, Le Faux/Julia's on Broadway, Dreamland, Seattle
  Night Market, Third Place Books, Elliott Bay Book Co., University Book
  Store, Cannonball Arts, Flight Club Seattle, Next Level Lounge,
  Picklewood, Third Place Commons — all confirmed already covered or
  already evaluated candidates, no action taken.
