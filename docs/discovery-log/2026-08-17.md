## Source discovery: music venues, city calendars, tech meetups, family events, new venue openings, breweries, cultural festivals, senior centers, sustainability

Scheduled discovery cycle. Ran `dead-sources.py` first — 22 zero-event
calendars and 1 external failure (`west-seattle-blog`, empty HTTP 200 body),
all already known/tracked from prior runs (see `docs/discovery-log/dead-sources.md`
and the 2026-08-16 log's note on `frye-art-museum-frye-art-museum`). No newly
30-day-stale sources identified this cycle, so no fresh `💀 Dead source` flags.

Nine web searches across verticals not covered by the last several runs
(which focused on community/nonprofits/art-walks, outdoors/bookstores/
comedy/eventbrite, and art-galleries/distilleries/roller-derby/nightlife/
breweries/coworking/fiber-arts): Seattle live-music-venue calendars,
`site:seattle.gov` calendars, tech meetups, family/kids events, new venue
openings, brewery release calendars, cultural heritage festivals, senior
center calendars, and environmental-sustainability nonprofit events.

The large majority of leads resolved to sources already `added` or already
tracked as candidates: SeatGeek/Songkick/ConcertFix/JamBase (national
aggregators, out of scope), all seattle.gov calendar pages (confirmed 410
Gone historically, see `dead-sources.md`), GeekWire, New Tech Seattle,
RailSpur, Seattle Chamber Music Society (all `added`), Flight Club Darts
S. Lake Union, Flight Club Seattle, ParentMap, Luma Seattle generic listing
(all `notviable`), Ballard Brewed Coalition, Fremont Brewing, Reuben's
Brews, Seattle Beer Week, Seattle Center Festál (all `added`), SouthEast
Seattle Senior Center (`blocked`), Sustainable Seattle (`notviable`).

New candidate files written this cycle:

- 💡 Candidate (🔥 High): **Seattle Tech Forum** — the org's own
  `/events` page embeds a Luma calendar (`lu.ma/seattletechforum`) rather
  than a native Squarespace Events Collection. Resolved the underlying Luma
  `cal-*` id from the rendered page and confirmed
  `https://api.lu.ma/ics/get?entity=calendar&id=cal-eQcMdYUbFxXxonI` returns
  a valid ICS feed — 91 events, dated well into 2027. Plain
  `sources/external/` ICS candidate, no custom ripper code needed. Not
  implemented this cycle (discovery/candidate-list-only run).
- 💡 Candidate (🔥 High): **The Center for Active Living** (formerly Senior
  Center of West Seattle, 4217 SW Oregon St) — `wscenter.org/calendar/?ical=1`
  is a working native ICS export, confirmed 100 events (fitness classes,
  computer lab, line dancing, support groups, community dining) at a single
  fixed address, dated through 2027. Plain `sources/external/` ICS
  candidate. Not implemented this cycle.
- 🔍 Investigating: **Kids Out and About Seattle** — regional
  EntertainmentCalendar.com franchise site; homepage is evergreen
  guide/article content (`/content/<slug>`), no `/calendar` path (404) and
  no RSS/iCal feed link found on a quick pass. Needs a deeper crawl before
  it's ruled in or out.
- 🔍 Investigating: **Macaroni KID North Seattle-Shoreline** — local
  franchise of the national Macaroni Kid network; `/events?format=json`
  isn't a real JSON collection (custom platform), no obvious API found.
  Would need `HTMLRipper` scraping and volume/geo verification (straddles
  Seattle/Shoreline) before implementing.
- 🔄 Status check: **Seattle's Child Calendar** (`status: candidate` since
  2026-08-14) — re-checked for a WordPress Event Calendar Pro iCal export
  (`?ical=1`) as noted in its file; the URL returns HTML, not an ICS feed.
  Left as `candidate` — still needs an `HTMLRipper` if pursued, no change
  in confidence.

No implementation this cycle — scoped to source discovery and candidate-list
maintenance. The two 🔥 High-confidence ICS candidates above (Seattle Tech
Forum, The Center for Active Living) are the top picks for the next
implementation cycle.

## Source discovery: implementing existing candidates (community/pets)

- ✅ Added: Saving Great Animals — ICS (Tribe Events) — PR #1222
