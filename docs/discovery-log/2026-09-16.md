## Source discovery: implementing another candidate (Green Seattle Partnership)

Scanned the backlog for a candidate with real, currently-verifiable event
data rather than starting a fresh web search — most Eventbrite/Squarespace
candidates already on file (`clean-greens-farm-market`,
`innervisions-posters`, `ncnw-seattle-section`,
`urban-community-councils-of-seattle`, `ballard-comedy-takeout`,
`nudibranch-coffee`, `asylum-collective`, `reverie-ballroom`) still show 0
live events as of today, and several `investigating` candidates remain
hard-blocked by SiteGround sgcaptcha challenges (`meeples-games`,
`gingers-pet-rescue`) or Wix's opaque signed Events widget
(`blue-moon-tavern`, `boneyard-seattle`, `southside-revolution`,
`axe-kickers`, `cascade-flag-football`) that aren't scrapable without
heavy reverse-engineering.

- ✅ Added: **Green Seattle Partnership** — citywide volunteer
  forest/park-restoration work parties run by Forterra's Green Seattle
  Partnership program. The org's own `greenseattle.org` marketing site is
  sgcaptcha-blocked (previously flagged in
  `docs/source-candidates/green-seattle-volunteer.md`), but its actual
  event platform, `seattle.greencitypartnerships.org`, is a plain,
  unprotected HTML calendar listing 30-40 upcoming work parties at any
  time across 30+ named Seattle parks/greenspaces. Implemented as a custom
  `IRipper` (`sources/green_seattle_partnership/`), `sourceRole: venue`
  (first-party organizer of its own program, same shape as `spl`), `geo:
  null` (per-event park-name locations, standard geocoding pipeline),
  tags `["Volunteer", "Outdoors"]`. Every event is parsed with `cost: {
  min: 0 }` directly (always free). 35 events, 0 parse errors verified via
  `ONLY_SOURCE=green-seattle-partnership`. See
  `docs/source-candidates/seattle-greencitypartnerships-org.md`.
- 🔄 Status fix: **Green Seattle Volunteer** (`greenseattle.org`) —
  flipped to `added`, pointing at the canonical
  `seattle-greencitypartnerships-org.md` record above; the marketing
  domain itself remains sgcaptcha-blocked and was never the real fetch
  target.

## Source discovery: implementing a candidate (First Hill Improvement Association)

Swept a wide slice of the existing `docs/source-candidates/` backlog
(`candidate`/`investigating` status) live before starting fresh searches —
most previously-flagged high-confidence picks (Squarespace/Eventbrite
sources noted with real upcoming events in earlier cycles) are back to 0
upcoming events on re-check: Clean Greens Farm & Market, NCNW Seattle
Section, Innervisions Posters (Eventbrite API `object_count: 0` on all
three), Phinney Books, Poquitos Seattle, Queer Events Seattle, Dog Yard Bar
(`/events-all` still dead since ~Dec 2024), 48 North SARC (still
deprioritized — regional feed, only Sloop Tavern YC is Seattle-proper and
that's already implemented separately), Grey Drizzle Fibers (Renton-based,
0/2 current events in Seattle), King County Parks Events (still outside
Seattle city limits). No change on any — filed as-is.

Also investigated the three Seattle Colleges campus calendars
(`docs/source-candidates/seattle-colleges-events.md` umbrella entry) —
North Seattle College uses a stale/misconfigured Trumba spud embed
(`webName: "auction-fundraiser"`, a demo placeholder that 410s), while
Seattle Central and South Seattle reference real Trumba webNames
(`scc_newscenter`, `new-south-all-events`) but the `trumba.com/calendars/*`
ICS/page endpoints for those names return 410 Gone / 404 — the CollegeNET
25Live-backed widgets appear to have migrated off public Trumba URLs.
Left as `investigating`, not filed as separate per-campus candidates yet
since none of the three have a reachable feed to record.

Also checked: PCC Markets cooking classes/events (client-rendered grid, no
JSON/API route found in static HTML — `investigating`), Wallingford
Chamber of Commerce (`wallingfordchamber.org` — Cloudflare challenge, 403
from this environment, not stageable), Uptown Alliance (Wix SPA, no
server-rendered event data), Scarecrow Video (still 429/Vercel bot
challenge, unchanged from prior check).

- ✅ Added: **First Hill Improvement Association**
  (`firsthill.org/neighborhoodcalendar`) — neighborhood community calendar
  covering First Hill venues (Frye Art Museum, Stimson-Green Mansion, Hotel
  Sorrento, Freeway Park) plus FHIA's own committee meetings. Confirmed
  Squarespace, but this collection uses Squarespace's **calendar-view**
  pagination (month-by-month via `pagination.nextPage`/`nextPageUrl`,
  returning an `items` array per month) rather than the simpler
  `upcoming`/`past` split most candidates in this backlog have failed on —
  the existing built-in `squarespace` ripper type already supports this
  shape (`items` fallback + string-valued `nextPage` pagination in
  `lib/config/squarespace.ts`), so no code changes were needed. Implemented
  as `sources/first_hill_improvement_association/ripper.yaml`,
  `sourceRole: aggregator` (matching the `visit-ballard` precedent),
  `geo: null`. 33 upcoming events, 0 parse errors, verified via
  `ONLY_SOURCE=first-hill-improvement-association npm run generate-calendars`.
  See `docs/source-candidates/first-hill-improvement-association.md`.
- 🔍 Investigating: North/Central/South Seattle College campus calendars —
  Trumba/25Live-backed widgets, no reachable public feed found this cycle.
- 🔍 Investigating: PCC Markets classes/events — client-rendered, no API
  endpoint found yet.
- ⛔ Blocked: Wallingford Chamber of Commerce — Cloudflare challenge (403)
  on both `/events/` and the Tribe Events ICS export from this environment.
- 🔄 Status fix: re-confirmed 0 change on Clean Greens Farm & Market, NCNW
  Seattle Section, Innervisions Posters, Phinney Books, Poquitos Seattle,
  Queer Events Seattle, Dog Yard Bar, 48 North SARC, Grey Drizzle Fibers,
  King County Parks Events.

## Source discovery: implementing a candidate (34th District Democrats)

Swept another slice of the `candidate` backlog live rather than starting a
fresh web search: Innervisions Posters/Seattle Records Eventbrite organizer
(`object_count: 0`), Queer Events Seattle and Nudibranch Coffee (Squarespace,
both `upcoming: 0`), Clean Greens Farm & Market (Eventbrite organizer still
`object_count: 0` — Harvest Dinner not yet posted), Rainbow City Softball
(schedule now released but only as a Google Sheets link, not a scrapable
feed), Emerald City Softball (`?ical=1` Tribe-style export returns 0 events;
LeagueApps league page has no public ICS/API), League of Women Voters of
Washington (Wild Apricot RSS feed works and has 100+ live items, but it's a
statewide org and nearly all entries are internal committee/affinity-group
Zoom meetings rather than public Seattle events — deprioritized), League of
Women Voters King County (`lwvskc.org` 403s from this environment, not
stageable), Puget Soundkeeper marine debris cleanups (`/events/` 403s), 34dems
`/news-events` page itself (Cloudflare/bot-check challenge page, unreadable).

- ✅ Added: **34th District Democrats** — West Seattle/White
  Center/Vashon/Burien legislative district Democratic party. No ICS/API,
  but the org's own `/minutes/` page states an authoritative cadence
  ("Membership Meetings are generally held on the second Wednesday of each
  month... open to the public"), corroborated by the live homepage's "next
  meeting" banner and multi-year (2019–2026) minutes history all landing on
  2nd Wednesdays. Meetings are currently Zoom-only (no fixed venue), so
  implemented as `sources/recurring/34th-district-democrats.yaml` (`geo:
  null`, `sourceRole: venue`) rather than a scraper — same shape as
  `sources/recurring/downtown-seattle-community-council.yaml`. Tags
  `Political`, `West Seattle`. 1 event verified via
  `ONLY_SOURCE=34th-district-democrats npm run generate-calendars`. See
  `docs/source-candidates/34th-dems.md`.
- 🔄 Status fix: re-confirmed 0 change on Innervisions Posters, Queer Events
  Seattle, Nudibranch Coffee, Clean Greens Farm & Market.
- 🔍 Investigating: Rainbow City Softball (schedule is a Google Sheet, not a
  feed), Emerald City Softball (no working ICS/API found yet).
- ⛔ Blocked: League of Women Voters King County (`lwvskc.org`, 403), Puget
  Soundkeeper (`pugetsoundkeeper.org/events/`, 403).

## Source discovery: implementing a candidate (Union Seattle)

Scanned the `candidate` backlog for a source with data verifiable without a
fresh web search. Re-checked the two 🔥/🟡-tier picks first — both still 0
upcoming events: Poquitos Seattle (Squarespace `?format=json`, `itemCount:
16`, `upcoming` empty, 14 `past`) and Queer Events Seattle (`upcoming: 0`,
`past: 1`). Grey Drizzle Fibers and LWV Washington remain deprioritized per
prior notes (not primarily Seattle). `growlerzseattle.com` has a broken TLS
cert (unable to get local issuer certificate) — not fetchable from this
environment, left as-is.

- ✅ Added: **Union Seattle** — LGBTQ+ bar/nightclub at 1009 E Union St,
  Capitol Hill. No ICS/API (Wix site, no dedicated events calendar), but the
  homepage states two weekly specials with explicit hours: Wing Wednesdays
  (4–10 PM, $5 wings) and Sunday Funday (brunch noon–4 PM, patio parties all
  day). Implemented as two `sources/recurring/` entries —
  `union-seattle-wing-wednesdays` and `union-seattle-sunday-funday` — same
  one-file-per-named-event pattern as `ccs-seattle-gaymers` /
  `ccs-seattle-gathering-happy-hour` at CC's. Skipped "Team Tuesdays" (named
  on the page but no stated time — would be a guess) and the "2026 PRIDE
  Block Party" (dated June 2026, already past). Geocoded via Nominatim
  (`47.6127716, -122.3189306`, OSM node `2159322642`, matches the venue's
  Google Maps pin). Tags `Nightlife`, `LGBTQ` (matching
  `queer_social_club/ripper.yaml`'s convention), `Capitol Hill`. 1 event per
  calendar, 0 parse errors, verified via
  `ONLY_SOURCE=union-seattle-wing-wednesdays,union-seattle-sunday-funday npm run generate-calendars`.
  See `docs/source-candidates/union-seattle.md`.
- 🔄 Status fix: re-confirmed 0 change on Poquitos Seattle, Queer Events
  Seattle.
- ⛔ Blocked: `growlerzseattle.com` — broken TLS certificate on the site
  itself (not a proxy/network issue), unreachable from this environment.

## Source discovery: implementing a candidate (Salle Auriol)

Reviewed the `candidate` backlog's existing notes for the highest-confidence
Squarespace/Eventbrite picks (Dog Yard Bar, Human People Beer, Phinney
Books, Poquitos Seattle, Queer Events Seattle, Reconnect South Park,
Reverie Ballroom, Uplift Climbing, Clean Greens Farm & Market, Innervisions
Posters, NCNW Seattle Section, Urban Community Councils of Seattle) — all
still recorded at 0 upcoming events as of their most recent check this
cycle, none re-fetched live this pass. Also ran several fresh web searches
(bookstore/comedy/brewery/gallery/night-market/record-fair verticals) —
every lead that surfaced (Third Place Books, Elliott Bay, Laughs Comedy,
Club Comedy, Emerald City Comedy, Mockingbird Comedy, Greg Kucera Gallery,
Museum of Museums, Flight Club (both), Slip Gallery, Seattle Christmas
Market, Admiral Pub, Skylark, The Beer Junction, Corner Pocket, Columbia
City Night Market, CID Night Market, CHASM, Punk Rock Flea Market, Columbia
City BIA/Gallery) turned out already `added`/`notviable`/`blocked`/`proxy`
in prior cycles — this repo's candidate backlog is thoroughly picked over.
`jetcitylabs.com` (from a fresh search) is sgcaptcha-blocked (SiteGround JS
challenge) even from this environment — left `blocked`, not staged (per the
"fails locally too" rule, nothing to prove).

- ✅ Added: **Salle Auriol** (Seattle Fencing Academy) — Interbay/Queen Anne
  fencing club, 1419 Elliott Avenue West. The Squarespace `/calendar` page
  is a static shell; the real schedule lives in an embedded
  `seattlefencing.sites.zenplanner.com` calendar iframe that's
  client-rendered (confirmed via headless-browser inspection — no event
  data in the plain-fetched HTML), so not a fit for this repo's fetch-only
  ripper infrastructure. The weekly pattern is stable and well-documented
  though (verified across 4 consecutive weeks + per-session duration from
  individual ZenPlanner enrollment pages), so implemented as two
  hand-coded `sources/recurring/` entries instead — Open Fencing (Saber)
  Tue/Thu/Fri 7:30–9pm at Main Location, and Open Fencing (Epee/Foil)
  Mon/Wed 7:30–9:30pm + Sat 10:30am–12:30pm Downstairs. Tags `Sports`,
  `Interbay`. Geocoded via Nominatim (`47.6307008, -122.3743874`, OSM node
  `2462428041`). 3 events per calendar, 0 errors, verified via
  `ONLY_SOURCE=salle-auriol-open-fencing-saber,salle-auriol-open-fencing-epee-foil npm run generate-calendars`.
  See `docs/source-candidates/salleauriol-com.md`.
- ⛔ Blocked: `jetcitylabs.com` — SiteGround sgcaptcha JS challenge even
  from this environment (not just CI), so nothing to stage. See
  `docs/source-candidates/jetcitylabs-com.md`.

## Source discovery: implementing a candidate (Whim W'Him Seattle Contemporary Dance)

Scanned the `candidate`/`investigating` backlog for a source verifiable
without a fresh web search, since most Squarespace/Eventbrite picks noted in
earlier cycles today are already re-confirmed at 0 upcoming events. Re-vetted
the pre-flagged 🔥 High-confidence files (`aaf-seattle`,
`center-for-active-living-west-seattle`, `disability-empowerment-center`,
`fog-room`, `jewish-federation-of-greater-seattle`, `lovestruck-in-seattle`,
`mr-bs-meadery`, `plant-amnesty`, `queen-anne-book-company`,
`seattle-cocktail-club`, `seattle-social-club`, `seattle-tech-forum`,
`the-cuff-complex`, `west-seattle-blog`) — all already `status: added`.
The three remaining 🔥-tagged-but-unresolved files are genuinely blocked:
`ballard-comedy-takeout` (Eventbrite series-expansion bug fixed in a prior
cycle, but the organizer's weekly open mic has gone dormant — `object_count:
0`), `olympicathleticclub-com` (JS-rendered class-schedule widget, not a
one-off event calendar), `teentix` (Craft CMS calendar grid with no
per-event data in static HTML, plus a real cross-source-dedup risk as a pure
aggregator). Also re-checked several `Squarespace`-platform `candidate`
files with real event *counts* (`asylum-collective`, `coca-seattle`,
`grey-drizzle-fibers`, `nudibranch-coffee`, `phinney-books`,
`poquitos-seattle`, `reconnect-south-park`) — all still 0 upcoming or
Seattle-outside per their own notes, no change.

- ✅ Added: **Whim W'Him Seattle Contemporary Dance** — itinerant Capitol
  Hill-area contemporary dance company. Its WordPress `/calendar/` page
  looked like a dead end in the original 2026-05-08 investigation (Tribe
  Events ICS export disabled), but re-probing the page found it actually
  embeds an **OvationTix** ticketing widget
  (`ovationtix.com/35510/production/1270644`) — the same built-in platform
  already used by `taproot`, `sound_theatre_company`, and `the_feast`.
  Confirmed the public calendar API
  (`https://api.ovationtix.com/public/calendar/client(35510)` with an
  `Origin: https://whimwhim.org` header) returns `clientName: "Whim W'Him"`,
  `clientActive: true`, and real Fall '26 season performances at Erickson
  Theater. Implemented as `sources/whim_whim/ripper.yaml` (itinerant company,
  `geo: null`, matching the `the_feast`/`sound_theatre_company` pattern),
  tags `Dance`, `Arts`. 7 events, 0 parse errors, verified via
  `ONLY_SOURCE=whim-whim npm run generate-calendars`. See
  `docs/source-candidates/whim-whim-seattle-contemporary-dance.md`.
- 🔄 Status fix: re-confirmed 0 change on `ballard-comedy-takeout`,
  `asylum-collective`, `coca-seattle`, `grey-drizzle-fibers`,
  `nudibranch-coffee`, `phinney-books`, `poquitos-seattle`,
  `reconnect-south-park`.

## Source discovery: implementing a candidate (Machine House Brewery)

Re-checked the backlog live rather than starting a fresh web search: PR
#760 (8-Bit Brass Band, blocked on upstream content) still shows the
newest Wix sitemap date as 2026-09-04 — still past today
(2026-09-16), so it stays as-is, blocked, not touched. Also fetched
`needlehook.com` (PNW fiber-arts community calendar, ICS confirmed
working) live — still exactly the same 2 Bellingham-only entries, no
Seattle listings; stays `notviable` for Seattle. `secretgardenbooks.com/
upcoming-events` still 403s from this environment (unchanged since
2026-09-11). Ran fresh searches across beer-release, food-truck,
Eventbrite-organizer, new-brewery, record-store, and yarn-shop verticals
— every real lead surfaced (Where Ya At Matt, Sync Seattle, Acorn
Street Shop, other yarn shops) was already `added` in a prior cycle.

- ✅ Added: **Machine House Brewery** (Bluegrass Jam) — previously
  filed as `investigating`/`candidate` (`machinehousebrewery-com.md`)
  because the WordPress `/calendar/` page's event data is loaded by a
  JS-rendered widget with no visible signature. Found the widget is
  **Elfsight's "Event Calendar" app**, whose config API
  (`core.service.elfsight.com/p/boot/?page=<url>&w=<widget-id>`) is
  public JSON with no auth needed — no headless browser required. Of
  104 events in the feed, 101 are one-off sports watch parties (all
  historical, 0 future) and 2 of the 3 weekly-recurring entries have a
  `repeatEndsDate` in 2024 (series ended). Only **Bluegrass Jam** (every
  Wednesday, 7–9:30pm, free, all ages) has `repeatEnds: "never"` — still
  live. Implemented as `sources/recurring/machine-house-brewery.yaml`
  (`sourceRole: venue`, `cost: free`, tags `Music`/`Rainier Valley`),
  same shape as `owl-n-thistle-jazz-jam`. Also corrected the venue's
  address in passing — it moved to 5718 Rainier Ave S (Hillman City)
  since this candidate was first filed; confirmed via the site's own
  `/contact/` page and an exact OSM node match. 1 event, 0 parse errors,
  verified via
  `ONLY_SOURCE=machine-house-brewery-bluegrass-jam npm run generate-calendars`.
  See `docs/source-candidates/machinehousebrewery-com.md`.
- 🔄 Status fix: re-confirmed 0 change on `needlehook` (Bellingham-only),
  `secret-garden-books` (still 403 from this environment). PR #760
  (8-Bit Brass Band) remains blocked on upstream content, unchanged.

## Source discovery: implementing a candidate (Seattle Yarn)

Swept the `candidate`/`investigating` backlog live first: most remaining
Squarespace/Eventbrite/LeagueApps/ClubExpress picks re-checked today (Vortex
Pottery, Reclaim Clay, Uplift Climbing, Phinney Books, Reconnect South Park,
Emerald City Softball, League of Women Voters King County) are unchanged —
either not a real events collection (`products` typeName, no `startDate`),
still 0 upcoming, or 403/404 on their documented endpoints. Ran fresh
searches across garden/plant-sale, makerspace, trivia-night, new-venue, and
craft-night verticals — nearly every real lead (Admiral Pub, Populus
Seattle, Seattle Makers, North End Makers, iloveseattle.org) was already
`added` in a prior cycle today. One new find:

- ✅ Added: **Seattle Yarn** (Admiral, West Seattle) — yarn shop with a
  stable, explicitly-stated "Sit and Stitch" drop-in schedule (every Sunday
  11am-5pm, every Tuesday 1-3pm, 2nd & 4th Wednesday 6-8pm) on a static
  Shopify content page — not a dated event feed, so implemented as
  `sources/recurring/seattle-yarn-sit-and-stitch.yaml` (3 schedules in one
  file), `sourceRole: venue`, tags `Community`, `West Seattle`. Geocoded to
  an exact OSM node match (`shop=sewing`, "Seattle Yarn"). 3 events, 0
  parse errors, verified via
  `ONLY_SOURCE=seattle-yarn-sit-and-stitch npm run generate-calendars`. See
  `docs/source-candidates/seattle-yarn.md`.
- 🔄 Status fix: re-confirmed 0 change on Vortex Pottery, Reclaim Clay
  (both Squarespace `products` collections, not events), Uplift Climbing,
  Phinney Books, Reconnect South Park (all 0 upcoming), Emerald City
  Softball (LeagueApps schedule pages 404 on direct fetch), League of Women
  Voters King County (`lwvskc.org` 403s from this environment, documented
  calendar URL also 404s).

## Source discovery: implementing a candidate (Reclaim Clay Collective)

Re-swept the remaining `candidate`/`investigating` backlog live before a
fresh search: all the previously-flagged Squarespace/Eventbrite 0-event
picks (Dog Yard Bar, Human People Beer, Poquitos Seattle, Queer Events
Seattle, Clean Greens Farm & Market, Innervisions Posters, Grey Drizzle
Fibers) are unchanged. Ran fresh searches across running/racing, art
gallery, holiday market, new-brewery, distillery/cidery, board game cafe,
drag brunch, and karaoke verticals — every real lead surfaced (Reuben's
Brews, Ounces Taproom, West Seattle Runner, Seattle Running Club, Slip
Gallery, Museum of Museums (closed, `notviable`), Seattle Local Markets
(`notviable`), Mox Boarding House (all three candidate files, `notviable`
— Occasion ticketing platform, no listing endpoint), Rickshaw Restaurant &
Lounge and Hula Hula (both `notviable` — nightly karaoke, no distinguishable
schedule or feed), Cougar Mountain Trail Series (`notviable` — outside
Seattle proper), Fog Room and Unicorn (already `added`)) was already
resolved in a prior cycle. `PublicDisplay.ART` remains a confirmed-reachable
but 🔴 Low-tier candidate (RSC flight-data payload has dates but no clean
title/venue JSON keys yet) — not picked up this cycle.

- ✅ Added: **Reclaim Clay Collective** — pottery studio at 800 S Weller St
  #201, Seattle (Chinatown-International District). Previously flagged
  🔴 Low-confidence because its `/classes-and-workshops?format=json`
  endpoint is a Squarespace **store** (product) collection, not an events
  collection — no `startDate` field, so the built-in `squarespace` ripper
  type doesn't apply. Implemented a custom `IRipper`
  (`sources/reclaim_clay/`) handling three distinct date-encoding shapes:
  per-variant `Dates Available`/`Available Dates` attributes (each an
  independently-purchasable session, including weekly-range strings within
  a single variant), a shared excerpt date offered at several bare
  per-slot times via a `Time` variant attribute, and a plain excerpt date
  (single day, weekly-recurring range, or one-off multi-day span).
  Gift-card products are filtered out before parsing. `sourceRole: venue`,
  fixed geo (Nominatim exact match, OSM way 235385510), tags `["Creation"]`.
  112 events, 0 parse errors, verified via
  `ONLY_SOURCE=reclaim-clay npm run generate-calendars`. PR #1506. See
  `docs/source-candidates/reclaim-clay.md`.
- 🔄 Status fix: re-confirmed 0 change on Dog Yard Bar, Human People Beer,
  Poquitos Seattle, Queer Events Seattle, Clean Greens Farm & Market,
  Innervisions Posters, Grey Drizzle Fibers.

## Source discovery: implementing a candidate (Seattle Drum School of Music)

Re-swept several already-`added`/`notviable` picks live before searching
further: Gasworks Brewing (Squarespace `events-page-master` collection
found via sitemap.xml this pass — real `events-stacked` collection,
`itemCount: 4`, but still 0 upcoming, all past; stays `notviable`), Seattle
Yoga News (Tribe REST API still `total: 0`), Kandelia (`/events/feed/`
still 0 `<item>`s). Fresh searches across dance-studio, vintage-market,
climbing-gym, improv-comedy, and new-brewery verticals turned up only
already-`added`/`notviable` sources (NW Dance, Unicorn, Jet City Improv,
Unexpected Productions, Half Moon Bouldering, Reuben's Brews, Gasworks,
Julia's on Broadway, Dreamland, Evergreen State Brewing). Also checked a
slice of the 2026-08-25 `Unknown`-platform candidate backlog:
`timslivemusic-com` (Cloudflare challenge, `blocked`), `salsacontodo-com`
(Wix Bookings SYNC widget, no static event data), `seattleartistleague-com`
(WordPress, no events link found), `arcana-crafts-tarot` (Squarespace,
`upcoming: 0`), `seattletri-org` (Laravel/Livewire SPA, no static event
data), `cowgilltrailcollective-com` (not fetched this pass).

- ✅ Added: **Seattle Drum School of Music** — Georgetown music school
  ("The LAB @ 1010") hosting community events (youth music, vocal improv
  jam, all-ages robotics workshops, Aztec dance practice) plus an
  occasional off-site show at Hellbent Brewing. Same WordPress "Modern
  Events Calendar" RSS pattern as the already-implemented `sources/nw_dance`
  — `/events/feed/` with structured `mec:*` fields — but this feed unrolls
  each recurring listing into one `<item>` per future occurrence (same
  guid, different `startDate`) rather than one post per series, so the
  ripper folds the occurrence date into the event id. Two known venues
  mapped to full addresses (`KNOWN_VENUES`); a rented weekly church
  service listing and a venue-less "T-Shirt Day" novelty listing are both
  filtered out before parsing (the new-source gate fails on any parse
  error from a brand-new source, so a locationless item gets filtered
  rather than surfaced as one). `sourceRole: venue`, `geo: null`, tags
  `Music`, `Georgetown`. 48 events, 0 parse errors, 0 unresolvable
  locations, verified via
  `ONLY_SOURCE=seattle-drum-school npm run generate-calendars`. See
  `docs/source-candidates/seattledrumschool-com.md`.
- 🔄 Status fix: re-confirmed 0 change on Gasworks Brewing, Seattle Yoga
  News, Kandelia.
- ⛔ Blocked: `timslivemusic.com` (Cloudflare JS challenge, 403 even from
  this environment).
- 🔍 Investigating: none new this pass.
