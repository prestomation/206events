# 2026-08-04 — Social Source Discovery

## Social source discovery: r/SeattleEvents

- Feed: 25 posts (7 new since last run)
- ❌ Skipped (one-off): Kellie Newton — "Risk Being Seen" weekend workshop — single event on personal coaching website (kellienewton.com/workshop) — via [Reddit post](https://old.reddit.com/r/SeattleEvents/comments/1vel57u/risk_being_seen_a_weekend_experiential_workshop/)
- No new candidates this run.

## Source discovery: implement highest-confidence backlog candidate

Re-checked every `status: candidate` file for live upcoming-event counts
before searching further (per the repeated finding in recent runs, this
project remains very thoroughly mined). None of the Squarespace/Eventbrite
candidates had repopulated (`coca-seattle`, `phinney-books`,
`the-cuff-complex`, `poquitos-seattle`, `human-people-beer`, etc. all still
at 0 upcoming events). `discnw`'s "27 events" turned out on closer
inspection to be season-long league/camp registration windows (e.g.
"6/1/26 - 8/10/26"), not discrete attendable happenings — deprioritized,
left as `candidate` with the concern already on file.

Ran a fresh round of ~6 web searches (music venues, beer/taprooms,
bookstores, comedy — verticals not covered by 2026-08-03's run) — all
leads already covered (`ounces_taproom`, `reubens_brews`,
`third_place_books`) or already tracked/`notviable` (`gasworks-brewing`,
`seattle-women-in-tech`). Also re-confirmed `rei-seattle-flagship-store`,
`axe-kickers`, and `seattle-book-club` are still blocked on
headless-browser rendering unavailable in this environment.

**✅ Added: Fremont Chamber of Commerce — `sources/fremont_chamber_of_commerce/` — PR #1099.**
Promoted the existing `candidate` (`docs/source-candidates/fremont-chamber-of-commerce.md`,
previously deprioritized as 🔴 Low/custom-scrape). The earlier "no ICS/RSS
export" finding only checked for a bulk calendar-wide feed; this pass found
each event has its own GrowthZone ICS export
(`/calendar/ICal/<slug>.ics`, surfaced by the "Add to Calendar" button),
giving clean structured date/time/location per event. Custom `IRipper`:
listing page → per-event slug → per-event ICS, parsed with `ical.js`.
Events with a blank ICS `LOCATION` (one of the 3 upcoming events, an
outdoor walk) get a neighborhood-level placeholder plus an
`UncertaintyError` rather than a guessed address. Verified via
`ONLY_SOURCE=fremont-chamber-of-commerce npm run generate-calendars`:
3 events, 0 parse errors, 1 non-fatal Uncertainty entry.