## Source discovery: candidate re-check (Sports)

- ✅ Added: Rat City Roller Derby — custom HTML (WordPress event detail pages) — 10 events — PR #1366 (Season 21 home-bout schedule + hosted BIPOC Ultimate tournament now live; candidate had been deprioritized pending a 2026–27 season announcement)

## Source discovery: candidate re-check (Music/OpenMic)

- ✅ Added: Badslava Seattle Open Mics — custom HTML (weekly listing table) — 26 events — PR #1367 (citywide open-mic directory; detail-page check showed some listings are monthly with a variable week rather than strictly weekly, so implemented as a plain per-build scrape of the live current-week table rather than an inferred recurring schedule)
- 🔍 Investigating: several previously-flagged Squarespace/Eventbrite candidates re-checked and still show 0 upcoming events (phinney-books, poquitos-seattle, reconnect-south-park, human-people-beer, dog-yard-bar, clean-greens-farm-market, ncnw-seattle-section, urban-community-councils-of-seattle, innervisions-posters) — no change
- ⛔ Blocked: Secret Garden Books — `secretgardenbooks.com/upcoming-events` now returns HTTP 403 from this environment (was previously uninvestigated); leaving as `candidate` per the "blocked here, don't implement" rule rather than flipping to `blocked` outright since it may be an intermittent WAF rule

## Source discovery: comedy/nightlife, seasonal (Oktoberfest/harvest), art galleries, Eventbrite/ICS/brewery discovery, dead-source recheck (2026-09-04, second pass)

Ran `dead-sources.py` against production `build-errors.json` — 1 zero-event
calendar (`roam-bar-roam-bar`). A WebFetch of the live page confirmed
current content (a recurring Wednesday DJ session), so this reads as the
same stale/transient snapshot flagged and dismissed on 2026-09-03 rather
than a genuine 30-day dead pattern; no `dead` flag applied.

Rotated searches across comedy clubs, Oktoberfest/fall-harvest seasonal
events, art galleries, new-venue openings, breweries, ICS-feed discovery,
and Eventbrite organizer discovery. Nearly all hits were already covered
(Emerald City Comedy, Laughs Comedy Club, Gallery 110, Ounces Taproom,
Reuben's Brews, Third Place Books, Book Larder, Seattle City of Lit,
Seattle Night Market, Vera Project) — expected given 900+ tracked
candidate files.

- ✅ Added: Bush Garden — recurring YAML (nightly karaoke, 6 weekly
  schedule entries covering every day except Tuesday) — 6 events, 0
  errors. See `docs/source-candidates/bush-garden-karaoke.md`. Also added
  a `KNOWN_VENUE_COORDS` entry in `lib/geocoder.ts` — Nominatim's
  structured geocoder placed the address ~700m off on a generic "South
  King Street" segment near Yesler Terrace instead of the actual building.
- 🔍 Investigating: PublicDisplay.ART re-checked — still a promising
  citywide arts aggregator (30-60+ events/day across neighborhood art
  walks, galleries, music, theater) but confirmed no plain-HTML-fetchable
  API: `/api/events`, `/api/calendar`, `/api/events/upcoming`,
  `/api/calendar/events` all 404, and the Next.js RSC payload in the raw
  HTML doesn't expose plain JSON key/value event fields via regex search.
  Needs a browser Network-tab pass to find the real data fetch — still
  🔴 Low-confidence, unchanged from 2026-08-21.
- ❌ Not viable (new finding): Fishermen's Fall Festival, Kenmore
  Oktoberfest, Fremont Oktoberfest — one-off/annual single-day festivals
  with no recurring feed or forward calendar to scrape (Kenmore is also
  outside Seattle city limits); not worth per-candidate tracking as
  standalone sources.
- 🔄 Status fix: none — Lowlander Brewing (still no events collection on
  its Craft CMS site) and Reverie Ballroom/Uplift Climbing/SODO BIA/DiscNW/
  Emerald City Trapeze (all previously `candidate`, re-confirmed unchanged
  this cycle — empty Squarespace collections, SiteGround JS-challenge, or
  too-thin event volume, respectively) all still hold their prior
  disposition; no edits needed to their files.

## Source discovery: bookstores (implementation cycle)

- 🚀 Proposed: Lovestruck in Seattle — built-in `eventbrite` type — 11 events
  verified via the public Eventbrite API — PR #1369 (see
  `docs/source-candidates/lovestruck-in-seattle.md`). Romance-only indie
  bookstore in Wedgwood; own Shopify site links out to an Eventbrite
  organizer (`112498075441`) for all dated events. Candidate stays
  `status: candidate` until CI confirms events > 0 post-merge.
