## Source discovery: outdoors/running/books/markets, karaoke/roller/board-games, civic/gallery, re-checks

Ran `skills/source-discovery/scripts/dead-sources.py` first. `zeroEventCalendars`
(23) and `externalCalendarFailures` (15) both match already-tracked, known
issues — the 15 external failures are all `Browserbase fetch failed: HTTP 402`
(Browserbase credit exhaustion, a non-fatal billing condition per
`docs/proxy-verification.md`, not a source problem), and the zero-event
calendars are the standing `seattle-food-trucks-*` set (proxy-staging PR
`#1453`, unchanged), `sam-*` sculpture-park/downtown/asian-art, and other
long-known small/empty branches. No new 30+-day dead source found.

Ran 5 rotations of fresh searches across verticals not hit hard recently:
outdoor events, bookstore/author readings, running clubs, holiday
markets/night markets, Eventbrite organizer discovery, civic/community
center calendars, karaoke bars, board game cafes, roller rinks,
immigrant/refugee orgs, new art galleries, silent disco, and "what's
opening this fall" venue-news searches (Gasworks Brewing SoDo, Evergreen
State Brewing, Reuben's Brews downtown taproom, The Vera Project's new
Georgetown venue, Bush Garden's reopened location, Mox Boarding House,
Meeples/The Missing Piece, Southgate Roller Rink, Roll Around Seatown,
ARTS at King Street Station, Silent Dance Alki). Every one of these
resolved to a source already `added` (bush-garden-karaoke,
southgate-roller-rink, gallery_mack, the_missing_piece, sonic_boom_records,
seattle-gov-arts, vera_project, reubens_brews) or a previously-evaluated
`notviable`/`blocked`/`investigating` candidate file — caught before
re-investigating by checking `sources/` and `docs/source-candidates/`
first, per the skill's step-4 dedup gate.

Also re-checked several open `candidate`-status entries that were close to
a decision or time-sensitive (holiday-season events that might have posted
by now):

- 🔄 Status fix: **Growlerz Seattle** (`growlerzseattle-com.md`) — TLS now
  works (was broken 2026-09-16), but the page's only calendar-shaped
  element is an Elfsight widget. Queried Elfsight's boot API directly for
  the widget id and it resolves to a `"popup"` app type with a
  `scheduleStart`/`scheduleEnd` window of 2024-08-14–2024-09-02 — a
  long-expired promo popup, not an events feed. The "Trivia! Wednesdays"
  text is static copy with no structured backing. Flipped `❌ notviable`.
- 🔄 Status fix: **48 North SARC** (`48-north-sarc.md`) — frontmatter still
  said `status: candidate` even though the 2026-09-08 note already
  documents it being deprioritized for good in favor of Sloop Tavern YC
  (`sloop-tavern-yacht-club.md`, added). Corrected the frontmatter to
  `notviable` to match.
- Re-checked, no change: **Dog Yard Bar** (`/events-all?format=json` still
  `itemCount: 123`, `upcoming: 0`), **Human People Beer** (`past` count
  grew 7→10 but `upcoming` still empty — collection is alive, just not
  publishing), **Hardcore 4 Harm Reduction** (still `HTTP 502`), **Clean
  Greens Farm & Market** (Eventbrite organizer API still `object_count: 0`
  — the Harvest Dinner still hasn't posted despite being mid/late fall now).

No new addable source cleared the quality gate today — every fresh lead
was either already covered or still blocked on the same issue as its last
check (no structured feed, 0 upcoming events, or a down/broken site).
Per the skill's rules, nothing gets implemented against an empty or
inaccessible pipeline; the above stay `candidate`/`notviable` until their
blockers change.
