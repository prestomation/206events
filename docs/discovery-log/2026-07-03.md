## Source discovery: Arts, theater, beer/drink, ICS feeds, new venues

Dead source check: `skills/source-discovery/scripts/dead-sources.py` — HTTP 403 from deployed site (same as prior cycles); unable to run automated check.

Verticals searched: art gallery events, theater calendars, beer release/brewery events, ICS-subscribable calendars, newly opened Seattle venues.

- 💡 Candidate: **Jewish Federation of Greater Seattle** — ICS (Tribe Events) — https://www.jewishinseattle.org/community-calendar/ — 🔥 High confidence, verified ICS feed with 29 upcoming events (secular community programming; explicitly excludes religious services)
- 🔍 Investigating: **Seattle Central Theatres** (Broadway Performance Hall + Erickson Theatre) — https://theatres.seattlecentral.edu/event-calendar — Drupal 10 site, no ICS/JSON endpoint found in static markup; calendar appears client-rendered, needs browser network-tab inspection to find the real data source
- ℹ️ Already covered: **ARTS at King Street Station** (seattle.gov gallery) — exhibitions are already surfaced via the existing `seattle-gov-arts.yaml` Trumba feed; no new candidate needed
- ℹ️ Already covered (no action): Fremont Brewing, Reuben's Brews, Seattle Public Theater, NWTheatre, Populus Seattle, Town Hall Seattle, Woodland Park Zoo, Climate Pledge Arena, RailSpur, Cannonball Arts Center, **Washington Beer Blog** (already implemented as `sources/external/washington-beer-blog.yaml`, pulling the linked Google Calendar ICS feed) — all confirmed already implemented under `sources/`
- 🔄 Status fix: none — Picklewood, Pitch the Baby, and Next Level Lounge remain `notviable` on re-check (no public event calendars found on any of the three sites)

## Source implementation: Comedy, Ballard (reverted)

- 🔄 Status fix (reverted after CI failure): **Ballard Comedy Takeout** — Eventbrite (`organizerId: 121332375671`) — weekly Thursday 8:30 PM open mic at Ballard Mandarin; implemented `sources/ballard_comedy_takeout/ripper.yaml` and opened PR #838, but the CI build produced 0 events + 1 parse error twice in a row (not transient) while the sibling Eventbrite source `actualize-air` succeeded in the same runs, confirming the shared `EVENTBRITE_TOKEN` works. Suspected cause: this organizer's event is part of an Eventbrite recurring "series" (`is_series: true`), and Eventbrite's private API is documented to return only the series parent (no concrete start time) from the organizer-events endpoint — a possible gap in the shared `EventbriteRipper` for recurring-series sources that couldn't be confirmed against the real authenticated API from this environment (the public unauthenticated mirror doesn't reproduce it). Reverted the source rather than merge with 0 events — see `docs/source-candidates/ballard-comedy-takeout.md` for full diagnostic notes; candidate left as `investigating`.
