## Social source discovery: r/SeattleEvents

No new candidates this run. The 4 new posts since the last run contained no
external URLs that qualify as event sources (all were social links, content
sites, or had no external links).

Feed: 25 posts (4 new since last run)

## Source discovery: food/drink festivals, beer/brewery, holiday markets, night markets/pop-ups, makerspaces, dance studios, new venues, Eventbrite/ICS discovery searches

Ran `dead-sources.py` first — 5 zero-event calendars (`external-on-the-hiyu`,
`external-seattle-childrens-museum`, `external-seattledances`,
`external-tilth-alliance`, `external-urban-league-seattle`) and 2 external
failures (`myballard` HTTP 403, `west-seattle-blog` empty response). All are
existing `status: added` sources already tracked from prior runs — most have
their own per-candidate file (`on-the-hiyu.md`, `seattle-childrens-museum.md`,
`seattledances.md`, `tilth-alliance.md`, `myballard.md`, `west-seattle-blog.md`);
`urban-league-seattle` predates the per-file split and is tracked instead in
`docs/discovery-log/dead-sources.md`. No new dead-source flags today.

- 💡 Candidate: Gates Foundation Discovery Center — WordPress custom `event` REST endpoint — https://www.discovergates.org/events/ (confirmed real `wp-json/wp/v2/event` API, but 0 events published right now — re-check next cycle)
- 🔍 Investigating: Seattle Swing Dance Club (Seattle WCS Calendar) — https://seattleswingdanceclub.com/seattlewcscalendar (Squarespace site, but the calendar itself is a static page with an embedded third-party widget, not a Squarespace events collection — need to find the widget's actual data source)
- 🔄 Status fix: Reverie Ballroom — re-checked, still 0 upcoming events (`?format=json` empty) — no change, `lastChecked` bumped
- ❌ Not Viable (already tracked, confirmed still true): Bite of Seattle, Fremont Brewing, Reuben's Brews, Ballard Brewed, Woodland Park Zoo, Climate Pledge Arena, Seattle Center (general + Festál), Sync Seattle, Seattle Social Club, Columbia City Night Market, Seattle Night Market, Rabbit Box, Seattle Makers, North End Makerspace, Seattle Creates, Century Ballroom, Pioneer Square Art Walk, Flight Club (both Capitol Hill music venue and South Lake Union darts bar) — all already covered by existing sources or previously evaluated candidate files; no action needed

No new source implemented this cycle — all newly-surfaced candidates
(Gates Foundation Discovery Center, Seattle Swing Dance Club) currently
return 0 events or lack a scrapable feed, so nothing cleared the "≥1 event,
verified live fetch" bar required before implementation.

