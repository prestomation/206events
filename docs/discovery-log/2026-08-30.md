## Source discovery: Oktoberfest/Halloween seasonal, LGBTQ, fiber arts, gardening, senior centers, new breweries (2026-08-30)

Ran `dead-sources.py` first — 4 zero-event calendars
(`downtown-seattle-association-other-events`, `frye-art-museum-frye-art-museum`,
`seattle-showlists-hotel-crocodile`, `tag-folk`) and 1 external failure
(`folkdance-page`: HTTP 403). All four zero-event calendars and the
`folkdance-page` failure have multi-week histories across prior discovery
logs (checked `docs/discovery-log/`) without a 30-consecutive-day pattern —
normal intermittent gaps, not new `dead` flags this cycle.

Rotated searches across fall-seasonal events (Oktoberfest, Halloween),
LGBTQ events, fiber arts/knitting, community gardens, senior centers,
`site:eventbrite.com` organizer discovery, immigrant/refugee cultural
orgs, and new brewery/taproom openings. Given 890+ tracked candidate
files, most hits were already implemented or tracked: Fremont Oktoberfest
(covered by the existing `fremont_chamber_of_commerce` ripper), Elysian's
Great Pumpkin Beer Festival (see new lead below), Reuben's Brews and
Ounces Taproom & Beer Garden (both already `status: added`), Sync
Seattle and Seattle Social Club (already tracked), Center for Active
Living / West Seattle senior center (already `added`, PR #1231).

One new 🔴 low-confidence candidate:

- **Elysian Brewing** (elysianbrewing.com/events) — Drupal 10 HTML, no
  ICS/JSON/JSON-LD found. Only 2 current items (recurring Trivia
  Tuesdays + the Oct 3 Great Pumpkin Beer Festival), with an
  inconsistently-formatted free-text date field across items — would
  need custom per-format date parsing. Kept as `candidate`, low
  priority given current volume.

Three re-surfaced stub candidates from the 2026-08-25 aggregator gap
analysis, investigated to a disposition:

- **International Examiner** (iexaminer.org) — Chinatown-ID community
  news org's monthly events-calendar page. Fetching from this
  environment returns an HTTP 202 SiteGround JS bot-challenge
  (`sgcaptcha`) interstitial. `candidate` → `blocked`.
- **SouthEast Seattle Senior Center** (sessc.org) — Tribe-Events-style
  URL pattern; `?ical=1` also hits the same SiteGround `sgcaptcha`
  challenge. `candidate` → `blocked`.
- **Seattle Gay Scene** (seattlegayscene.com) — the previously-flagged
  `/calendar-2/` follow-up page now 404s, and the WP REST API confirms
  no `event`/`tribe_events` post type exists — it's an editorial blog,
  not a structured calendar. `candidate` → `notviable`.

One more SiteGround-blocked lead, new this cycle:

- **Garden Love** (gardenlove.org) — Seattle garden-events aggregator
  surfaced via a community-garden search. Same `sgcaptcha` block
  signature as above; nothing to evaluate from here. `status: blocked`.

One `notviable` lead — geography:

- **Evergreen State Brewing** — new taproom announced Aug 29, but the
  address (1933 Dock Street) is in Tacoma, not Seattle; fails the
  geographic gate.

Re-checked one existing `notviable` stub with no change:

- **Needlehook** (needlehook.com) — PNW fiber-arts ICS feed still
  contains only the same 2 Bellingham-only events as the 2026-08-05
  check; no Seattle listings yet.

- ✅ Added: none this cycle — candidate-list-only run, no implementation
- 💡 Candidate: Elysian Brewing — custom HTML (Drupal) — 🔴 low confidence — https://www.elysianbrewing.com/events
- ❌ Not Viable: Seattle Gay Scene — editorial blog, no structured event calendar
- ❌ Not Viable: Evergreen State Brewing — new taproom is in Tacoma, not Seattle
- 🔄 Status fix: International Examiner (iexaminer.org) — `candidate` → `blocked` (SiteGround captcha)
- 🔄 Status fix: SouthEast Seattle Senior Center (sessc.org) — `candidate` → `blocked` (SiteGround captcha)
- 🔍 Investigating: none new this cycle
- 💀 Dead source flagged: none new (zero-event/failure set matches the established intermittent pattern, not a 30-day threshold)
