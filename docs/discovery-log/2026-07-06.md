## Source discovery: Cultural/community organizations, library lectures, disability inclusion

Dead source check: fetched `https://206.events/build-errors.json` via `curl`
(direct `urllib` still gets HTTP 403 from this environment, same known
limitation as prior cycles) — `totalErrors: 450`, but `zeroEventCalendars: []`,
`externalCalendarFailures: []`, `configErrors: []`. No dead sources to flag.

Verticals searched: Vietnamese community orgs, Ethiopian/Eritrean community
centers, Latino/Hispanic community orgs, Seattle Public Library author/lecture
calendar, disability-inclusion nonprofits.

- 💡 Candidate: **Disability Empowerment Center** — Squarespace (`events-stacked`
  collection) — `https://www.disabilityempowerment.org/events` — 7 confirmed
  upcoming events verified via raw epoch `startDate` timestamps (Virtual/Westside/
  Eastside peer support groups, EPIC community outings) — 🔥 High confidence
- ❌ Not Viable: **Eritrean Association in Greater Seattle** — Squarespace
  confirmed, but the `/events` collection (`events-stacked`) is empty
  (`?format=json` shows no populated items); real content lives at
  `/events-blog/<slug>` as prose posts about annual observances (49th Annual
  Festival, Martyr's Day, Women's International Day, etc.) with no
  scrapable day-level structured dates
- 🔒 Blocked (not stageable): **Ethiopian Community in Seattle (ECS)** —
  `ecseattle.org/events/` returns HTTP 403 directly from this environment;
  no platform identified, nothing to prove from here
- 🔒 Blocked (not stageable): **Casa Latina** — WordPress (`wp-json` link
  header present) but `casa-latina.org/events/` returns HTTP 403 directly
  from this environment
- ℹ️ Already covered (no action): **Friends of Little Sài Gòn**
  (`docs/source-candidates/friends-of-little-saigon.md`, `status: added`),
  **Vietnamese Cultural Center** (`docs/source-candidates/vietnamese-cultural-center.md`,
  `status: added`), **Seattle Public Library — Authors & Books**
  (`docs/source-candidates/spl-authors-books.md`, `status: added`) — all
  confirmed already implemented, resurfaced by these searches but no new
  candidate needed
