## Source discovery: bookstores, beer/food, farmers markets, tech Eventbrite orgs, open mics/comedy, running clubs — plus a sweep of the `candidate`/`investigating` backlog (2026-09-03)

- ✅ Added: Pony Seattle — recurring YAML (9 distinctly-named nightlife nights) — PR #1360
- 🔍 Investigating: Grey Drizzle Fibers — re-checked; the Seattle-located "Flock Fiber Festival" has passed, leaving 0 of 2 current upcoming Squarespace events in Seattle (North Bend + Bellevue). Confirms this Renton-based organizer's Seattle presence is occasional, not primary.
- 🔍 Investigating: WomenHack Seattle — confirmed 2 live Seattle dates (Oct 15, Dec 3 2026) via the public Eventbrite organizer API on the global `12632213821` org, `expand=venue` shows `venue.address.city: "Seattle"` (lat/lng given, no street address — "Job Fair" placeholder revealed only to registrants). Both listings are titled "Employer Ticket" — the WebFetch read of the event page describes it as primarily for companies recruiting talent, not general job-seeker RSVP. Borderline fit for a community calendar; leaving as `investigating` rather than implementing a custom filtering ripper against a B2B-only listing. Revisit if a general-admission/job-seeker ticket type is confirmed.
- 🔍 Investigating: Columbia City Seattle Calendar (Wild Apricot) — confirmed the `/calendar` page server-renders a small "Upcoming Events" widget (no JS needed) with 2 real dated events, but both are internal "CCBA Membership Meeting" board meetings, not public-facing events; the wider event set mentioned by other tooling (98118 Festival, art receptions) isn't reachable via plain HTML fetch. Same character concern as SODO BIA (internal meetings vs. public events).
- ❌ Not viable: Bathtub Gin & Co (SpotHopper platform) — events page currently reads "We are updating our events" (0 events); no re-check schedule needed given SpotHopper has no feed regardless
- 🔄 Status fix: Barnes & Noble Downtown Seattle — checked again; store's own Eventbrite organizer still not discoverable, and the generic corporate B&N Eventbrite org (`17337409237`) carries only other-store/virtual events, no Seattle Pike St listings. Still `investigating`.
- Re-checked with no change (still 0 upcoming events / still blocked): Poquitos Seattle, Dog Yard Bar, Innervisions Posters & Framing, Ballard Comedy Takeout, Clean Greens Farm & Market, NCNW Seattle Section, Urban Community Councils of Seattle, Rat City Roller Derby (off-season), SODO BIA (SiteGround JS-challenge), 48 North SARC (not Seattle-filterable), CoCA Seattle.
- Attempted a headless-browser (Playwright/Chromium) pass on several JS-rendered candidates (Boneyard Seattle, Blue Moon Tavern, Raygun Lounge, Versatile Arts) to work around their client-side-only calendars; the sandboxed agent proxy resets long-lived/HTTP2 tunnels after ~6s (`ws_closed_mid_exchange` in the proxy status log) for arbitrary third-party hosts, so this isn't viable from this environment. Left these as `investigating` — unchanged.
