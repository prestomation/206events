## Source discovery: bookstores, beer/food, farmers markets, tech Eventbrite orgs, open mics/comedy, running clubs — plus a sweep of the `candidate`/`investigating` backlog (2026-09-03)

- ✅ Added: Pony Seattle — recurring YAML (9 distinctly-named nightlife nights) — PR #1360
- 🔍 Investigating: Grey Drizzle Fibers — re-checked; the Seattle-located "Flock Fiber Festival" has passed, leaving 0 of 2 current upcoming Squarespace events in Seattle (North Bend + Bellevue). Confirms this Renton-based organizer's Seattle presence is occasional, not primary.
- 🔍 Investigating: WomenHack Seattle — confirmed 2 live Seattle dates (Oct 15, Dec 3 2026) via the public Eventbrite organizer API on the global `12632213821` org, `expand=venue` shows `venue.address.city: "Seattle"` (lat/lng given, no street address — "Job Fair" placeholder revealed only to registrants). Both listings are titled "Employer Ticket" — the WebFetch read of the event page describes it as primarily for companies recruiting talent, not general job-seeker RSVP. Borderline fit for a community calendar; leaving as `investigating` rather than implementing a custom filtering ripper against a B2B-only listing. Revisit if a general-admission/job-seeker ticket type is confirmed.
- 🔍 Investigating: Columbia City Seattle Calendar (Wild Apricot) — confirmed the `/calendar` page server-renders a small "Upcoming Events" widget (no JS needed) with 2 real dated events, but both are internal "CCBA Membership Meeting" board meetings, not public-facing events; the wider event set mentioned by other tooling (98118 Festival, art receptions) isn't reachable via plain HTML fetch. Same character concern as SODO BIA (internal meetings vs. public events).
- ❌ Not viable: Bathtub Gin & Co (SpotHopper platform) — events page currently reads "We are updating our events" (0 events); no re-check schedule needed given SpotHopper has no feed regardless
- 🔄 Status fix: Barnes & Noble Downtown Seattle — checked again; store's own Eventbrite organizer still not discoverable, and the generic corporate B&N Eventbrite org (`17337409237`) carries only other-store/virtual events, no Seattle Pike St listings. Still `investigating`.
- Re-checked with no change (still 0 upcoming events / still blocked): Poquitos Seattle, Dog Yard Bar, Innervisions Posters & Framing, Ballard Comedy Takeout, Clean Greens Farm & Market, NCNW Seattle Section, Urban Community Councils of Seattle, Rat City Roller Derby (off-season), SODO BIA (SiteGround JS-challenge), 48 North SARC (not Seattle-filterable), CoCA Seattle.
- Attempted a headless-browser (Playwright/Chromium) pass on several JS-rendered candidates (Boneyard Seattle, Blue Moon Tavern, Raygun Lounge, Versatile Arts) to work around their client-side-only calendars; the sandboxed agent proxy resets long-lived/HTTP2 tunnels after ~6s (`ws_closed_mid_exchange` in the proxy status log) for arbitrary third-party hosts, so this isn't viable from this environment. Left these as `investigating` — unchanged.

## Source discovery: rec sports leagues, drag/burlesque, poetry, night markets/craft fairs, wellness, Halloween/seasonal, dead-source recheck (2026-09-03, second pass)

Ran `dead-sources.py` against the production `build-errors.json` — 5
zero-event calendars listed (`frye-art-museum-frye-art-museum`,
`pacific-science-center-special-events`,
`pacific-science-center-laser-dome`, `pacific-science-center-imax`,
`roam-bar-roam-bar`), all of which have recurred across most of the last
month of logs. Re-checked all three underlying rippers (`frye-art-museum`,
`roam-bar`, `pacific-science-center`) with a fresh local `ONLY_SOURCE`
build against the live sites: `frye-art-museum` returned 5 events,
`roam-bar` returned 1 event, and `pacific-science-center`'s three
calendars returned 8/37/59 events — 0 errors across all of them, so none
are actually dead and no `dead` flag was applied. The production
zero-event listing looks like a stale/transient snapshot rather than a
genuine 30-day pattern (consistent with these same names recurring in the log
without action across several prior cycles).

Rotated searches across verticals not covered in recent days: rec
sports leagues (kickball/dodgeball), drag brunch/burlesque, poetry &
spoken word open mics, night markets & craft/vintage pop-ups, yoga/
wellness studios, and Halloween-season attractions. Given 900+ tracked
candidate files, most hits were already implemented (Renegade Craft
Fair, SOL Yoga Seattle, Mr. B's Meadery/Fun House Cabaret, The Missing
Piece, Queen Anne Farmers Market, South Lake Union Saturday Market) or
already dispositioned (Silent Disco NW/Secret Sunset Seattle — seasonal,
still 0 events at last check; Gasworks Brewing — still no events
collection; Jet City Labs 206 Maker's Market — still blocked/403).

- 🔄 Status fix: Underdog Seattle (underdogseattle.com) — the existing
  `notviable` entry had rejected it on a wrong premise (misidentified as
  a nightlife/DJ venue). Re-investigated under its real category, adult
  recreational sports leagues (kickball, dodgeball, flag football,
  volleyball, etc.): schedules are gated behind per-league detail pages
  with free-text dates, no aggregate calendar, RSS/ICS, or public API.
  Checked sibling LGBTQ+ rec-sports org OutLoud Sports Seattle
  (outloudsports.com/seattle) too — same shape, schedules live behind
  the third-party LeagueApps platform with nothing public. Corrected the
  tags/description and re-confirmed `notviable` on the real grounds.
- ❌ Not Viable: Nile Nightmares Haunted House (nilenightmares.com) —
  seasonal Halloween attraction with real Oct 2026 dates, but its one
  physical location is 6601 244th St SW, Mountlake Terrace — outside
  Seattle city limits, not a borderline "mostly Seattle" case.
- 💡 Candidate: Seattle Design Festival (seadesignfest.org) — WordPress
  nonprofit design festival (Aug 2026 dates already concluded); its
  `events` custom post type isn't REST-exposed and no Tribe
  Events/ICS/RSS feed exists. Low per-year event yield for the scraping
  effort required; left as `candidate` to revisit closer to the 2027
  festival announcement when a fresh, fetchable schedule exists.
- 💀 Dead source flagged: none — all three candidate zero-event
  production sources checked (`frye-art-museum`, `roam-bar`,
  `pacific-science-center`) produced real events in a fresh local
  build, so not a genuine dead pattern this cycle.

## Source discovery: implement Ballard Clay Studio (2026-09-03, third pass)

- ✅ Added: Ballard Clay Studio — recurring YAML (5 weekly wheel/handbuilding
  class slots) — PR #TBD
