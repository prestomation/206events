## Source discovery: community/business-association calendars, brewery/nightlife, arts, sports

- ✅ Added: Columbia City Business Association (`docs/source-candidates/columbia-city-seattle.md`) — custom HTML (Wild Apricot) — 3 events — PR #TBD
- ⛔ Blocked: Stir Up The Paint (`docs/source-candidates/stir-up-the-paint.md`) — ICS (WordPress/Tribe Events) confirmed working (30 events, majority Seattle), but the feed started reliably returning a LiteSpeed "Bot Verification" challenge page under repeated fetches from this environment
- 🔄 Status fix: several stale `candidate`/`investigating` entries re-checked, no change — Phinney Books, Poquitos Seattle, Queer Events Seattle, NCNW Seattle Section, Urban Community Councils of Seattle, Dog Yard Bar, Human People Beer, Reconnect South Park, Asylum Collective, Ballard Comedy Takeout, Kandelia, Seattle Yoga News all still show 0 upcoming events

## Source discovery: music venues, bookstores, comedy/trivia, farmers markets, new venues, art galleries, drag/nightlife

Ran a second pass rotating into verticals not covered by this morning's run
(live music venue round-ups, bookstore/author-reading calendars, trivia/open
mic/comedy, farmers markets, newly-opened venues, art gallery openings, drag
shows, record-store in-stores, night markets/pop-up markets, breweries,
comedy clubs).

- Re-ran `skills/source-discovery/scripts/dead-sources.py` against
  production — same 21 already-tracked zero-event calendars (all sub-
  calendars of existing multi-calendar sources: `sam-*`, `seattle-food-
  trucks-*`, `spl-northeast`, etc. — an existing-source `expectEmpty`
  question for `build-report`, not a new dead-source flag here) and the
  same known Browserbase-402/West Seattle Blog external failures. No new
  dead-source flags.
- Every concrete lead from this pass — Songkick/SeatGeek/Ticketmaster/
  JamBase (aggregators, not primary sources), Third Place Books, Elliott Bay
  Book Co., SPL Authors & Books, Hopvine Pub, a/stir, Queen Anne/Central
  District/Magnolia/Ballard/SLU farmers markets, Flight Club, Picklewood,
  RailSpur, Sonic Boom Records, J. Rinehart Gallery, Julia's on Broadway/Le
  Faux, Queer/Bar, Unicorn, Laughs Comedy Club, Seattle Night Market,
  Columbia City Night Market, CID Night Market, Georgetown Brewing, Great
  Notion Georgetown — already has an `added`/`notviable` candidate file or a
  live ripper/recurring entry. No re-proposals.
- 🔄 Status fix: **Queer/Bar** (`docs/source-candidates/queer-bar.md`) was
  incorrectly `notviable` — that verdict was against the wrong URL
  (`/events-one`); the real Squarespace events collection is at `/calendar`
  and has been live since PR #272. Flipped to `status: added`.
- 🔄 Status fix: **Unicorn** (`docs/source-candidates/unicorn-seattle.md`)
  re-checked — its Squarespace `/events` collection, empty when the source
  was implemented as 4 recurring-YAML calendars in June, is now populated
  (10 upcoming items), but every occurrence matches the existing recurring
  schedules exactly. No change needed; `lastChecked` bumped.
- ❌ Not Viable: **Harris Harvey Gallery** (`docs/source-candidates/harris-harvey-gallery.md`) —
  downtown Squarespace gallery; has a real `/events` collection but it's
  been unused since 2021 (`upcoming: []`), exhibitions are announced only as
  prose on static pages.
- ❌ Not Viable: **Doris** (`docs/source-candidates/doris-pioneer-square.md`) —
  new Pioneer Square cocktail bar (Rally Hospitality); Wix site, no public
  events feed, `/events-groups` is a private booking-inquiry form.
- No new source implemented this cycle — every fresh lead across the
  searched verticals resolved to an existing entry, a non-viable platform,
  or (Queer/Bar, Unicorn) a correction to already-`added` candidates rather
  than something new to build.
