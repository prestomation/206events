## Source discovery: implementing another candidate (Magnolia Yoga and Healing Arts)

Tenth rotation of the day. Re-checked the backlog's still-0-upcoming-events
candidates (Poquitos, Human People Beer, Dog Yard Bar, Reverie Ballroom,
Reconnect South Park, Queer Events Seattle, Phinney Books, Nudibranch
Coffee, Clean Greens Farm & Market, NCNW Seattle Section, Urban Community
Councils, Innervisions Open Mic, Uplift Climbing) via their Squarespace/
Eventbrite APIs directly — no change on any. Picked up the existing
`magnolia-yoga.md` backlog entry instead of a fresh search, since it was
already flagged as a real fixed weekly class schedule worth a
`sources/recurring/` implementation.

- ✅ Added: **Magnolia Yoga and Healing Arts** (3150 West Government Way,
  Magnolia) — the `/class-schedule` page renders its weekly table as
  static HTML inside a Squarespace code block (no JS execution needed).
  Implemented as 10 `sources/recurring/magnolia-yoga-*.yaml` files, one
  per distinct class (Flow I/II, Hatha Yoga I, Slow Flow, Chair Yoga,
  Flow & Restore, Meditation, Gentle Yoga, Flow II/III, Gentle Yoga with
  Yoga Nidra, Sound Bath), following the existing multi-file-per-venue
  pattern (`blue-highway-games-*`, `ccs-seattle-*`). Three irregular slots
  (an alternating Yin/Restorative class, a quarterly cross-venue happy
  hour, and a "every other week during summer" class) were intentionally
  left out rather than guessed at. 15 upcoming events, 0 parse errors
  verified via `ONLY_SOURCE=magnolia-yoga-*`. See
  `docs/source-candidates/magnolia-yoga.md`.

## Source discovery: implement candidate (Sleight of Hand Cellars SODO)

Ran `skills/source-discovery/scripts/dead-sources.py` first — 13 zero-event
calendars and 15 external failures, all pre-existing/known (food-truck
stops, SAM sub-venues, and Browserbase `HTTP 402` credit-exhaustion
failures already tracked by proxy-escalation). No new `status: dead` flags.

Re-checked the existing backlog's Squarespace/Eventbrite `candidate` files
with 0 upcoming events at last check (Poquitos, Human People Beer, Dog Yard
Bar, Reverie Ballroom, Reconnect South Park, Queer Events Seattle, Phinney
Books, Clean Greens Farm & Market, NCNW Seattle Section, Urban Community
Councils, Innervisions Open Mic) — all still 0 upcoming events live, no
change.

Ran fresh searches across comedy, bookstores, farmers markets, art gallery
openings, community-center ICS feeds, record stores, drag brunch, craft
markets, sailing/rowing clubs, new venue openings, distilleries, urban
wineries, cideries, and ghost tours. Most leads resolved to sources already
live or already tracked in the backlog (Laughs Comedy Club, Capitol Hill
Comedy Bar — same address as the already-added Emerald City Comedy Club,
Urban Craft Uprising, Renegade Craft Fair, Gasworks Brewing, Magnuson
Brewery, Seattle Yacht Club, Seattle Sailing Club — all previously
`notviable`/`candidate`/already covered). Doundrins Distilling turned up a
real Shopify events-as-products feed but is based in Cottage Grove, WI, not
Seattle — not pursued.

- ✅ Added: **Sleight of Hand Cellars SODO** (Walla Walla winery's Seattle
  tasting room, 3861 1st Ave S, SODO) — built-in `squarespace` ripper
  (`sources/sleight_of_hand_cellars`). Confirmed Squarespace Events
  collection (`itemCount: 278`) with 13 upcoming events verified via
  `?format=json` (real future `startDate` epoch timestamps, Sept 2026 –
  Feb 2027): wine release parties, tribute-band shows, and a monthly
  "Sleightly Buzzed" comedy series. 0 parse errors in a local `ONLY_SOURCE`
  build. Added the `Wine` tag to `lib/config/tags.ts`. See
  `docs/source-candidates/sleight-of-hand-cellars.md`.

## Source discovery: implementing another candidate (Nine Hats Wines)

Fifth rotation of the day. Re-checked the backlog's Squarespace/Eventbrite
`candidate` files still sitting at 0 upcoming events (Reverie Ballroom,
Poquitos, Vortex Pottery, Clean Greens Farm & Market, Innervisions Open
Mic, NCNW Seattle Section, Urban Community Councils, Human People Beer,
Dog Yard Bar, Grey Drizzle Fibers, Phinney Books, Queer Events Seattle,
Reconnect South Park, Uplift Climbing, Nudibranch Coffee) — no change on
any. Ran fresh searches across trivia nights, poetry slams/open mics,
improv comedy, cooking classes, indie movie theaters, Oktoberfest/brewery
events, knitting/fiber-arts meetups, board game store tournaments, silent
disco, and choir/ukulele open-sings. Nearly every lead resolved to a
source already live in this repo (Jet City Improv, Unexpected Productions,
Central Cinema, The Beacon Cinema, Grand Illusion Cinema, Blue Highway
Games, Seattle Folklore Society, Greater Seattle Choral Consortium,
Ounces Taproom & Beer Garden) or an already-filed candidate with no new
information (Rain City Slam — `notviable`, domain expired; The Pantry
Seattle — re-confirmed JS-rendered with no visible event markup even on
`/calendar/classes`, still `investigating`; Needlehook — `notviable`,
Bellingham-only; Mox Boarding House, Meeples Games, Seattle Knitters
Guild — re-confirmed prior verdicts, no change).

- ✅ Added: **Nine Hats Wines** — a search for "Seattle vinyl record swap"
  turned up a monthly vinyl-swap night at 3861 1st Ave S — the exact same
  SODO Urbanworks address as Sleight of Hand Cellars, added earlier today.
  Turns out to be a *different* wine label sharing the same tasting room,
  on its own domain (`ninehatswines.com`, Craft CMS/Vite, not Squarespace)
  with its own distinct events (trivia, pizza-making classes, a monthly
  vinyl swap, a mimosa brunch, a terrarium class). The `/events` page
  server-renders its event cards directly into the HTML with semantic
  `<time datetime>` attributes — no JS execution needed — so implemented
  as a custom `HTMLRipper` (`sources/nine_hats_wines/`). 5 upcoming events,
  0 parse errors confirmed via `ONLY_SOURCE=nine-hats-wines`. See
  `docs/source-candidates/nine-hats-wines.md`.

## Source discovery: implementing another candidate (Seattle Otters Water Polo)

Sixth rotation of the day. Ran fresh searches across karaoke bars, drag
bingo/brunch, pinball leagues, and new-taproom openings — nearly every
lead resolved to a venue already covered (Reuben's Brews, Queer/Bar,
Unicorn, Dreamland, Rickshaw, Hula Hula) or an already-`notviable`
personal touring calendar (Sativa the Queen). Spot-checked Cafe
Calaveras (Squarespace events collection, 0 upcoming — left uninvestigated
as a new candidate file since there's nothing to implement against yet)
and two Wix venues from the aggregator-gap backlog (Shibuya Hi-Fi,
Union Seattle) that remain JS-rendered with no fixed recurring times
confirmed — no change.

- ✅ Added: **Seattle Otters Water Polo** — masters water polo club with
  public drop-in scrimmage practices. The site names two seasons: a
  summer Green Lake session with a variable "until sunset" end time
  (not implementable without guessing duration) and a currently-live
  "2026 Fall Session" — Tue/Thu 8-9:30pm, Sept 8 - Dec 17, at Medgar
  Evers Pool (500 23rd Ave, Seattle) — with a fixed, confirmable
  schedule. Added `sources/recurring/seattle-otters-water-polo.yaml`
  covering the Fall session only. Verified 2 upcoming events (next
  Tue/Thu occurrences), 0 errors via
  `ONLY_SOURCE=seattle-otters-water-polo`. See
  `docs/source-candidates/otterpolo-com.md`.

## Source discovery: implementing another candidate (Orca Running)

Seventh rotation of the day. Re-checked the backlog's Squarespace/Eventbrite
`candidate` files still at 0 upcoming events (Poquitos, Human People Beer,
Dog Yard Bar, Reverie Ballroom, Reconnect South Park, Queer Events Seattle,
Phinney Books, Asylum Collective, Nudibranch Coffee, NCNW Seattle Section,
Urban Community Councils, Innervisions Open Mic) via their Eventbrite/
Squarespace APIs directly — no change on any. Ran fresh searches across run
clubs, escape rooms, and holiday/pop-up markets; most leads had no
structured feed (Wix client-rendered event widgets, marketing-copy-only
class pages) or no comprehensive dated calendar (individual escape-room
venues, generic holiday-market roundup articles).

- ✅ Added: **Orca Running** — PNW race organizer whose website links each
  race to a `runsignup.com/Race/WA/<city>/<Slug>` registration page.
  RunSignUp exposes a public, keyless REST endpoint per race
  (`runsignup.com/Rest/race/<id>?format=json`) with structured address,
  per-heat start/end times, and registration fees. Filtered the org's
  ~15-race annual calendar down to the 4 races whose `address.city` is
  Seattle (Orca Half Marathon, Pumpkin Spice Run, Electric Cookie Run,
  One Step Closer) — the rest are in Kirkland, Redmond, Poulsbo, and
  other WA cities and are out of scope. Implemented as a custom
  `IRipper` (`sources/orca_running/`) with a hand-maintained `raceIds`
  allow-list, grouping same-day heats into one event and deriving a
  real cost range from the live registration fees. 5 upcoming events, 0
  parse errors verified via `ONLY_SOURCE=orca_running`. See
  `docs/source-candidates/orca-running.md`. PR #1478.

## Source discovery: implementing another candidate (AI House)

Eighth rotation of the day. Checked several roller-derby leagues (Rat
City, Tilted Thunder, Jet City, PFM) turned up via a fresh "Seattle
roller derby schedule" search — none had a usable live feed (Rat City's
custom WordPress `event` post type has no populated detail content yet;
Tilted Thunder's Squarespace `/events` is a plain content page with 0
items; the others had no recognized platform). Also re-checked
`rainbow-city-softball` and `emerald-city-softball` (both now run The
Events Calendar/Tribe plugin, but `wp-json/tribe/events/v1/events`
returns `total: 0` on both — schedules not yet published) and Seattle
CityClub's two Eventbrite organizer ids (`107830570021`,
`12026318033`, both "No upcoming events"). Quality Flea Center's own
Eventbrite org also shows 0 upcoming events; its two known recurring
markets (CHASM, Punk Rock Flea Market) are already covered/`notviable`.

- ✅ Added: **AI House** — Seattle's AI innovation hub at Pier 70 (2801
  Alaskan Way), a City of Seattle / AI2 Incubator / Ada Developers
  Academy partnership. No built-in ripper type fits Luma (its public
  `api.lu.ma` REST endpoint rejects the page's `cal-*` id), so
  implemented as a custom `IRipper` (`sources/ai_house/`) reading the
  `__NEXT_DATA__` JSON Luma's Next.js page embeds — same pattern as the
  existing `sources/new_tech_seattle` (a different org/venue on
  Meetup.com). 14 upcoming events, 0 parse errors verified via
  `ONLY_SOURCE=ai-house`. See `docs/source-candidates/ai-house.md`.
  PR #1479.

## Source discovery: implementing another candidate (Seattle Book Club)

Ninth rotation of the day. Checked open PRs for prior drains/discovery
runs first (none open against this queue besides an already-staged
`requires-proxy-testing` PR and a long-stalled draft blocked on the
source's own website, not on review — left alone). Ran fresh searches
across vintage/pop-up markets, plant sales/swaps, queer dance parties, new
brewery openings, and open-studio tours — nearly every lead was already
covered, regionally out-of-scope (Wanderlust Nursery, KP's Lab — mostly
Everett/Lynnwood/Mercer Island), or already correctly filed `notviable`
(Seattle Local Markets). Also confirmed PR #760 (8-Bit Brass Band) is
still genuinely blocked on the band's own website having zero published
future events (checked the sitemap + JSON-LD `startDate` on every listed
page live — all past) rather than on anything actionable here.

- ✅ Added: **Seattle Book Club** — re-investigated this backlog candidate
  (`seattle-book-club.md`, stuck at `investigating` since July because the
  events calendar is client-rendered). A plain `curl` of
  `/apps/events/calendar` surfaces the widget's inline fetch call: a
  public, unauthenticated Shopify-app JSON API
  (`api.eveyevents.com/production-v2/storefront/calendar?shop=<domain>`).
  Its `schedule` array is the actually-rendered, per-occurrence data
  (recurring listings expanded into real future dates), but only within
  the queried `startDate`/`endDate` window — same monthly-loop shape as
  Seattle Blues Dance Collective. Confirmed events across genuinely
  Seattle-area venues (Figurehead Brewing/Fremont, Pure Barre/Green Lake,
  The Ladies Room/Greenwood, Cafe Bambino, A La Mode Pies/West Seattle,
  U Wine Bar/Wallingford, Hotel Sorrento/First Hill), each with a real
  ticket price and product image. Implemented as a custom `IRipper`
  (`sources/seattle_book_club/`). 14 upcoming events, 0 parse errors
  verified via `ONLY_SOURCE=seattle-book-club` (12 of 14 events geocoded
  cleanly; 1 non-fatal geocode gap left for the geo-resolver queue). See
  `docs/source-candidates/seattle-book-club.md`. PR #1480.

## Source discovery: art walks, makerspaces, jazz/burlesque venues, holiday markets

Eleventh rotation of the day. Ran `skills/source-discovery/scripts/dead-sources.py`
first — 19 zero-event calendars (all pre-existing: food-truck stops, SAM
sub-venues, plus `frye-art-museum`, `queen-anne-book-company`, `seatoday`,
and `urban-family-brewing` sitting at 0 for this check only — not enough
history here to confirm a 30-day dead pattern, leaving these for
`build-report` to track over time rather than flagging) and 15 external
failures (all known Browserbase `HTTP 402` credit exhaustion + one
pre-existing Geekwire 403, both already tracked by proxy-escalation). No
new `status: dead` flags.

Ran fresh searches across art walks, hiking/outdoor clubs, makerspaces,
holiday markets, gallery openings, new brewery taprooms, drag show
venues, silent disco, indie bookstore events, pottery/ceramics studios,
roller rinks, climbing gyms, and speakeasy live-music bars. The overwhelming
majority of leads resolved to sources already live in this repo or
already filed in the 900+ existing candidate files — West Seattle/
Pioneer Square/Downtown art walks, Seattle Makers, North End Makers,
Seattle Christmas Market, Reuben's Brews, Skylark Cafe, Julia's on
Broadway/Le Faux, Clock-Out Lounge, The Alley, Southgate Roller Rink
(all seven weekly skate nights), Seattle Aquarium, The Rendezvous, and
several ceramics studios (Potterings, Saltstone, Ballard Clay, Seward
Park Clay, Reclaim Clay, Seatown Pottery) — confirmed no change on their
existing `blocked`/`notviable`/`added` status. Cottontail Club
(re-confirmed "no events at the moment" on its Wix `/events` page) and
Bouldering Project Seattle (still a bare 403 from this environment) also
re-checked with no change.

- ✅ Added: **Owl 'N Thistle** (808 Post Ave, Pioneer Square) — Irish pub
  hosting Seattle's longest-running jazz jam (weekly since 1997). The
  venue's own Wix site lists a full weekly live-music lineup but gives no
  start times for most nights; only the Tuesday Jazz Jam's 9:45pm–12:45am
  slot is independently corroborated across multiple sources (Earshot
  Jazz, All About Jazz's 25-year retrospective, a Chris Bickley Jazz
  performance listing, and the jam's own Instagram bio), with a
  consistently-reported no-cover policy. Implemented as a single
  `sources/recurring/owl-n-thistle-jazz-jam.yaml` entry covering Tuesdays
  only — the other five nights (Sun/Mon/Thu/Fri/Sat) were intentionally
  left unimplemented since no start time for them could be corroborated
  anywhere. 1 upcoming event, 0 errors verified via
  `ONLY_SOURCE=owl-n-thistle-jazz-jam`. See
  `docs/source-candidates/owl-n-thistle.md`.

## Source discovery: implementing another candidate (The Improv Place)

Twelfth rotation of the day. Re-checked the perennial 0-upcoming-events
backlog (Poquitos, Phinney Books, Reconnect South Park, Nudibranch Coffee,
Queer Events Seattle, Human People Beer, Dog Yard Bar, Urban Community
Councils, NCNW Seattle Section, Clean Greens Farm & Market, Innervisions
Open Mic, Ballard Comedy Takeout) via their Squarespace/Eventbrite APIs —
no change on any. Ran fresh searches across comedy/improv, ghost tours,
Toastmasters clubs, and new bars — Toastmasters clubs are private-meeting
format (low public-event value) and every ghost-tour/new-bar lead was
either a third-party booking platform (Viator/GetYourGuide, not the
operator's own feed) or already filed (Doris, RailSpur, Ounces Taproom,
Reuben's Brews, Populus Seattle, Flight Club — both the Capitol Hill music
venue and the South Lake Union darts bar candidate files).

- ✅ Added: **The Improv Place** — Seattle nonprofit improv theater running
  recurring jam/workshop series at rented venues (Langston Hughes PAI,
  Seattle Center Armory). Its Eventbrite organizer only advertises 2
  listings, but the org's own site links a fuller Luma calendar
  (`luma.com/theimprovplace`) whose Next.js `__NEXT_DATA__` blob is the
  same shape already consumed by the existing `sources/ai_house` ripper.
  Unlike AI House's single fixed venue, this org's events span multiple
  rented spaces, so implemented with ripper-level `geo: null` and a
  per-event `location` string; when Luma obfuscates a series' exact
  address behind RSVP, the event still publishes with the best-effort
  city/neighborhood text and is flagged via the Uncertainty system
  (`location` field) rather than guessing a street address. Implemented as
  a custom `IRipper` (`sources/improv_place/`). 6 upcoming events, 0 parse
  errors verified via `ONLY_SOURCE=improv-place`. See
  `docs/source-candidates/improv-place.md`.

## Source discovery: false-start on National Nordic Museum (already covered)

Thirteenth rotation of the day. Confirmed PR #760 (8-Bit Brass Band) is
still blocked on the band's own website, not on anything actionable here.
Ran fresh searches across rowing clubs, salsa/Latin dance nights, night/
vintage markets, and pickleball — nearly every lead resolved to a source
already covered (Green Lake Crew/GLSCC, Seattle Night Market, Fremont
Evening Market via Fremont Chamber of Commerce) or had no structured feed
(Pocock Rowing Club — Wix, no calendar; Lake Union Crew — static
hand-maintained WordPress list with no times/locations; Baila District —
previously `investigating`, no populated Squarespace events collection).

- ❌ **False start:** investigated and implemented a custom `HTMLRipper`
  for the National Nordic Museum (`nordicmuseum.org/calendar`) before
  discovering — only after CI flagged an unrelated new-source parse-error
  gate — that this venue has been covered since 2026-05-08 as
  `sources/national_nordic_museum/` (PR #326; see
  `docs/source-candidates/national-nordic-museum.md`). The skipped-slug
  quality-gate check (`ls docs/source-candidates/`, "not already covered
  under `sources/`") was done against a `nordic-museum.md` filename that
  didn't exist, without a direct `grep -ril nordic sources/` — the actual
  existing file is `national-nordic-museum.md`/`sources/national_nordic_museum/`.
  Reverted the duplicate ripper and closed the PR without merging; no
  change landed. **Process fix for future rotations:** always
  `grep -ril "<slug or venue keyword>" sources/ docs/source-candidates/`
  before implementing, not just an `ls`/filename guess.

## Source discovery: implementing another candidate (Union Cultural Center)

Fourteenth rotation of the day. This time, `grep -ril` against `sources/`
came first for every candidate considered, per the process fix above.
Checked several more leads before landing on this one: Pocock Rowing Club
(Wix, no populated calendar), Lake Union Crew (static hand-maintained
WordPress list, no per-race times/locations), Blue Moon Tavern (Wix
`wix-warmup-data` blob present but 0 events in it at check time — the
underlying calendar app is empty right now, re-check later), Axe Kickers
(same `wix-warmup-data` check, no events app data found at all), Beneath
the Streets (a continuous multi-times-daily walking tour, not really
discrete "events" — deprioritized as a poor content-model fit), Asylum
Collective, PCC Markets Classes, and REI Seattle flagship (all previously
`investigating`/re-confirmed no change).

## Source discovery: implementing another candidate (WTIA & Community Events)

Fifteenth rotation of the day, run as the scheduled source-discovery task.
Re-swept `docs/source-candidates/` for any high-confidence `candidate`
entries left un-implemented before searching fresh: re-checked ~15
Squarespace/Eventbrite backlog entries stuck at 0 upcoming events (Dog
Yard Bar, Grey Drizzle Fibers, Human People Beer, Phinney Books, Poquitos
Seattle, Reclaim Clay, Reconnect South Park, Reverie Ballroom, Uplift
Climbing, Vortex Pottery, Queer Events Seattle, Clean Greens Farm &
Market, NCNW Seattle Section, Innervisions Open Mic, Urban Community
Councils) live via their Squarespace `?format=json`/Eventbrite public API
endpoints directly — no change on any (confirms `build-report`'s "healthy,
no actionable errors" precondition for running this skill). Also
re-checked Rainbow City Softball and Emerald City Softball (both still no
populated public schedule feed) and 48 North SARC (still correctly
deprioritized — regional multi-club feed, only 1 of 9 clubs Seattle-proper).

- ✅ Added: **WTIA & Community Events** — the existing `washington-technology-
  events.md` candidate flagged a "Community Events" section on Luma
  (`luma.com/wtia`) behind the GrowthZone-hosted main site. Confirmed the
  same `__NEXT_DATA__` Next.js pattern already used by `sources/ai_house`
  and `sources/improv_place`. This calendar is a genuine cross-org
  aggregator (WTIA's own programming plus externally-submitted community
  events like the Cyber Risk Alliance Cybersecurity Summit and SASE STEM
  Connect), so implemented `sourceRole: aggregator`, `geo: null`, with
  per-event location resolved from Luma's `geo_address_info`. Discovered
  mid-implementation that externally-submitted entries use a different
  payload shape than WTIA's own Luma-native events (no event-level
  `api_id`; a `duration_interval` ISO-8601 string instead of `end_at`) —
  handled by falling back to the wrapping calendar entry's own id and
  adding an ISO-8601 duration parser. Zoom-only entries publish
  `location: "Virtual"` rather than guessing a physical address. 11
  upcoming events, 0 parse errors verified via
  `ONLY_SOURCE=wtia-community-events`. See
  `docs/source-candidates/washington-technology-events.md`.

- ✅ Added: **Union Cultural Center** (803 S King St, Chinatown-ID) —
  Webflow site with server-rendered `/classes-and-events` and per-class
  `/workshop-event-program/<slug>` detail pages (no JS execution needed).
  Of 9 listed programs, 5 have open-ended weekly/monthly schedules and
  were implemented as `sources/recurring/union-cultural-center-*.yaml`
  (Capoeira Angola, Capoeira Rodas, West African Dance, Wing Chun, and
  the confirmed half of Samba Raiz — its second back-to-back class has no
  stated end time and was left unimplemented rather than guessed); a
  bounded 8-week paid kids/family series, a "year-round" institutional
  program, and a "DATES: TBD" program were intentionally skipped. 7
  upcoming events across the 5 calendars, 0 errors, verified via
  `ONLY_SOURCE=union-cultural-center-capoeira-angola,union-cultural-center-capoeira-rodas,union-cultural-center-west-african-dance,union-cultural-center-wing-chun,union-cultural-center-samba-no-pe`.
  See `docs/source-candidates/unionculturalcenter-org.md`.

## Source discovery: implementing another candidate (Northwest Trail Runs)

Sixteenth rotation of the day, run as the scheduled source-discovery task.
The Squarespace `?format=json` endpoint returned an identical cached
"Please Stand By" interstitial for every site probed this rotation
(including already-merged sources like `sofhcellars.com`), so Squarespace
candidates were unverifiable from this environment this cycle — deferred
the usual backlog re-check and searched non-Squarespace verticals instead
(DICE venues, running clubs, food trucks, vintage markets, nonprofit
galas). Confirmed OHM Nightclub, The Funhouse, and Black Lodge (DICE)
are already covered or correctly `notviable`; Slim's Last Chance and Axe
Kickers remain unreachable Wix client-rendered calendars; Ridgecrest
Public House and PNW Neighborhood Markets re-confirmed `notviable`
(Shoreline/Edmonds, out of scope).

- ✅ Added: **Northwest Trail Runs** — previously `blocked` (Cloudflare
  challenge on `/events/` and `/calendar/` in a June check). Both are
  reachable now (200, plain server-rendered HTML) — the block appears to
  have been transient. Re-investigated the full 2026-27 Winter Trail
  Series lineup from each event's own detail page (which conveniently
  lists the whole season as plain text) and confirmed 4 of 7 races are
  Seattle-proper: Carkeek Cooler, Ravenna Refresher, Seward Solstice, and
  Interlaken Icicle Dash (the other 3 — Redmond, Kenmore, Sammamish — are
  out of scope). No ICS/API; implemented as a custom `IRipper`
  (`sources/northwest_trail_runs/`) with a hand-maintained `eventSlugs`
  allow-list, same pattern as `sources/orca_running`'s `raceIds`. 4
  upcoming events, 0 parse errors, verified via
  `ONLY_SOURCE=northwest-trail-runs npm run generate-calendars`
  (geocoding confirmed accurate for all 4 parks). See
  `docs/source-candidates/northwest-trail-runs.md`.
