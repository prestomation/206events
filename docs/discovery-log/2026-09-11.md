## Source discovery: implementing highest-confidence backlog candidate

Swept the existing `docs/source-candidates/` backlog (194 `candidate` +
44 `investigating` entries) for the highest-confidence implementable
source rather than running fresh searches. Most top candidates
(Squarespace/Eventbrite sources with 0 upcoming events, SiteGround
sgcaptcha-blocked orgs, JS-rendered Wix/Notion calendars) are still not
implementable this cycle — re-checked several live and confirmed no
change. Found one implementable candidate: a small venue with a stable,
verifiable weekly schedule better modeled as recurring events than a
live-scraped calendar.

- ✅ Added: The Alley (West Seattle) — recurring (2 weekly resident-band
  jazz nights: Sunday DLux Jazz Trio, Monday Westside Jazz Trio) — PR pending
- 🔄 Status fix: Poquitos Seattle — re-checked live, still 0 upcoming events, no change
- 🔄 Status fix: Asylum Collective — re-checked live, still 0 upcoming events, no change
- 🔄 Status fix: Clean Greens Farm & Market — re-checked live (Eventbrite API), still 0 upcoming events, no change
- 🔄 Status fix: Innervisions Posters & Framing — re-checked live (Eventbrite API), still 0 upcoming events, no change
- 🔄 Status fix: Secret Garden Books — re-checked live, still HTTP 403 from this environment, no change
- 🔍 Investigating: Vortex Pottery Classes — page has changed since last
  check from a prose class schedule to a Squarespace commerce/subscription
  listing; no longer shows day/time schedule text, needs a fresh look

## Source discovery: arts/culture, outdoors, discovery-oriented sweep

Ran `skills/source-discovery/scripts/dead-sources.py` — 22 zero-event
calendars and 14 `externalCalendarFailures`, all pre-existing/known
(the 14 external failures are all Browserbase `HTTP 402` credit
exhaustion, owned by proxy-escalation Mode B, not a discovery action;
the 22 zero-event calendars are individual `seattle-food-trucks`
stops, SAM branches, and other previously-flagged entries with no new
404/410 signal — no new `status: dead` flags this cycle).

Ran 9 fresh web searches rotating through arts & culture, outdoors &
sports, comedy/nightlife, food & drink, and discovery-oriented queries
(new venues, festivals, night markets, climbing gyms, board game
cafes, karaoke/silent disco, `site:seattle.gov`). Every real lead
either was a source already live in `sources/`/`sources/external/`
(e.g. Seattle Art Museum, Seattle Theatre Group's Paramount/Moore/
Neptune) or traced to an entity already tracked in the 900+-file
`docs/source-candidates/` backlog (Reverie Ballroom, Uplift/Edgeworks
climbing, Mox Boarding House, Meeples Games, Salsa Con Todo, Flight
Club, Seattle Women in Tech, etc.) — all previously investigated and
correctly left `notviable`/`blocked`/`candidate`. One entity got its
first per-candidate file this cycle:

- 🔍 Investigating: TeenTix — citywide teen-ticket-program aggregator
  (Craft CMS); calendar page shows only per-day event *counts*, no
  per-event data in static HTML — underlying data endpoint not yet
  located. Also a significant cross-source dedup risk if implemented
  (aggregates partner orgs already covered individually). **Not
  actually new** — logged `❌ Not Viable` in
  `docs/discovery-log/2026-04-23.md` before the per-candidate-file
  system existed, so this is its first standalone file rather than a
  first look; reopened as `investigating` (not re-closed as
  `notviable`) since this pass narrowed the platform/endpoint question
  further without fully resolving it. New file:
  `docs/source-candidates/teentix.md`.
- 🔄 Status fix: Reverie Ballroom — re-checked live (`?format=json`),
  still an empty page shell, no events collection. No change.
- 🔄 Status fix: SODO BIA — re-checked live, ICS URL still redirects to
  the SiteGround `sgcaptcha` JS challenge even with the project's
  standard UA (three consecutive checks now: 08-05, 08-11, 09-11).
  Flipped `status: candidate` → `blocked` — hard-blocked even from this
  environment, not just CI, per the README's definition of `blocked`.
- 🔄 Status fix: Poquitos Seattle, Clean Greens Farm & Market — both
  re-confirmed 0 upcoming events (checked earlier today in the backlog
  sweep above; consistent on a second check this cycle).

No new source implementable this cycle beyond the backlog sweep's
addition above (The Alley). Nothing crossed from `candidate`/
`investigating` into a fetchable, non-empty pipeline today.

## Source discovery: untriaged aggregator-gap-analysis backlog

Re-checked several single-event "aggregator gap analysis" candidate
files (`platform: Unknown`, auto-generated from a one-off sample) for
an actual confirmed platform:

- ✅ Added: The Traveling Goat (Queen Anne bar) — custom HTML ripper
  (`sources/traveling_goat`) — 12 events, 0 parse errors, 1 non-fatal
  uncertainty (a themed promo with no explicit start time). The
  `/events` page is a hand-authored Wix layout (no calendar plugin,
  no schema.org) but renders server-side with a clean repeating
  (date, title, description) block structure — see
  `docs/source-candidates/travelinggoatseattle-com.md`.
- ❌ Not Viable: Central Saloon — WordPress `/events/` page is behind a
  SiteGround `sgcaptcha` JS challenge even from this environment (a
  plain fetch never passes it); not stageable per the JS-challenge
  shortcut without first confirming a residential/browserbase fetch
  clears it, deferred rather than guessed.
- 🔍 Investigating: The Fremont Social (SpotHopper-platform bar site,
  `thefremontsocial.com/events`) — static fetch returns the page shell
  but no event markup in this environment (likely populated by a
  client-side widget); no confirmed data shape yet.
- 🔍 Investigating: Kells Irish Restaurant & Pub — storefront is
  Shopify (`/products.json` returns merch only), but the `/events`
  page's watch-party listings aren't in the static HTML either
  (client-rendered widget); no confirmed data shape yet.
- 🔄 Status fix: Machine House Brewery — reconfirmed WordPress
  `/calendar/` has no Tribe Events signature and no event data in
  static HTML (JS-rendered widget), consistent with prior note.
