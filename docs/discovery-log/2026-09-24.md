## Source discovery: music/comedy/beer/community re-checks, improv/poetry/night-market/climbing/pottery/knitting/flea-market, King County city calendars

Swept the standing candidate list first. The only two `💡 candidate`-status
entries on file (Nudibranch Coffee, Phinney Books) are still stuck at 0
upcoming events per their re-check history; all 8 `investigating` entries
(Baila District, CoCA Seattle, Kirkland Wednesday Market, Fremont Fridays,
Nook & Cranny Books, Seattle Cider Taproom, Seattle Mahjong Club, Seattle
Southside) remain blocked on either 0 live events, a required Algolia API
key, or an unresolved cadence conflict — none flipped to implementable.

Ran fresh searches across music venues, comedy clubs, breweries, community
centers, new-venue openings, art galleries, open mics, drag/burlesque,
record stores, improv theaters, poetry series, night markets, climbing
gyms, wineries, cocktail bars, vintage/flea markets, fiber arts guilds, and
King County city calendars (Renton, Burien, Kent, Beacon Hill, Georgetown).
The overwhelming majority of hits were already covered
(`sources/`/`sources/external/`) or already tracked as `notviable`/
`blocked`/`investigating` in `docs/source-candidates/` — this candidate
pool has been heavily mined over many prior runs.

- ✅ **Added: Renton Downtown Partnership** — Squarespace, aggregator,
  `geo: null` — 2 live events (Olde Fashioned Halloween Party Oct 24 2026,
  Gobble Wobble Wine Walk Nov 21 2026) confirmed via
  `ONLY_SOURCE=renton-downtown-partnership npm run generate-calendars`
  (0 errors) — PR TBD
- ❌ Not Viable: Flight Club darts (SLU) and Flight Club Seattle (Capitol
  Hill live-music venue) — already tracked, no public calendar (re-confirmed)
- ❌ Not Viable: Momentum Climbing (SoDo) — `/sodoevents/` is a stale
  Elementor-style template with unpopulated "EVENT NAME" placeholders, last
  modified 2022; no real events
- 🔍 Investigating: Discover Burien calendar — Squarespace, but a
  `calendarView`/`monthFilter` collection shape the built-in `squarespace`
  ripper type doesn't support (no `upcoming`/`past` split, just the current
  month's mixed past+future items); needs either calendarView support added
  to the built-in type or a small custom ripper
- ❌ Not Viable: The Cap (Toronto, not Seattle), Corralitos Cultural Center
  (Watsonville CA, not Seattle) — geography
- ❌ Not Viable: WordsWest Literary Series — regular monthly series ended
  2019, only sporadic reunion events since
- 🔄 Re-checked, no change: Easy Street Records (DICE venue name still
  unresolvable from this environment), Gasworks Brewing (`/events-page-
  master?format=json` still 0 upcoming), Greenlake Brewing (still parked
  domain), Potterings (still HTTP 403 locally)

## Source discovery: art/festival/brewery/holiday-market re-checks, King County city calendars (batch 7)

No open source-discovery PR to sweep (last merge on this branch was
`#1594`, already on `main`). Confirmed `origin/main` current, no rebase
needed. Ran `skills/source-discovery/scripts/dead-sources.py` again: same
20 zero-event calendars and 16 external failures as this morning's run
(15 `Browserbase HTTP 402` billing exhaustion for `proxy-escalation`,
`saving-great-animals` empty-response) — no new 30+-day dead source.

Ran fresh searches across art gallery openings, fall festivals, new
brewery taprooms, holiday markets, and a `site:eventbrite.com` sweep.
Every hit (Gallery B612, J. Rinehart Gallery, Henry Art Gallery, Frye Art
Museum, publicdisplay.art, Seattle Center Festál, Seattle Christmas
Market, Reuben's Brews, Ounces, Urban Craft Uprising, National Nordic
Museum's Julefest, Unexpected Productions) was already `added` or
previously evaluated — this pool is heavily mined. Bainbridge Island
Museum of Art is Kitsap County, out of scope.

Pivoted to King County suburbs not yet checked in `docs/source-candidates/`
(Auburn, Federal Way, Shoreline, SeaTac, Bothell — Bellevue, Issaquah,
Mercer Island, Redmond, Tukwila, Renton, Kent, Kirkland were already
covered from earlier batches):

- ✅ **Added: City of Bothell Events** — ICS (CivicPlus
  `common/modules/iCalendar`, same platform as `city-of-redmond-community-events`),
  aggregator, `geo: null` — 21 live events (pet fair, coworking meetups, a
  4-part tabletop RPG series, trick-or-treat, a careers expo, city
  holiday closures) confirmed via `ONLY_SOURCE=city-of-bothell npm run
  generate-calendars` (0 fatal errors, 3 non-fatal geocode errors on
  addresses with embedded HTML markup — left for the geo-resolver queue).
  `sources/external/city-of-bothell.yaml`. "Bothell" tag already
  registered (used by `kcls`). — PR TBD
- 🔍 Investigating: **City of Auburn** — reachable, but runs on a Liferay
  portal; the "Subscribe to iCal" control is a JS action URL that renders
  an HTML instructions page rather than a direct `.ics` link. Needs more
  digging to find the actual generated feed URL.
- 🔍 Investigating: **City of Federal Way** — reachable Drupal site, but
  its calendar page (`/calendar/event-list`) has no ical/RSS export in
  the rendered HTML; would need a custom scraper.
- 🔒 Blocked: **City of Shoreline**, **City of SeaTac** — both domains
  return HTTP 403 "Access Denied" for every path tried, including their
  bare homepages, from this environment. Not a CI-only block (nothing to
  stage for the proxy ladder) — genuinely unreachable from here right now.
- ⏭ Not yet checked this run: Sammamish, Newcastle, Normandy Park, Maple
  Valley, Covington, Des Moines, Lake Forest Park, Duvall, Snoqualmie,
  North Bend, Enumclaw, Black Diamond, Carnation, Vashon (city calendar,
  separate from the already-added Vashon arts/farmers-market sources) —
  good next-batch targets for the same CivicPlus/Liferay pattern sweep.
