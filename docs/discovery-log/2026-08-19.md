## Source discovery: candidate implementation cycle

- ✅ Added: 36th District Democrats — recurring (`sources/recurring/`) — PR #1233
- 🔄 Status fix: Dog Yard Bar Events — re-checked; `/events-all` collection confirmed live but 0 upcoming events (stale since Dec 2024) — held at `candidate`

## Source discovery: wellness/yoga, pets, science lectures, maker spaces, car culture, small press/zine fairs, adult sports leagues, nightlife

Scheduled discovery cycle. Ran `dead-sources.py` first — 4 zero-event
calendars (`seattle-creates-all-events`, `external-on-the-hiyu`,
`external-seattle-childrens-museum`, `external-seattledances`) and 2
external failures (`west-seattle-blog`, empty HTTP 200 body;
`urban-league-seattle`, HTTP 403). All but `seattle-creates-all-events` are
already tracked from prior runs (`docs/discovery-log/dead-sources.md`).
`seattle-creates-all-events` is a first-time sighting for this specific
calendar name (Squarespace `?format=json` confirms `upcoming: []`, `past:
30` — the aggregator's own events collection is genuinely empty right now,
not a fetch failure). A single zero-event snapshot doesn't meet the
30-consecutive-day bar for `status: dead`; noting for the build-report
skill to monitor rather than flagging.

Eight web searches across verticals not covered by the last several runs
(2026-08-13 through 2026-08-18 covered open-mic/nightlife/community-centers/
outdoors/music-venues/city-calendars, LGBTQ+/film/night-markets/galleries/
dance/breweries/poetry/markets, and music-venues/tech-meetups/family/
new-venues/festivals/senior-centers): yoga/wellness studio calendars,
dog-friendly event calendars, science lecture series, maker-space workshop
calendars, cars-and-coffee/car-show calendars, zine/small-press fairs, adult
recreational sports leagues, and new bar/nightclub openings.

The majority of leads resolved to sources already `added` or already
tracked: Seattle Makers, North End Makers, Town Hall Seattle, PCNW, Science
in Seattle, Sol Yoga Seattle, Rigs & Coffee at Tiki Motors, The Shop Club
Seattle, Lake Washington Cars & Coffee, Seattle Art Book Fair, RailSpur,
Picklewood, ORA Seattle — all already `added` or have existing candidate
files.

New candidate files written this cycle:

- 🔍 Investigating: **Seattle Yoga News** (`seattleyoganews.com/calendar/`)
  — citywide yoga aggregator running WordPress/Tribe Events; confirmed the
  plugin and an `?ical=1` link, but the Tribe REST API returns 0 events and
  the ICS export itself returns an empty HTTP 200 body. Left as `candidate`
  pending a future re-check rather than `notviable`, since the pipeline
  shape (plain external ICS) is sound if the feed starts returning data.
- 🔍 Investigating: **Seattle RATS** (adult recreational soccer league,
  `seattlerats.org/calendar/all-events/`) — Firebase-backed Angular SPA, no
  server-rendered event data or public API found on a quick pass. Low
  priority (game-schedule fixtures more than public events, plus no
  scrapable endpoint).
- ⏳ Re-checked, now **blocked**: **Q Nightclub** — previously `notviable`
  via Tixr (2026-07-21); re-investigated via its AXS ticketing page
  (`axs.com/venues/125609/q-nightclub-seattle-tickets`), a built-in ripper
  type. Both a plain and a full browser-header-spoofed `curl` return HTTP
  403 — matches the same AXS bot-protection block already documented as the
  reason `sources/5thavenue/ripper.yaml` is `disabled: true` for another
  venue on the same platform. Not stageable (fetch fails even locally);
  flipped to `blocked` with both investigations recorded in the file.
- ❌ Not Viable: **Datza Yoga** (Eastlake studio) — its `/yoga-events/` page
  is static blog/marketing content about past retreats, no calendar plugin
  or feed.
- ❌ Not Viable: **Rex Roxy Pets** dog-events page — Wix SPA (no
  server-rendered content) and primarily a Shoreline/Edmonds/North Seattle
  audience, not Seattle-focused.
- ❌ Not Viable: **Seattle Zine Fest**, **Short Run Comix and Arts
  Festival**, **Seattle Anarchist Bookfair** — all single-annual-event
  WordPress sites with no calendar plugin, ICS export, or API; same shape
  as the previously-rejected "Taste of West Seattle".
- 🔄 Status fix: **Dog Gone Seattle** — new candidate file, `status:
  blocked`. `/category/events/` immediately redirects to a SiteGround
  `sgcaptcha` JS challenge page rather than returning content, so per the
  "fetch fails locally too" rule it's recorded as blocked rather than
  staged for proxy testing (no working pipeline to prove yet).

No new source implemented this cycle — no unimplemented lead cleared the
fetch-validation bar with confirmed events (both AXS and sgcaptcha-gated
leads failed the fetch step; the yoga aggregator's own feed is currently
empty). **Seattle Tech Forum** (🔥 High-confidence ICS candidate via its
Luma-backed feed, noted in the 2026-08-17 log) is the top unimplemented
pick for a future implementation cycle — its 2026-08-17 sibling candidate,
The Center for Active Living, was implemented since then (PR #1231,
merged).
