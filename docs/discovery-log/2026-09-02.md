## Source discovery: existing candidate implementation (Trinity Nightclub)

- ✅ Added: Trinity Nightclub — downtown Pioneer Square nightclub (107
  Occidental Ave S). The existing `trinity-nightclub.md` candidate had
  found the venue's Squarespace homepage linking out to Eventbrite for
  ticket sales but hadn't confirmed the organizer id. Re-investigated and
  found the organizer link directly on the homepage
  (`eventbrite.com/o/trinity-nightclub-33445751813`); the public
  Eventbrite v3 API confirms organizer `33445751813` has 38 live events
  (weekly Y2K Thursdays, Fridays at Trinity, and Reload Saturdays series).
  Implemented as the built-in `eventbrite` ripper type —
  `sources/trinity_nightclub/ripper.yaml` — PR pending. Reuses the
  existing `EVENTBRITE_TOKEN` repo secret already wired into CI for other
  Eventbrite sources (fog-room, club-sur, space-city); no new secret
  needed.
- 🔄 Status fix: `trinity-nightclub.md` marked `added`

## Source discovery: existing candidate implementation (DanceUS Seattle Swing Calendar)

- ✅ Added: DanceUS Seattle Swing Calendar — citywide swing-dance scene
  round-up (classes and socials across ~18 Seattle venues: Century Ballroom,
  Reverie Ballroom, Swing It Seattle, Salsa Con Todo, Arthur Murray, Lowdown
  Ballroom, etc.). The existing `danceus-swing.md` candidate had found the
  page but assumed a plain HTML scrape with no ICS/API; re-investigated and
  found the listing page embeds a full `application/ld+json` Event array
  with per-event lat/lng, venue name, description, and image — only start
  time (and price, for some) needed to come from the visible event cards.
  Implemented as a custom `IRipper` — `sources/danceus_swing/ripper.ts`
  (`sourceRole: aggregator`, `geo: null`, tag `Dance`) — PR #1337. 45
  events confirmed in a local `ONLY_SOURCE` build; 2 Eastside listings
  (Kirkland, Mercer Island) filtered via the JSON-LD address field to stay
  Seattle-focused.
- 🔄 Status fix: `danceus-swing.md` marked `added`

## Source discovery: existing candidate implementation (Watershed Pub & Kitchen)

- ✅ Added: The Watershed Pub & Kitchen — Northgate/Lake City neighborhood
  pub (10104 3rd Ave NE) hosting live music, trivia, a silent book club,
  cider tastings, and periodic food/beer events. The existing
  `watershedpub-com.md` candidate was a bare aggregator-gap stub; a fresh
  investigation found the `/events` page is fully server-rendered HTML
  (React Router SSR, "bevwerk" platform) — no proxy or JS execution
  needed — but has no ICS/JSON feed, so implemented as a custom
  `HTMLRipper` (`sources/watershed_pub/ripper.ts`, `sourceRole: venue`,
  tags `["Nightlife", "Northgate"]`). The site's free-text
  `.blurb-date` field mixes perpetually-recurring blurbs (no date),
  closure/hours notices (date with no time), and real timed events
  (single date+time, same-day range, or short multi-day range); the
  parser uses the presence of "@" plus a 14-day span cap to keep only
  genuine one-off occurrences. 7 events, 0 errors confirmed via
  `ONLY_SOURCE=watershed-pub npm run generate-calendars` against the
  live site.
- 🔄 Status fix: `watershedpub-com.md` marked `added`

## Source discovery: existing candidate implementation (Paper Boat Booksellers)

- ✅ Added: Paper Boat Booksellers — independent bookstore in the West
  Seattle Junction (4522 California Ave SW). The existing
  `paperboatbooksellers-com.md` aggregator-gap stub only had a single
  sample event and no platform investigation; re-investigated and found
  the site's real Squarespace events collection at
  `/calendar/events` (`format=json` → `events-stacked`, `itemCount: 84`,
  24 upcoming). Implemented as the built-in `squarespace` ripper type —
  `sources/paper_boat_booksellers/ripper.yaml` (`sourceRole: venue`,
  tags `Books`, `West Seattle`) — no custom code needed. 24 events, 0
  errors confirmed via `ONLY_SOURCE=paper-boat-booksellers npm run
  generate-calendars`. `geo` resolved via OSM Nominatim (node
  2397384462).
- 🔄 Status fix: `paperboatbooksellers-com.md` marked `added`

## Source discovery: candidate correction (48 North SARC)

- 🔄 Status fix: `48-north-sarc.md` — captured the real ICS URL and
  fetched it. The feed's current events are almost entirely non-Seattle
  yacht clubs (Anacortes, Bellingham, Edmonds, Tacoma, Port Townsend,
  Rose City in Portland OR, Royal Victoria in BC) with no per-event
  location data to filter on; only 1 of 9 organizing clubs
  (Sloop Tavern YC) is Seattle-based. Deprioritized as not
  Seattle-focused enough to implement as-is; left at `status: candidate`
  with findings recorded for a future revisit if the feed's club mix
  shifts.

## Source discovery: existing candidate implementation (SANCA)

- ✅ Added: SANCA (School of Acrobatics & New Circus Arts) — a prior
  investigation (2026-08-31, `sanca-seattle.md`) found SANCA's own
  website has a working Tribe Events ICS feed but its content is
  entirely school-administrative (closures, enrollment windows), not
  audience-facing shows, and marked the source `notviable`. Found a
  different channel while chasing an unrelated Emerald City Trapeze
  lead: its Halloween show ("Carnevolar XIV: Ascension") tickets
  through Eventbrite organizer `44465040643`, whose account name is
  SANCA — SANCA has operated Emerald City Trapeze Arts (2702 6th Ave S,
  SoDo) as a second location since 2023, alongside its own Georgetown
  studio (674 S Orcas St). The public Eventbrite API
  (`/organizers/44465040643/events/?status=live&expand=venue`) confirms
  3 live events (the three Carnevolar nightly occurrences, Oct 29-31
  2026), each carrying a real per-event venue address + lat/lng.
  Implemented as the built-in `eventbrite` ripper type —
  `sources/sanca/ripper.yaml` (`sourceRole: venue`, `geo: null` since
  the org spans two physical locations — mirrors the existing
  multi-branch `third_place_books` pattern) — no new secret needed,
  reuses `EVENTBRITE_TOKEN`. Schema validated via
  `ONLY_SOURCE=sanca npm run generate-calendars` (no `EVENTBRITE_TOKEN`
  in this environment, so 0 events locally with the expected
  "environment variable is not set" `ParseError`, same verification
  pattern used for `fog_room`/`trinity_nightclub`); full event count
  will confirm in CI, which has the token.
- 🔄 Status fix: `sanca-seattle.md` marked `added`

## Source discovery: existing candidate implementation (Elysian Brewing)

- ✅ Added: Elysian Brewing — Capitol Hill flagship brewpub (1221 E Pike
  St). The existing `elysian-brewing.md` candidate had found the site's
  `/events` page is a plain Drupal 10 page with only 2 upcoming items
  and free-text, inconsistently-formatted dates (a recurring-phrase item
  and a one-off `MM DD, YYYY • time range` item), and flagged it 🔴 Low
  confidence for a custom scraper. Re-investigated: the recurring item —
  "Trivia Tuesdays @ Capitol Hill Pub", every Tuesday 7-9 PM — fits the
  `sources/recurring/` model directly (same pattern as the existing
  Geeks Who Drink/Head in the Clouds trivia-night sources), sidestepping
  the free-text date-parsing problem entirely. Implemented as
  `sources/recurring/elysian-capitol-hill-trivia.yaml` (`sourceRole:
  venue`, tags `Trivia`, `Pub Trivia`, `Capitol Hill`; geo resolved via
  OSM Nominatim, node 1726088085). The one-off "Great Pumpkin Beer
  Festival 2026" (Oct 3) was left out — no confirmed year-over-year
  recurrence pattern to encode as a schedule. 1 event, 0 errors
  confirmed via `ONLY_SOURCE=elysian-capitol-hill-trivia npm run
  generate-calendars`.
- 🔄 Status fix: `elysian-brewing.md` marked `added`

## Source discovery: existing candidate implementation (Salsa Vida Seattle)

- ✅ Added: Salsa Vida Seattle — citywide salsa/bachata scene round-up
  (Reverie Ballroom, Sea Monster Lounge, Salsa Con Todo, Sueños de Salsa,
  Seattle Harbor Nightclub, Baila District, Waterfront Park). The existing
  `salsa-vida-seattle.md` candidate had confirmed 43 upcoming events but no
  RSS/ICS/API, flagging it for HTML scraping. Re-investigated: the guide
  page server-renders a single `application/ld+json` `ItemList` of `Event`
  items on first load (no browser/AJAX needed) with real start/end times,
  full street address + lat/lng, price, and description per event — same
  shape as the existing DanceUS JSON-LD rippers. Implemented as a custom
  `IRipper` — `sources/salsa_vida_seattle/ripper.ts` (`sourceRole:
  aggregator`, `geo: null`, tag `Dance`) — PR #1350. Filtered to
  `addressLocality === "Seattle"`: 14 of the 20 items in the fetched
  sample are Seattle-proper; 6 (Shoreline, Kent, Kirkland) are dropped. 14
  events, 0 errors confirmed via `ONLY_SOURCE=salsa-vida-seattle npm run
  generate-calendars` against the live site.
- 🔄 Status fix: `salsa-vida-seattle.md` marked `added`

## Source discovery: existing candidate implementation (Seattle Chamber of Connection — Best Day Ever)

- ✅ Added: "Best Day Ever" — a recurring monthly neighborhood-tour series
  hosted by the Seattle Chamber of Connection, spotlighting local businesses
  in a different Seattle neighborhood each time (Ballard, Hillman City,
  Fremont, Lake City, Central District dates mentioned on the org's page).
  The existing `seattle-chamber-of-connection.md` candidate had confirmed
  the org's `/event-calendar` page lists real dated events but found no
  Squarespace events collection (`?format=json` returns an empty static
  page, not `events-stacked`) and flagged a Luma link for a different
  program. Re-investigated: most listed events link out to Luma
  registration pages; found the org runs its own Luma calendar at
  `luma.com/bestdayever` (id `cal-uCEOOuhY6UQpJ77`), whose standard ICS
  export (`api.lu.ma/ics/get?entity=calendar&id=...`, same pattern as the
  existing `seattle-tech-forum` source) returns a valid feed with 2
  upcoming events, each carrying a full street address and `GEO`
  coordinate. Implemented as
  `sources/external/seattle-chamber-best-day-ever.yaml` (`sourceRole:
  venue`, `geo: null` since the venue rotates monthly, tag `Community`).
  2 events, 0 errors confirmed via
  `ONLY_SOURCE=seattle-chamber-best-day-ever npm run generate-calendars`
  against the live feed.
- 🔄 Status fix: `seattle-chamber-of-connection.md` marked `added`

## Source discovery: existing candidate implementation (The Tin Lizzie Lounge)

- ✅ Added: The Tin Lizzie Lounge — hotel lounge bar inside the MarQueen
  Hotel, Lower Queen Anne (600 Queen Anne Ave N). The existing
  `thetinlizzielounge-com.md` candidate was a bare aggregator-gap stub
  (single sample event, "Balboa, Booze and a Dash o' Blues"); re-investigated
  and found the `/live-music/` page server-renders a fixed weekly/monthly
  schedule of four distinct recurring live-music programs, each with a
  clean ordinal-weekday pattern: Balboa dance social (2nd & 4th Mondays),
  bluegrass duo Josh Philpott and Tim Wetmiller (every Thursday), singer
  Enrique Henao (1st Sundays), and the Christian Smith Quartet jazz night
  (2nd & 4th Sundays). Implemented as four `sources/recurring/` files
  (mirroring the existing `jazztime-seattle-*` one-file-per-program
  pattern), `sourceRole: venue`, geo resolved via OSM Nominatim (node
  1773269726). The page's "Uptown Art Walk" listing was left out (already
  covered by `sources/recurring/uptown-artwalk.yaml`), as was "Featured
  Sunday Artist" (a rotating-performer placeholder with no fixed name).
  4 events (1 per program), 0 errors confirmed via `ONLY_SOURCE=...
  npm run generate-calendars`.
- 🔄 Status fix: `thetinlizzielounge-com.md` marked `added`

## Source discovery: existing candidate implementation (CC's Seattle)

- ✅ Added: CC's Seattle — longtime LGBTQ+ bar on Capitol Hill (1701 East
  Olive Way). The existing `ccs-seattle.md` candidate had confirmed the
  Wix `/events` page lists a fixed weekly/monthly schedule of
  distinct-named recurring nights rather than one-off dated events, and
  flagged it for a `sources/recurring/` entry. Re-fetched the page to
  confirm the current live schedule and exact times, and geocoded the
  address via OSM Nominatim (node 2158768715). Because each named night
  is a genuinely distinct event (not one event on multiple schedules),
  implemented as nine separate `sources/recurring/ccs-seattle-*.yaml`
  files (mirroring the existing multi-file-per-venue pattern used by
  `unicorn-seattle-*`/`dreamland-*`): Tunesday Music Bingo (every Tue),
  Seattle GAYMERS (every Wed), The Gathering Happy Hour/PNW Black
  Pride/ONYX Northwest (1st Fri), Seattle Men in Leather Social (1st
  Fri), Fetish Night (1st Sat, $5), Naked Monday Meet Up (2nd Mon),
  Funderwear (3rd Sat, $5), Furry Friday/XL Bears Bear Bust (4th Fri),
  WSLO Community Social (4th Sat). The venue's weekly ASL class was
  shown "currently on summer break" at time of check, so left out of
  this pass. `sourceRole: venue`, tags `Nightlife`, `Capitol Hill`. 9
  events (1 per file), 0 errors confirmed via `ONLY_SOURCE=<the 9
  names> npm run generate-calendars`; discovery API validated via `npm
  run check-discovery-api`.
- 🔄 Status fix: `ccs-seattle.md` marked `added`

## Source discovery: record stores, climbing gyms, coffee-shop open mics, fall/holiday festivals, karaoke bars, new venues, axe throwing, jazz clubs (2026-09-02)

Rotated searches across verticals not covered in the last several days:
record-store in-stores, climbing-gym event calendars, coffee-shop open
mics, October/holiday-market seasonal festivals, roller rinks, karaoke
bars, newly-opened venues, axe-throwing leagues, and jazz clubs. Given
900+ tracked candidate files, most hits were already implemented
(Sonic Boom Records, Couth Buzzard, Skylark Cafe, Half Moon Bouldering,
Railspur, Seattle Jazz Fellowship, Ozzie's) or already dispositioned as
`blocked`/`notviable` in earlier cycles (Bouldering Project — 403;
Edgeworks Climbing — 403 on every path incl. Rock Gym Pro; Uplift
Climbing — Squarespace page with no real events collection; Blade &
Timber, Flight Club Darts SLU, Flight Club Seattle, Hula Hula,
Rickshaw Restaurant & Lounge — no public events calendar). Re-checked
several long-standing 0-event `candidate` Squarespace/Eventbrite leads
(Dog Yard Bar, Reverie Ballroom, Phinney Books, Poquitos Seattle,
Innervisions Posters, Glitter and Gold Studio) — all still genuinely
0 upcoming events at time of check, so none crossed the "never merge
at 0 events" gate this cycle.

One re-investigation flipped to a real, high-confidence implementation:

- ✅ Added: **Dimitriou's Jazz Alley** — established Belltown jazz club
  (2033 6th Ave). Previously flagged `status: proxy` on 2026-05-08 after
  `calendar.jsp` returned HTTP 503 from every path tried. Re-checked
  live this cycle and found a normal HTTP 200 with real event data — the
  earlier 503 was evidently a transient outage, not a persistent block,
  so no proxy is needed after all. Implemented as a custom `IRipper`
  (`sources/jazz_alley/ripper.ts`, `sourceRole: venue`, tags `Music`,
  `Belltown`) doing a two-level scrape: the calendar page lists 26
  upcoming multi-night "shows" linking to a per-show detail page, and
  each detail page's `<select name="perfnum">` carries the real
  per-night (sometimes per-set) showtimes plus price. A fully sold-out
  performance found live is parsed and emitted with `cost: { soldOut:
  true }` rather than silently dropped. 100 events, 0 errors confirmed
  via `ONLY_SOURCE=jazz-alley npm run generate-calendars` against the
  live site — PR #1354.
- 🔄 Status fix: `jazz-alley.md` marked `added` (was `proxy`)
- ❌ Not Viable: none new this cycle (all dispositions above were
  re-confirmations of prior findings, not new verdicts)
- 🔍 Investigating: none carried forward this cycle

## Source discovery: existing candidate implementation (Seattle Artist League)

- ✅ Added: Seattle Artist League — art school in Georgetown (5516 4th Ave
  S) offering drawing, painting, pottery, and sculpture classes/workshops.
  The existing `seattle-artist-league.md` candidate had confirmed
  WordPress with no ICS/JSON feed and flagged HTML scraping of the classes
  listing. Re-investigated: the site runs WooCommerce, and its public
  "Store API" (`/wp-json/wc/store/v1/products?per_page=100`, used by the
  site's own headless cart widgets, no auth required) returns the full
  ~85-product class catalog as structured JSON — no HTML scraping needed
  after all. The class start date lives only in the free-text product
  title (e.g. "Beginning Drawing WEDNESDAY EVENING begins 10.28"); time
  (and occasionally an off-site location, e.g. an outdoor drawing workshop
  at the Duwamish River) lives in inconsistently-worded "Time:"/"Where:"
  bullets inside `short_description`. Implemented as a custom `JSONRipper`
  — `sources/seattle_artist_league/ripper.ts` (`sourceRole: venue`, tags
  `Arts`, `Georgetown`; geo resolved via OSM Nominatim, way 231286747) —
  filtering out Membership/Payment-Due products and ONLINE-titled classes
  (not physical Seattle events), and falling back to a placeholder
  time/location + `UncertaintyError` for the minority of listings with no
  `Time:`/`Where:` bullet — including the small number of "@ The Brick"
  listings (a separate SAL-run space at a different street address) that
  only mention it in the title. A product whose title has no date token at
  all (gift certificates, the certificate program, drop-in sessions, kits,
  independent study) is silently skipped rather than reported as a
  ParseError — real store content, not a malformed class, and CI's
  `newSourceParseErrors` gate fails the build on *any* ParseError from a
  brand-new source. 65 events, 0 ParseErrors, 5 pending uncertainties (4
  missing time, 3 missing an off-site location, 2 overlapping both)
  confirmed via `ONLY_SOURCE=seattle-artist-league npm run
  generate-calendars` against the live site.
- 🔄 Status fix: `seattle-artist-league.md` marked `added`

