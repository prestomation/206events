## Source discovery: existing candidate implementation (Trinity Nightclub)

- ✅ Added: Trinity Nightclub — downtown Pioneer Square nightclub (107
  Occidental Ave S). The existing `trinity-nightclub.md` candidate had
  found the venue's Squarespace homepage linking out to Eventbrite for
  ticket sales but hadn't confirmed the organizer id. Re-investigated and
  found the organizer link directly on the homepage
  (`eventbrite.com/o/trinity-nightclub-33445751813`); the public
  Eventbrite v3 API confirms organizer `33445751813` has 38 live events
  (weekly Y2K Thursdays, Fridays at Trinity, and Reload Saturdays series).
  Implemented as the built-in `eventbrite` ripper type —
  `sources/trinity_nightclub/ripper.yaml` — PR pending. Reuses the
  existing `EVENTBRITE_TOKEN` repo secret already wired into CI for other
  Eventbrite sources (fog-room, club-sur, space-city); no new secret
  needed.
- 🔄 Status fix: `trinity-nightclub.md` marked `added`

## Source discovery: existing candidate implementation (DanceUS Seattle Swing Calendar)

- ✅ Added: DanceUS Seattle Swing Calendar — citywide swing-dance scene
  round-up (classes and socials across ~18 Seattle venues: Century Ballroom,
  Reverie Ballroom, Swing It Seattle, Salsa Con Todo, Arthur Murray, Lowdown
  Ballroom, etc.). The existing `danceus-swing.md` candidate had found the
  page but assumed a plain HTML scrape with no ICS/API; re-investigated and
  found the listing page embeds a full `application/ld+json` Event array
  with per-event lat/lng, venue name, description, and image — only start
  time (and price, for some) needed to come from the visible event cards.
  Implemented as a custom `IRipper` — `sources/danceus_swing/ripper.ts`
  (`sourceRole: aggregator`, `geo: null`, tag `Dance`) — PR #1337. 45
  events confirmed in a local `ONLY_SOURCE` build; 2 Eastside listings
  (Kirkland, Mercer Island) filtered via the JSON-LD address field to stay
  Seattle-focused.
- 🔄 Status fix: `danceus-swing.md` marked `added`
