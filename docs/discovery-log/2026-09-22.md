## Source discovery: arcade bars, board game cafes, distilleries, wineries, pottery, comics, plant shops, makerspaces, knitting

No open source-discovery PR to sweep (last run's PR merged; open PRs today
are `#1537` UI fix, `#1453` seattle-food-trucks proxy staging, and `#760`
8-Bit Brass Band, all unrelated/unchanged). `origin/main` confirmed current,
no rebase needed.

Re-checked the standing highest-confidence `💡 candidate` entries first via
live Squarespace `?format=json` / Eventbrite public-API calls: Dog Yard Bar,
Phinney Books, Poquitos Seattle — all still 0 upcoming events, no change.

Ran fresh searches across distillery/winery tasting rooms, board game cafes,
arcade bars, comic shops, pottery studios, plant shops, and
makerspaces/knitting stores. Most leads resolved to sources already `added`
(Phoenix Comics and Games, Plant Amnesty, The Missing Piece, North End
Makers, Acorn Street Shop, Raygun Lounge) or previously-evaluated candidates
(Mox Boarding House — Occasion ticketing platform, no listing endpoint,
`notviable`; Meeples Games — still SiteGround JS-challenge blocked even from
this environment, confirmed via `sg-captcha: challenge` response header;
REI Seattle flagship — still unreachable; Seattle Artist League / PCC
Markets — still client-side-rendered with no confirmed JSON endpoint;
Seattle Knitters Guild — not primarily Seattle).

- ✅ Added: **Time Warp** (pinball and noodle bar, 1420 10th Ave, Capitol
  Hill) — found via an "arcade bar" search. Runs the WordPress "Simple
  Calendar" (Google Calendar Events) plugin — the same `simcal-event` HTML
  grid pattern already implemented for `sources/hellbent_brewing`. Confirmed
  22 distinct dated events server-rendered for the remainder of September
  2026 (recurring weekly karaoke, weekly/biweekly DJ nights, daily happy
  hour) via live fetch. No ICS/JSON export or working month-pagination
  endpoint found, so implemented as a 🔴 custom HTML ripper reusing the
  proven `hellbent_brewing` parsing approach (`data-event-start`/
  `data-event-end` Unix timestamps on each `<li class="simcal-event">`).
  Address geocoded via an exact OSM POI match (`osmType: node`,
  `osmId: 11880442163`). Tags `Nightlife`, `Gaming`, `Capitol Hill`.
  Implemented as `sources/time_warp_bar/`. Verified via
  `ONLY_SOURCE=time-warp-bar npm run generate-calendars`: 7 events, 0
  errors, 0 geocode errors. Full `npm run test`: 3966 tests, 226 files
  green. See `docs/source-candidates/time-warp-bar.md`.
- ❌ Not Viable: **Fancy Plants Seattle** — Weebly site, terrarium class
  dates only in marketing prose, no structured/dated feed found.
- ❌ Not Viable: **Waterland Arcade** — venue is in Des Moines, WA, outside
  Seattle proper; fails the Seattle-focus gate as a standalone venue.

No other fresh lead cleared the quality gate today — everything else was
either already covered, blocked, or had 0 upcoming events on live re-check.

## Source discovery: live music, tech/civic aggregators, senior/community centers, dance, aggregator-gap backlog sweep

Checked for open discovery PRs from prior runs first — none found (last
merge was `#1562`, already landed on `main`). Confirmed `origin/main` was
current, no rebase needed.

Ran `skills/source-discovery/scripts/dead-sources.py` against the local
build output: 20 zero-event calendars (the standing `seattle-food-trucks-*`
set, `sam-*` sub-venues, and other long-known small/empty branches — no
change) and 16 external failures (15 are `Browserbase fetch failed: HTTP
402` billing exhaustion, handled by `skills/proxy-escalation`;
`saving-great-animals` is the pre-existing empty-ICS issue). No new
30+-day dead source found.

Ran fresh searches across live music venues, Eventbrite/ICS/RSS discovery
queries, new venue openings, comedy open mics, drag shows, tech meetups,
wine/cocktail tastings, immigrant/refugee orgs, skateparks/BMX, Black-owned
business events, vintage markets, dance studios, nonprofit galas, rooftop
bars, tabletop RPG/game stores, anime conventions, record fairs, cideries,
and distilleries — nearly every lead resolved to a source already `added`
(chophouse_row, emerald_city_comedy, sync_seattle, seattle_cocktail_club,
seattle-tech-forum, bbyc_ballard, blue-highway-games-*, cidbia) or a
previously-evaluated candidate (Pike Pine Social, Joe's Bar & Grill CID,
We Rise Wines, Needlehook/Northwest Yarns — still Bellingham-only,
Improbable Escapes, King County Parks, KCLS — regional not Seattle-proper).
Re-checked several standing `investigating`/near-decision candidates
(Asylum Collective — still 0 upcoming Squarespace events; Ballard Comedy
Takeout — still dormant on the public Eventbrite mirror; Scarecrow Video —
still connection-reset blocked; Seattle Skate Features — no address or
confirmed schedule, Instagram-only). New but not viable this pass: Shib
Shib (Chinatown-ID cocktail bar, opened Sept 3 — Squarespace site with no
events page yet, email-list only); Seattle ReCreative (confirmed Squarespace
+ 3 embedded public Google Calendars, decoded and ICS-verified live, but
dominated by internal ops noise — "Store Closed"/"Private Space Rental" —
and only 0-2 future events across all three feeds at time of check, not
worth the noise).

Also swept the ~150-entry "aggregator gap analysis" backlog (`docs/source-
candidates/*.md` tagged `Discovered via aggregator gap analysis`, mostly
last-checked 2026-08-25, still `status: candidate`) for anything
individually promising: `apm.activecommunities.com`/`anc.apm.
activecommunities.com` (Seattle Parks & Rec ActiveCommunities) is a real,
high-volume system but sits behind an F5/BigIP session-cookie + CSRF-token
SPA with no discoverable public REST endpoint from this environment — left
as `candidate`, not upgraded. `mountaineers.org` returned HTTP 403
(Cloudflare) on this pass. Most others in the backlog are outside Seattle
proper (Redmond/Kent/Renton/Tukwila/Mercer Island/Bellevue city sites,
King County Parks, KCLS) or single-city-block regional aggregators too
broad to scope safely (Bandsintown, SeatGeek, Songkick, ImGoodEvents,
Nextdoor).

- ✅ Added: **GenPride Center** (LGBTQ+ senior-services nonprofit, 1521
  Broadway Suite A, First Hill) — confirmed via the aggregator-gap
  candidate `genprideseattle-org.md` (sample: "Hot Lunch"). The WordPress
  `/community-calendar/` page itself has no scrapable feed (JS-rendered,
  no ICS/Tribe/JSON), but `/programs/` and `/hot-lunch/` publish the
  center's full slate of regular in-person programs as plain server-
  rendered text with explicit day-of-week + time schedules — verified
  directly against the raw HTML. Implemented as **8**
  `sources/recurring/genpride-*.yaml` entries (one program each, same
  multi-file-per-venue pattern as `blue-highway-games-*`): Hot Lunch
  (every Tue & Thu), Seasoned Together (last Monday), Senior Trans Support
  Group (2nd & 4th Wed), Elders Meet & Greet (1st & 3rd Wed), Senior Men's
  Support Group (2nd & 4th Tue), PRIDE Writers (1st & 3rd Wed), Dr. John's
  Tech Assist Clinic (1st & 3rd Thu), Grief & Loss Support Group (2nd &
  4th Tue). Geocoded via Nominatim (`osmType: way`, `osmId: 808907363`).
  Tags `Community`, `Queer`, `First Hill`. Skipped "Game Club" (1st, 2nd &
  3rd Tuesdays — three ordinals aren't representable by the `parseSchedule`
  DSL's `every`/`last`/single-ordinal/`Nth and Mth` patterns; encoding it
  as "1st and 3rd" would silently drop the 2nd Tuesday, so it was left out
  rather than guessed). Verified via `ONLY_SOURCE=<8 names> npm run
  generate-calendars`: all 8 build with 0 errors, 9 events across the new
  tag aggregates. Full `npm run test:all` (3958 + 602 tests) green.
- ❌ Not Viable: **Seattle ReCreative** (Georgetown/Greenwood makerspace) —
  confirmed 3 public Google Calendar embeds with live ICS exports, but
  content is dominated by internal-ops entries (store hours/closures,
  private rentals) and had 0-2 future events across all three feeds at
  time of check; not worth the noise this cycle.
- 🔍 Investigating: **Seattle Skate Features** — Wix site, confirmed
  Wednesday/Friday community skate sessions on the events page but no
  physical address or confirmed exact schedule found; Instagram-only for
  current dates.
