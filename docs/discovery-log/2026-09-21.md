## Source discovery: implementing a candidate

Swept the standing candidate list first — re-checked the highest-confidence
`💡 candidate`-status entries already on file (Dog Yard Bar, Reverie Ballroom,
Phinney Books, Poquitos Seattle, Reconnect South Park, Nudibranch Coffee,
Uplift Climbing, Grey Drizzle Fibers, Queer Events Seattle, Human People Beer,
Midwest Coast Brewing, Clean Greens Farm & Market, Urban Community Councils
of Seattle, Seattle Cider Taproom, NCNW Seattle Section, Innervisions
Posters/Open Mic Night) via live Squarespace `?format=json` / Eventbrite
public-API re-checks — all still 0 upcoming events, no change. Grey Drizzle
Fibers now has 1 upcoming event, but it's in Bellevue (not Seattle), so still
fails the Seattle-focus gate.

Ran fresh searches across trivia/karaoke bars, drag brunch, record stores,
farmers/growers markets, art gallery openings, plant shops, and new
brewery/taproom openings. Most leads resolved to sources already `added`
(Salmon Bay Eagles, NW Dance, Conor Byrne, Reuben's Brews, West Seattle
Nursery) or previously-evaluated candidates (Wanderlust Nursery — notviable,
regional not Seattle-primary; Borealis on Aurora — real Tribe Events ICS
feed but the venue is in Shoreline, not Seattle proper).

- ✅ Added: **Seattle Growers Market** (Georgetown wholesale flower market,
  665 South Orcas St) — confirmed Squarespace events collection
  (`?format=json`, `itemCount: 35`, 5 upcoming events with future epoch
  `startDate`s: Coffee & Cut Flowers grower meetups, Fall Hand-tied Bouquet
  workshop, Hand-Build a Ceramic Vase workshop, Yule Log Centerpiece
  workshop). 🔥 High-confidence built-in `squarespace` type, implemented as
  `sources/seattle_growers_market/ripper.yaml` — no custom code needed.
  Address geocoded via Nominatim (`osmId: 2396888125`, node). Tags
  `Creation`, `Georgetown`. Verified via
  `ONLY_SOURCE=seattle-growers-market npm run generate-calendars`: 5 events,
  0 errors, 0 geocode errors.

No other fresh lead cleared the quality gate today — everything else was
either already covered, blocked (Easy Street Records still can't resolve
the correct DICE venue-name filter from this environment; Hardcore 4 Harm
Reduction still `HTTP 502`), or 0 upcoming events on live re-check.

## Source discovery: vegan food, historic tours, dive-bar trivia, new openings, yoga, science/maker/senior verticals, night markets

Checked the two standing open non-discovery PRs first — `#1453`
(seattle-food-trucks proxy staging, unchanged) and `#760` (8-Bit Brass Band,
still blocked on upstream, unchanged). No open source-discovery PR from a
prior run to sweep (yesterday's, `#1547`, already merged). Confirmed
`origin/main` is current, no rebase needed.

Ran `skills/source-discovery/scripts/dead-sources.py` against the production
`build-errors.json`: 19 zero-event calendars (all previously-tracked —
`seattle-food-trucks-*` stops staged under `#1453`, `sam-*` sub-venues, and
other long-known small/empty branches) and 16 external failures (15 are
`Browserbase fetch failed: HTTP 402` billing exhaustion, handled by
`skills/proxy-escalation`; `saving-great-animals` is a pre-existing `null`
error). No new 30+-day dead source found.

Ran fresh searches across vegan food festivals, historic walking tours,
dive-bar/pool-hall trivia, new bar openings, yoga studios, science lecture
series, maker/craft fairs, senior centers, night markets, and tech meetups.
Nearly every lead resolved to a source already `added` or a previously
evaluated candidate (Historic Seattle, Wild Rose Bar, Belltown Yacht Club,
Queen Anne Beerhall via seattle_showlists, Quality Flea Center via
punk-rock-flea-market/CHASM, Unicorn Seattle, Julia's on Broadway/Le Faux,
Queer/Bar, SouthEast Seattle/Ballard/Central Area/West Seattle senior
centers, Seattle Night Market, Seattle Local Markets, CID Night Market,
Renegade Craft Fair, Urban Craft Uprising, Seattle Makers) — caught before
re-investigating by checking `sources/` and `docs/source-candidates/` first.

- ✅ Added: **South Park Senior Citizens** (South Park Neighborhood Center,
  8201 10th Ave S #4) — found via the senior-center vertical search, not
  previously tracked under this name. WordPress site running The Events
  Calendar (Tribe Events) plugin; its `?ical=1&eventDisplay=list` export
  returns a valid ICS feed directly (HTTP 200) from this environment — no
  proxy needed. 30 upcoming VEVENTs: community dining, EnhanceFitness,
  Zumba, karaoke, Lotería y BINGO, book club, tech support, cultural
  celebrations. 🔥 High-confidence ICS integration, no custom code —
  `sources/external/south-park-senior-citizens.yaml`. Geocoded to the exact
  building via Nominatim (`osmType: way`, `osmId: 224019307`). "South Park"
  was not yet a registered neighborhood tag, so it was added to
  `city.config.ts`'s `neighborhoods` list in the same PR. Tags `Community`,
  `South Park`. Verified via `ONLY_SOURCE=south-park-senior-citizens npm run
  generate-calendars`: 30 events, 0 errors, 0 geocode errors. Full `npm run
  test` (3958 tests, 225 files) green. See
  `docs/source-candidates/south-park-senior-citizens.md`.
- ❌ Not Viable: **Death & Co Seattle** (Pioneer Square cocktail bar,
  opened Sept 2026) — no public events calendar; reservations via Resy only.
- ❌ Not Viable: **Radical Vegan Fest** — single annual one-day event at an
  already-covered venue (Quality Flea Center); organizer site
  (`radicalveganfest.com`) didn't resolve from this environment.
- ❌ Not Viable: **Vegan Street Fair (Seattle)** — single annual one-day
  event (Cal Anderson Park) with no organizer site of its own, only
  secondary listings.
- 🔍 Investigating: **Nerd Nite Seattle** — confirmed monthly lecture
  series (next: Old Stove Brewing, Oct 2, 2026), but neither
  `nerdnite.com/seattle/` nor `/city/seattle` resolved (both 404); the
  correct chapter URL wasn't found this cycle.
