## Source discovery: implementing a candidate (Baba Yaga)

Checked the two open source PRs first: `#1453` (seattle-food-trucks proxy
staging) unchanged, owned by proxy-escalation. `#760` (8-Bit Brass Band)
re-checked live — fetched the full 30-entry event sitemap and every event
detail page's JSON-LD `startDate`; the newest is still "Nerd Night at
Barboza" on 2026-09-04, three days in the past — remains correctly held as
a draft per its own note until the band publishes upcoming shows.

Swept the `status: candidate` backlog for a source with currently-verifiable
data rather than a fresh web search, since the backlog is heavily
picked-over after 21+ prior rotations. Re-checked the highest-confidence
🔥/🟡-tier picks first — all still 0 upcoming events, no change: Midwest
Coast Brewing (`/upcoming?format=json` still `itemCount: 0`, static page not
a real Events collection), Poquitos Seattle (`itemCount: 16`, `upcoming`
empty), Queer Events Seattle (`upcoming: 0`, `past: 1`), Seattle Cider
Taproom (Eventbrite `object_count: 0`), Innervisions Posters / Seattle
Records (`object_count: 0`), Clean Greens Farm & Market (`object_count: 0`),
Dog Yard Bar (`/events-all` still dead since ~Dec 2024), 48 North SARC
(still deprioritized — regional feed), Grey Drizzle Fibers (still
Renton-based, 0/2 Seattle-located). Also re-confirmed `secretgardenbooks.com/
upcoming-events` still 403s from this environment (unchanged since
2026-09-11) and `growlerzseattle.com` still has a broken TLS certificate.

One promising lead paid off: `babayagaseattle-com.md` had been filed as an
`Unknown`-platform aggregator-gap-analysis stub, but Baba Yaga is a real
Pioneer Square music venue/bar already present only as a non-dedicated entry
inside `sources/seattle_showlists`.

- ✅ Added: **Baba Yaga** — Pioneer Square bar/music venue (124 S
  Washington St). Confirmed it runs its event promos through **SpotHopper**,
  the same restaurant/bar marketing platform already integrated for
  `sources/angry_beaver_seattle`, `sources/sullys_queen_anne`, and
  `sources/bad_alberts` — spot id `275781` found embedded in the site's own
  texting-permission contact link. Public, unauthenticated JSON API
  (`spothopperapp.com/api/spots/275781/events`) confirmed live with 2
  upcoming free promo-day events (National Coffee Day, National Taco Day).
  Implemented as `sources/baba_yaga` (custom `JSONRipper`), copied directly
  from the proven `sullys_queen_anne` SpotHopper parser. Per AGENTS.md
  "Prefer venue websites over showlists," marked `skip: true` on the
  existing `Baba Yaga` `VENUE_CONFIG` entry in `sources/seattle_showlists`,
  removed its calendar entry from `seattle_showlists/ripper.yaml`, and added
  `allowed-removals/seattle-showlists-baba-yaga.ics`. Reused the venue's
  existing geo/OSM match from the showlists entry (no new geocode needed).
  `sourceRole: venue`, `cost: free`, tags `Music`, `Nightlife`,
  `Pioneer Square`. 2 events, 0 parse errors, verified via
  `ONLY_SOURCE=baba-yaga npm run generate-calendars`; full `npm run test`
  (3867 tests) and `npx tsc --noEmit` (no new errors) both green. See
  `docs/source-candidates/babayagaseattle-com.md`.
- 🔄 Status fix: re-confirmed 0 change on Midwest Coast Brewing, Poquitos
  Seattle, Queer Events Seattle, Seattle Cider Taproom, Innervisions
  Posters, Clean Greens Farm & Market, Dog Yard Bar, 48 North SARC, Grey
  Drizzle Fibers, `secretgardenbooks.com` (still 403), `growlerzseattle.com`
  (still broken TLS cert).

## Source discovery: implementing a candidate (Ozzie's DJ Nights & Karaoke)

Twenty-third rotation of the day. Checked for open PRs from prior drains/
discovery runs first — only the two known standing PRs exist (`#1453`
seattle-food-trucks proxy staging, `#760` 8-Bit Brass Band), both
unchanged, no action needed; confirmed `origin/main` == the just-merged
Baba Yaga commit, no rebase needed. Ran a wide round of fresh searches
across life drawing, jazz jam, silent disco, goth/industrial nightlife,
whiskey/wine tasting, queer prom/drag king, and food truck verticals —
nearly every real lead was already `added`/`notviable`/`investigating` in
this extremely well-picked-over backlog (Massive/Sketchy figure drawing,
Owl 'n Thistle jazz jam, Silent Disco NW, Silent Dance Alki, Columbia City
Night Market, Seattle Night Market). A few genuinely new venues turned out
not to have real machine-readable calendars: Smith Tower (`/happenings/`
is static category copy, individual FareHarbor tour items rather than
dated one-off events), Screwdriver Bar (Squarespace confirmed but
`/events?format=json` is `typeName: page`, `itemCount: 0` — not a real
Events collection), Life on Mars (WordPress/Divi, no Tribe Events plugin
or structured event data), Art Love Salon (Next.js SPA, no RSC flight
data in the plain-fetched HTML this time, unlike PublicDisplay.ART).
Investigated **Dark Seattle** (`darkseattle.net`), a hand-curated
multi-venue goth/industrial show calendar in the same shape as the
already-implemented `nw_metal_calendar` (dated headers, `.outlined` event
divs) — real and reachable, but heavy overlap with already-covered venues
(Kremwerk, Barboza, Baba Yaga, Rendezvous, Chop Suey) and no year in the
date strings; left as a 🔴 Low-confidence `investigating` candidate for a
future cycle rather than implemented this pass.

Washington Street Art Center (from an "art gallery Squarespace" search)
turned out to be in Somerville, MA, not Seattle — rejected, not filed as a
candidate.

- ✅ Added: **Ozzie's DJ Nights & Karaoke Night** — two more recurring
  programs at the already-covered Ozzie's (105 W Mercer St, Lower Queen
  Anne; `ozzies-run-club`/`ozzies-drag-brunch` added 2026-08-23). The
  original investigation flagged nightly karaoke and Friday/Saturday DJ
  nights as real but "not distinctively dated beyond 'every night' or a
  single weekly slot" and deprioritized them — but the site's banner
  ticker states explicit fixed times for both ("KARAOKE NIGHTLY · 9PM",
  "DJ FRI & SAT · 10PM"), and the already-implemented
  `bush-garden-karaoke` source is precedent for exactly this "every day"
  recurring shape. Implemented as two more `sources/recurring/` files
  (same one-program-per-file convention as this venue's existing two):
  `ozzies-karaoke` (7 `every <day>` entries, 21:00, `PT5H` — 9pm to the
  venue's stated 2am close) and `ozzies-dj-nights` (`every
  Friday`/`every Saturday`, 22:00, `PT4H` — 10pm to 2am close). Both
  `cost: free` (no cover charge mentioned anywhere on the site). Reused
  the venue's existing geo (OSM node 2400948514). Tags `Nightlife`,
  `QueenAnne`. 7 + 2 events, 0 parse errors, verified via
  `ONLY_SOURCE=ozzies-karaoke,ozzies-dj-nights npm run
  generate-calendars`; full `npm run test` (3867 tests) green. See
  `docs/source-candidates/ozzies-in-seattle.md`.
- 🔍 Investigating: **Dark Seattle** (`darkseattle.net`) — real,
  reachable, hand-curated multi-venue goth/industrial calendar with a
  parseable `.outlined`-div structure (same shape as `nw_metal_calendar`),
  but dates omit the year and there's heavy cross-source overlap with
  already-covered venues. Not implemented this cycle.
