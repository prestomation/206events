## Source discovery: music venues, community centers, art galleries, running/race calendars, pop-up markets, karaoke/nightlife (2026-08-26)

Ran `dead-sources.py` first — 5 zero-event calendars (`cidbia-cidbia-events`,
`sam-sam-downtown`, `sam-sam-asian-art`, `sam-sam-sculpture-park`,
`three-dollar-bill-cinema-three-dollar-bill-cinema`) and 13 external failures,
12 of which are `Browserbase fetch failed: HTTP 402` (proxy credit
exhaustion this run, not a source problem) plus `west-seattle-blog`
(`TypeError: fetch failed`; `sources/external/west-seattle-blog.yaml` already
documents this as a known-broken upstream ICS export — the endpoint returns
HTTP 200 with an empty body — while the site's own events page still works,
so the source is intentionally kept rather than disabled). SAM's three
branches and CIDBIA have shown up intermittently with 0 events across many
prior discovery logs without hitting a 30-consecutive-day pattern — normal
programming gaps, not death.

**Three Dollar Bill Cinema is a real signal, not a routine gap.** Its
`ripper.yaml` carries a prior-investigation comment saying "8 events are now
posted through Aug 26 ... treat a future 0-event build as a real signal" —
and today (Aug 26) is exactly that boundary. Live-checked
`threedollarbillcinema.org/upcomingevents?format=json`: 0 upcoming events,
most recent past screening was early today (2026-08-26 UTC), with a
near-continuous cadence through mid-August before that. This is the first
occurrence of this exact pattern (the one other zero-event mention, Aug 1,
was a different already-closed incident), so it doesn't meet the
30-consecutive-day `dead` threshold yet — but it's worth a build-report
follow-up in the next few days rather than being dismissed. No `dead` flag
written this cycle; noting here for visibility.

Rotated searches across music venues, community centers, art galleries,
running/5K races, pop-up markets, and karaoke/nightlife — a saturated set
of verticals at this point (830+ tracked candidate files). Nearly every hit
was either an aggregator we don't implement individually (JamBase,
Songkick, SeatGeek, Ticketmaster, RunGuides, RaceMob, EverOut, do206) or a
venue already covered/tracked (Crocodile, Gallery 110, Slip Gallery, Museum
of Museums [closed, already `notviable`], Seattle Artist League [already
`candidate`], Cuff Complex, Octopus Bar, Rickshaw Restaurant & Lounge,
Unicorn, Ozzie's, Dreamland).

One genuinely new lead investigated to a disposition:
- **M. Rosetta Hunter Art Gallery** (Seattle Central College, Capitol Hill)
  — plain Drupal HTML, only ~3 exhibitions/year with month-level date
  ranges only (no specific opening dates/times), irregular gallery hours.
  Not enough structured/dated data to produce calendar events. `notviable`.

Also re-verified two stub candidates from the 2026-08-25 aggregator gap
analysis:
- **KidsQuest Children's Museum** (kidsquestmuseum.org) — confirmed
  address is 1116 108th Ave NE, Bellevue, WA. Entirely outside Seattle;
  fails the geographic gate. Flipped `candidate` → `notviable`.
- **Olympic Athletic Club** (olympicathleticclub.com) — confirmed single
  location in Ballard (passes the geo gate), but the sample events are
  recurring group-fitness class instances (BodyPump, kids' Tae Kwon Do
  slots) rather than one-off happenings — closer to a class schedule.
  `/classes/` is JS-rendered with no obvious ICS/JSON feed on a quick
  pass. Flipped `candidate` → `investigating`, noted the class-schedule
  concern and the need to find the underlying booking-platform API
  before treating this as implementation-ready.

- 💡 Candidate: none new and implementation-ready this cycle — the two
  fresh leads above both resolved to `notviable`, and no other unseen
  Seattle sources surfaced across the rotation.
- ❌ Not Viable: M. Rosetta Hunter Art Gallery — no specific dated events,
  only month-range exhibition listings (see above)
- 🔄 Status fix: KidsQuest Children's Museum — `candidate` → `notviable`
  (Bellevue, not Seattle)
- 🔄 Status fix: Olympic Athletic Club — `candidate` → `investigating`;
  geo gate passes, class-schedule fit and data source still unresolved
- 💀 Dead source flagged: none new (Three Dollar Bill Cinema noted above
  as a real signal worth a build-report follow-up, not yet at the
  30-day `dead` threshold)

## Source discovery: implementing highest-confidence candidate

- ✅ Added: Rigs & Coffee at Tiki Motors — recurring (3rd Saturday monthly) — `sources/recurring/rigs-and-coffee-tiki-motors.yaml`
- 🔄 Status fix: Rigs & Coffee at Tiki Motors — tag corrected from `SoDo` to `Georgetown` per Nominatim address lookup; `status: candidate` → `added`
