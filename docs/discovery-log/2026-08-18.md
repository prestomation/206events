## Source discovery: LGBTQ+/queer, film screenings, night markets, art galleries, dance, roller skating, historic house museums, breweries, spoken word/poetry, plant swaps, vintage/pop-up markets, axe throwing, warehouse raves, drag brunch, vinyl record fairs/swap meets, immersive art, chess clubs, fiber arts

Ran `dead-sources.py` first — 3 zero-event calendars
(`external-on-the-hiyu`, `external-seattle-childrens-museum`,
`external-seattledances`) and 1 external failure (`west-seattle-blog`,
empty HTTP 200 body). All already tracked from prior runs — `seattledances`
and `west-seattle-blog` are documented in `docs/discovery-log/dead-sources.md`;
`on-the-hiyu` and `seattle-childrens-museum` already have candidate files
noting their intermittent/zero-event history. No new action needed.

Ran a wide rotation of verticals not covered in the last several cycles
(2026-08-13 through 2026-08-17 focused on open mic/nightlife/community
centers/outdoors/music venues/city calendars — see those days' logs).
Checked ~25 leads across the searches below against the existing 705-file
`docs/source-candidates/` set and `sources/`; the overwhelming majority
were already tracked (added, candidate, notviable, or blocked), which is
expected this deep into the discovery backlog. Two genuinely new items:

- 💡 Candidate: Fog Room (rooftop bar, Downtown) — custom HTML (ProcessWire,
  server-rendered) — https://www.fogroomseattle.com/events/ — recurring
  Sunday Drag Brunch + occasional themed nightlife events; 🔴 Low tier
  (custom scraper, per-event detail page needed for start time)
- ❌ Not Viable: Rain City Slam (poetry slam org) — org page has no
  calendar and its own domain (raincityslam.org) is expired/parked

Already covered, confirmed no action needed (sampled — not exhaustive):
Century Ballroom (notviable), Reverie Ballroom (candidate, re-confirmed
still 0 events as of 2026-08-10), Museum of Museums (notviable), Gallery
110 (added), Seattle Film Society (added), Central Cinema / Grand Illusion
Cinema (already `sources/external/`), CID Night Market / Columbia City
Beatwalk (added), Historic Seattle / Log House Museum (added / notviable),
Reuben's Brews (already a source), Pop-Up Poetry Slam Seattle (candidate),
Axe Kickers / Blade & Timber (investigating / notviable), Poquitos Seattle
(candidate), Unicorn / Kenyon Hall / CHASM / Punk Rock Flea Market
(all added), El Sueñito Brewing (already a source), Seattle Chess Club /
Rough & Tumble Pub / The Cuff Complex (all added), Needlehook (notviable —
regional PNW/Bellingham feed, not Seattle-specific), Queer Bar (already a
source).

No new source implemented this cycle — no unimplemented 💡 candidate
cleared the fetch-validation bar with confirmed events beyond Fog Room,
which is a 🔴 Low custom-scraper lead left for a future implementation
cycle per the "always implement highest-confidence candidate first" rule
(no higher-tier unimplemented candidate was found in today's search set).

## Source discovery: implementation cycle — West Seattle community

Implementation-only cycle. No new searches run; picked the highest-confidence
existing 💡 candidate from the 2026-08-17 log, which named **The Center for
Active Living** and **Seattle Tech Forum** as the two 🔥 High ICS picks for the
next implementation cycle.

Chose The Center for Active Living over Seattle Tech Forum: both are plain ICS
feeds, but this one is a native WordPress/Events-Calendar export on the org's
own domain with a single fixed street address (so it lands in `venues.json`),
whereas the Tech Forum feed routes through a third-party Luma calendar id.

- ✅ Added: **The Center for Active Living** — external ICS
  (`sources/external/center-for-active-living.yaml`) — 100 events —
  `wscenter.org/calendar/?ical=1`

Verification: `ONLY_SOURCE=center-for-active-living npm run generate-calendars`
produced 100 events across `external-center-for-active-living`, `tag-community`,
and `tag-west-seattle`, with 1 venue emitted to `venues.json` and no parse or
geocode errors. `geo` resolved via Nominatim to OSM way `450278367` (listed
under the venue's former name, "Senior Center of West Seattle").

One transient JS bot-challenge interstitial was observed on the ICS URL
mid-session and did not reproduce on any later fetch, with or without a browser
UA. Recorded in the candidate file; not treated as a block, so the source ships
at `proxy: false` (rung 1) rather than being staged for proxy testing.
