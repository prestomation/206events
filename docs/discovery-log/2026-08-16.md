## Source discovery: implementing an existing candidate (Savoy Swing Club)

Scheduled implementation cycle — reviewed the outstanding `status: candidate`
queue in `docs/source-candidates/` (~96 files) for the highest-confidence
unimplemented lead rather than running a fresh web-search rotation.

**savoy-swing** was the clear pick: flagged "Strong find" in the 2026-08-14
log with confirmed Squarespace platform detection and 20+ dated events, but
not yet implemented. Re-verified live: `?format=json` returns `itemCount:
138`, 2 events in the `upcoming` array with future `startDate` epoch values
(Aug 22 and Aug 29, 2026 — "Dance Workshop & Guided Practice" and "Speakeasy
at Savoy: The Baseless Accusations"). Built-in `squarespace` type, no new
ripper code — `sourceRole: venue` (Savoy Swing Club's own events at its own
venue, not a republished aggregate). Geocoded Lowdown Ballroom (628 11th Ave
E, Seattle, WA 98102) via Nominatim. Used tags `["Dance", "Capitol Hill"]`
(canonical `Dance` spelling from `lib/config/tags.ts`, not the candidate
note's `Dancing`/`Swing`). `ONLY_SOURCE=savoy-swing` build confirmed 2
events, 0 errors.

- ✅ Added: **Savoy Swing Club** — Squarespace (built-in type) — 2 events —
  0 errors — PR #1212 (merged).
- 🔄 Status fix: Savoy Swing — `candidate` → `added`.

Other high-confidence candidates noted in the 2026-08-14/15 logs (King
County Parks Socrata API, 48 North SARC, Not Creepy Gathering, Saving Great
Animals, West Seattle Indivisible, NW Dance, Sound Theatre Company via
OvationTix, Trinity Nightclub's Eventbrite link) remain unimplemented — left
for a future cycle per the "one source per cycle" rule.

### Dead sources

Not re-run this cycle (checked 2026-08-14/15, no new 30-day-stale sources
identified since then).

## Source discovery: art galleries, distilleries, roller derby/skating, ceramics, burlesque, axe throwing, nurseries, historic tours, queer nightlife, breweries, night markets, coworking spaces, sports clubs, fiber arts

Concurrent discovery cycle (different session, same calendar day) — ran a
fresh web-search rotation rather than sweeping the candidate backlog first.

Ran `dead-sources.py` first — 4 zero-event calendars
(`frye-art-museum-frye-art-museum`, `external-on-the-hiyu`,
`external-seattle-childrens-museum`, `external-seattledances`) and 1
external failure (`west-seattle-blog`, empty HTTP 200 body). All but
`frye-art-museum-frye-art-museum` are already-known, already-tracked
issues from `docs/discovery-log/dead-sources.md` / prior runs — none
has newly crossed the 30-consecutive-day bar, so no `💀 Dead source`
flags this cycle. `frye-art-museum-frye-art-museum` is a first-time
sighting: its `expectEmpty` flag was removed 2026-08-09 (calendar was
listing 5 upcoming events at the time), and the site still returns
HTTP 200 on `/calendar` — likely a transient parse/content dip rather
than a dead source. Noting for the build-report skill to monitor rather
than re-flagging `expectEmpty` (which would mask a real regression if
it isn't transient).

Rotated into verticals not covered by the last several runs (which
focused on reddit/social, open-mic/drag/board-games/vintage-markets/
silent-disco/comics/escape-rooms/karaoke/planetarium, backlog vetting +
community-centers/art-walks/new-venues/nonprofits/car-meets/sci-fi-cons,
and outdoors/running/climbing/bookstores/food-festivals/comedy/
eventbrite-discovery): art galleries, distilleries, roller
derby/skating rinks, pottery/ceramics studios, burlesque/cabaret, axe
throwing, plant nurseries, historic walking tours, queer nightlife, new
brewery openings, night markets, coworking spaces, rugby/cricket clubs,
astronomy clubs, and knitting/fiber-arts guilds. 10 web searches total.

The large majority of leads resolved to sources already `added` or
already tracked as candidates: Gallery 110, Gallery Mack, Slip Gallery
(all `added`), Southgate Roller Rink (7 recurring entries, `added`),
Rat City/Jet City/Tilted Thunder/PFM Roller Derby, Seward Park Clay
Studio (all tracked, none currently viable — Rat City's 2026 season
ended, re-check Sept/Oct when 2026–27 announced), Axe Kickers/Blade &
Timber/Gasworks Brewing/Historic Seattle/Wanderlust Nursery/West
Seattle Nursery/Reuben's Brews/Queer Social Club/Queer/Bar/Charlie's
Queer Books (all tracked or already `added`), CID Night Market
(covered by `sources/cidbia/`), Seattle Astronomical Society (`added`).

New candidate files written this cycle:

- ❌ Not Viable: **Wanderlust Nursery** — re-checked; `upcoming` has
  repopulated (4 events, confirming the Squarespace pipeline works),
  but only 1 of 4 is in Seattle proper (Admiral Block Party) — the
  other 3 are in Federal Way and Bellevue. Regional plant-events
  curator, not primarily Seattle. Flipped `candidate` → `notviable`.
- ❌ Not Viable: **The Cloud Room** (Capitol Hill coworking space,
  Squarespace, 16 verified upcoming events) — same address (1424 11th
  Ave) as the already-`added` `sources/chophouse_row/` ripper, whose
  events collection is a superset that already includes every Cloud
  Room event (prefixed `"Cloud Room: ..."`, confirmed by title
  comparison: 27 upcoming on Chophouse Row's feed vs. 16 on Cloud
  Room's own page). Adding it would duplicate an existing source.
- ❌ Not Viable: **Seattle Knitters Guild** — Weebly, no structured
  feed; of ~15 dated events only 1 (a December market) is in Seattle,
  the rest are Tacoma/Spokane/Port Angeles/out-of-state.
- ❌ Not Viable: **Kiln Fremont** (coworking space) — events calendar
  is gated behind "Member Events," not public.
- 🔍 Investigating: **Southside Revolution** — South Seattle junior
  roller derby league, real and active, but the site is a Wix SPA with
  a signed Wix Events widget (not one of our built-in platforms, no
  plain scrapable endpoint found on a quick pass).
- 🔍 Investigating: **Seattle Rugby Club** — Webflow site with a mixed
  home/away schedule; no feed found on a quick pass, low priority
  (niche sport, uncertain per-season home-game volume).
- 🔄 Status fix: **Reclaim Clay Classes** — re-checked the Squarespace
  `?format=json` endpoint flagged 8/14 as unverified: it's a
  product/store collection, not an events collection (no `startDate`
  field; dates are unstructured text in each product's `excerpt`).
  Confirms 🔴 Low-confidence (needs a custom scraper), stays
  `candidate`.

**Implementation note:** this session's backlog sweep also independently
picked Savoy Swing as the top unimplemented candidate — it was implemented
and merged concurrently by the other session (PR #1212, see the log section
above), so no separate implementation was needed here.

Other backlog candidates checked and passed over this cycle: Human
People Beer, Poquitos Seattle, Reverie Ballroom, Talarico's Pizzeria,
Clean Greens Farm & Market (all still 0 upcoming events per their own
notes — re-check later), Uplift Climbing / Vortex Pottery (recurring-
class pattern, not a dated-events collection — better fits as
`sources/recurring/` entries in a future cycle), Grey Drizzle Fibers
(borderline Seattle-focus, Renton-based with only 1 of 2 live events in
Seattle), Trinity Nightclub (needs its Eventbrite organizer ID
confirmed first), Tabletop Village / Zulus Games (Shopify, worth a
follow-up `/collections/*.json` probe), 48 North SARC (high volume but
region-wide WA/OR/BC scope needs Puget-Sound filtering before it's a
clean add).
