## Social source discovery: r/SeattleEvents

Feed: 25 posts (6 new since last run)

- ❌ Skipped (already covered): PhinneyWood Art Walk — `phinneycenter.org/events/art-walk/` — already covered by `sources/external/phinney-neighborhood-association.yaml` (Tribe Events ICS) and `sources/recurring/phinney-greenwood-artwalk.yaml`.
- ❌ Skipped (already covered): WABA Korea Expo & Festival — single event page (`waterfrontparkseattle.org/blog/event/2026-waba-korea-expo-and-festival`) on Waterfront Park, already covered by `sources/waterfront_park/`.
- ❌ Skipped (one-off / broadly covered): South Lake Union Block Party (8/14) — single annual block party; SLU events broadly covered by `sources/discover_slu/`.
- ❌ Skipped (one-off): Chase Oliver Washington appearance — `lpwa.ticketspice.com/chase-oliver` — single TicketSpice political event, not a recurring source.
- ❌ Skipped (not event source): `lpwa.org/donate` — donation page.
- 🔁 Re-sighted (existing candidate, status `notviable`): Seattle AttendTickets Free Shows — `seattle.attendtickets.com/free` — same weekly snapshot pattern, no new structured data. Updated `lastChecked` + sighting note in `docs/source-candidates/attendtickets-free-shows.md`.

No new candidates this run. All six new posts resolved to already-covered
sources, one-off events, or the already-known AttendTickets candidate.

## Source discovery: implement highest-confidence candidate

Swept the open candidate backlog for a currently-viable source rather than a
fresh search — re-checked ~20 stale `candidate` entries
(`?format=json`/organizer-API live status) across Squarespace, Eventbrite,
and Humanitix sources. Nearly all are still at 0 upcoming events at time of
check (`phinney-books`, `nudibranch-coffee`, `talaricos-pizzeria`,
`poquitos-seattle`, `human-people-beer`, `reconnect-south-park`,
`coca-seattle`, `seattle-cityclub`, `urban-community-councils-of-seattle`,
`ncnw-seattle-section`, `clean-greens-farm-market`, `innervisions-posters`,
`seattle-street-food-festival`, `gates-foundation-discovery-center`).
`wanderlust-nursery` now has 5 upcoming events but 4/5 are outside Seattle
(SeaTac, Federal Way, Bellevue) — fails the Seattle-focus gate, left as
`candidate`. `sodo-bia` remains blocked by an SGCaptcha JS challenge.
`knkx-events` (surfaced 8/11) turned out to be a single non-Seattle featured
event (Victoria, B.C.), not a real events list — left as `candidate` with a
note. `madrona-neighborhood-association`'s `/events/` page is Elementor
theme demo content (fake Bali/Indonesia placeholder events), not the real
event listings described when the candidate was logged — the actual data
lives on unstructured individual static pages; still a 🔴 Low custom-scraper
candidate, left as-is.

- ✅ Added: The Residency Seattle — Squarespace (built-in type) — 1 event — PR #1186 — youth hip-hop performing-arts nonprofit, multi-venue (`geo: null`), re-verified live with a confirmed Aug 15 show at Nippon Kan Theatre after being held back at 0 events on 2026-08-03.
