## Social source discovery: r/SeattleEvents

Feed: 25 posts (6 new since last run)

- ❌ Skipped (already covered): PhinneyWood Art Walk — `phinneycenter.org/events/art-walk/` — already covered by `sources/external/phinney-neighborhood-association.yaml` (Tribe Events ICS) and `sources/recurring/phinney-greenwood-artwalk.yaml`.
- ❌ Skipped (already covered): WABA Korea Expo & Festival — single event page (`waterfrontparkseattle.org/blog/event/2026-waba-korea-expo-and-festival`) on Waterfront Park, already covered by `sources/waterfront_park/`.
- ❌ Skipped (one-off / broadly covered): South Lake Union Block Party (8/14) — single annual block party; SLU events broadly covered by `sources/discover_slu/`.
- ❌ Skipped (one-off): Chase Oliver Washington appearance — `lpwa.ticketspice.com/chase-oliver` — single TicketSpice political event, not a recurring source.
- ❌ Skipped (not event source): `lpwa.org/donate` — donation page.
- 🔁 Re-sighted (existing candidate, status `notviable`): Seattle AttendTickets Free Shows — `seattle.attendtickets.com/free` — same weekly snapshot pattern, no new structured data. Updated `lastChecked` + sighting note in `docs/source-candidates/attendtickets-free-shows.md`.

No new candidates this run. All six new posts resolved to already-covered
sources, one-off events, or the already-known AttendTickets candidate.

## Source discovery: implement highest-confidence candidate

Swept the open candidate backlog for a currently-viable source rather than a
fresh search — re-checked ~20 stale `candidate` entries
(`?format=json`/organizer-API live status) across Squarespace, Eventbrite,
and Humanitix sources. Nearly all are still at 0 upcoming events at time of
check (`phinney-books`, `nudibranch-coffee`, `talaricos-pizzeria`,
`poquitos-seattle`, `human-people-beer`, `reconnect-south-park`,
`coca-seattle`, `seattle-cityclub`, `urban-community-councils-of-seattle`,
`ncnw-seattle-section`, `clean-greens-farm-market`, `innervisions-posters`,
`seattle-street-food-festival`, `gates-foundation-discovery-center`).
`wanderlust-nursery` now has 5 upcoming events but 4/5 are outside Seattle
(SeaTac, Federal Way, Bellevue) — fails the Seattle-focus gate, left as
`candidate`. `sodo-bia` remains blocked by an SGCaptcha JS challenge.
`knkx-events` (surfaced 8/11) turned out to be a single non-Seattle featured
event (Victoria, B.C.), not a real events list — left as `candidate` with a
note. `madrona-neighborhood-association`'s `/events/` page is Elementor
theme demo content (fake Bali/Indonesia placeholder events), not the real
event listings described when the candidate was logged — the actual data
lives on unstructured individual static pages; still a 🔴 Low custom-scraper
candidate, left as-is.

- ✅ Added: The Residency Seattle — Squarespace (built-in type) — 1 event — PR #1186 — youth hip-hop performing-arts nonprofit, multi-venue (`geo: null`), re-verified live with a confirmed Aug 15 show at Nippon Kan Theatre after being held back at 0 events on 2026-08-03.

## Source discovery: food/drink, city.gov calendars, RSS feeds, holiday markets, new venues, science/maker/roller-skating, Eventbrite, Georgetown/Beacon Hill, record stores, yarn/craft shops, theater, hiking, tech meetups, social dance

Ran `dead-sources.py` first — 4 zero-event calendars (`external-on-the-hiyu`,
`external-seattle-childrens-museum`, `external-seattledances`,
`external-urban-league-seattle`, all already `status: added`/`proxy` in
their candidate files — nothing new to flag) and 1 external failure
(`west-seattle-blog`, also already `status: added`). A single snapshot
isn't enough to confirm 30+ consecutive zero-event days for any of these,
so none were flagged `dead` this run.

Ran a wide rotation of searches (food festivals, beer/taproom calendars,
seattle.gov calendars, RSS feeds, holiday markets, new 2026 venues, science
lecture series, maker spaces, roller skating, Eventbrite organizers,
Georgetown/Beacon Hill neighborhood associations, record store in-store
shows, yarn/craft shops, small theater companies, hiking clubs, tech
meetups, social dance studios). The overwhelming majority of results were
already-covered sources or previously-evaluated candidates (552 files in
`docs/source-candidates/` already) — see below for the few genuinely new
findings.

- 💡 Candidate: Sueños de Salsa — recurring weekly social (custom, likely
  `sources/recurring/`) — https://suenosdesalsa.com/socials-and-practicas/
  — salsa/bachata studio at 6512 Roosevelt Way NE; weekly Wednesday social
  dancing, 8:45 pm.
- 💡 Candidate: The Knitters Studio — custom HTML (🔴 low confidence) —
  https://www.theknitterstudio.com/classes-and-events-1 — Squarespace
  site but the events page is a plain page (`itemCount: 0`), not a
  Squarespace Events collection; dates are embedded in prose.
- ❌ Not Viable: Wool Friends — https://www.wool-friends.com/ — Square
  Online (Weebly) JS SPA, no accessible structured data.
- ❌ Not Viable: Jive Time Records — https://jivetimerecords.com/ — no
  events/calendar section on the site at all.
- ❌ Not Viable: Seattle Dance Cast — https://www.seattledancecast.org/ —
  unstructured Google Sites hobbyist page, redundant with the already-
  covered `seattle-dance-info` source.
- ❌ Not Viable: Acorn Street Shop — https://www.acornstreet.com/ — Rain
  POS-hosted class calendar with no accessible structured data found.
- ❌ Skipped (already covered): Reuben's Brews, Stoup Brewing, Ballard
  Brewed, Ounces Taproom, Seattle Night Market, Seattle Local Markets
  (already `notviable`), Seattle Christmas Market, Flight Club (both the
  Capitol Hill venue and the SLU darts bar — both already `notviable`),
  Lowlander Brewing (already `notviable`), RailSpur (already `added`),
  Georgetown Business Association (already an external source), Easy
  Street Records (already `blocked`), Sonic Boom Records (already a
  ripper), North End Makers / Seattle Makers (already rippers), Town Hall
  Seattle (already an external calendar), NWTheatre, GreenStage, Taproot
  Theatre, Century Ballroom, Reverie Ballroom, Seattle Dance Info, Salsa
  Con Todo, The Mountaineers, New Tech Seattle, Seattle Rep — all
  previously evaluated or already implemented.
