# 2026-08-03 Discovery Log

## Source discovery: comedy, art galleries, outdoor/festival, Eventbrite orgs, Labor Day, senior centers, cultural orgs, vintage/pop-up markets, roller skating, new venues

Ran `dead-sources.py` first — 4 zero-event calendars this snapshot
(`frye-art-museum`, `kremwerk`, `sync-seattle`, `urban-family-brewing`),
0 external failures. Same set seen in recent snapshots (`tag-sand-point`
dropped off this time); none newly meeting the 30+ consecutive day
dead-source bar — nothing flagged as newly `dead`.

Searched ~15 queries across a wide rotation of verticals. As in every
recent cycle, the large majority of leads are already covered by an
existing ripper/external/recurring source or already tracked in
`docs/source-candidates/` (516 files going in) — this project remains
very thoroughly mined:

- ❌ Skipped (already covered): Laughs Comedy Club, Club Comedy Seattle,
  Emerald City Comedy Club, Unexpected Productions, Gallery 110, Henry
  Art Gallery, Seattle Art Museum, Bumbershoot, Sync Seattle, Seattle
  Social Club, New Tech Seattle/The Collective (venue), Folio: The
  Seattle Athenaeum, C&P Coffee, Reuben's Brews, NAAM, Jet City Labs,
  Seattle Local Markets, Kraken Community Iceplex, Orquesta Northwest,
  Elliott Bay Book Co — all already `added` sources or existing tracked
  candidate files, no status change
- ❌ Not viable (this pass): Assembly Art Fair (new Ballard-area gallery
  fair, but single annual 4-day event, already past for 2026 — same
  category as other single-annual-event fairs already excluded);
  Central Area Senior Center (Wix `events-viewer` widget, no API — same
  pattern already ruled out for `folio-seattle-athenaeum`); The Collective
  Seattle's own Eventbrite org page (0 events currently listed, distinct
  from the venue of the same name that hosts New Tech Seattle)
- 🔴 Blocked: Ballard Senior Center — plain `curl` with a UA header
  returns a bare HTTP 403 from this environment; per the "fails locally
  too" rule, not staged for proxy testing
- 🔄 Status fix: Roll Around Seatown — re-confirmed `notviable` (Wix, no
  API), `lastChecked` bumped to today; no other change
- 💡 Candidate: The Residency (youth hip-hop nonprofit, Pioneer Square) —
  Squarespace confirmed (`events-stacked` collection), but `upcoming: []`
  at time of check — all 4 listed events (Second Sunday Jam, fundraiser
  at the Showbox, Spring Showcase) already passed relative to today.
  Re-check next cycle; the org clearly runs a recurring annual cadence of
  public shows once new dates are posted.
- ❌ Not viable (this pass, no dedicated site/calendar): Emerald City
  Skaties (informal roller-skating social posts, no discoverable
  dedicated calendar site)

No source cleared the implementation bar this cycle (built-in-type feed
confirmed working with events > 0, or custom-scrape target confirmed
reachable with live data) — the one promising lead (The Residency) is
currently between announced events and needs a re-check once its next
show is posted.

## Source discovery: re-check candidate backlog, drag/dance/karaoke/night-market/comics verticals

Re-checked every `status: candidate` file in `docs/source-candidates/` (28
files) for a live upcoming-event count before searching further. All
remain at 0 upcoming events since their last check (Squarespace
`upcoming: []` or Eventbrite `upcomingEventsTotal: 0`): `phinney-books`,
`open-books-poem-emporium` (still 🔴 low-confidence prose scrape, not
attempted), `stone-way-cafe`, `the-cuff-complex`, `poquitos-seattle`,
`reverie-ballroom`, `coca-seattle`, `kandelia`, `human-people-beer`,
`grey-drizzle-fibers` (now down to 1 upcoming event, still North Bend —
outside Seattle), `wanderlust-nursery`, `uplift-climbing`,
`reconnect-south-park`, `ncnw-seattle-section`, `seattle-cityclub`,
`urban-community-councils-of-seattle`, `clean-greens-farm-market`,
`bellevue-arts-fair`, `discnw`, `fremont-chamber-of-commerce`,
`fremont-fridays`, `magnolia-summerfest`, `rat-city-roller-derby`,
`redmond-arts-festival`, `sakura-con`, `seattle-farmers-markets`,
`seattle-street-food-festival`, `whim-whim-seattle-contemporary-dance`.

Also re-checked `ballard-comedy-takeout` (still dormant — organizer's
public API mirror still `object_count: 0`) and the `investigating` queue
(`axe-kickers`, `baila-district`, `raygun-lounge`, `seattle-book-club`,
`seattle-central-theatres`, `silent-dance-alki` — all JS-rendered pages
with no discoverable API from this environment; confirmed headless-browser
rendering is not viable here either, Chromium via Playwright can't reach
any host through this session's proxy, not just these sites).

Ran a fresh round of ~10 web searches across drag brunch, swing/Latin
dance, karaoke, night markets, and comic book stores:

- ❌ Skipped (already covered): Century Ballroom / Swing It Seattle (both
  previously `notviable` — re-confirmed no change), Poquitos Copacabana
  Drag Brunch (existing 0-event candidate), Dreamland Fremont, Unicorn
  Seattle karaoke/drag (all already `added` via `sources/recurring/`),
  The Collective Seattle karaoke and St Andrews Pub karaoke (both
  Eventbrite, both 0 upcoming events at time of check), Outsider
  Comics/Phoenix Comics (no structured event feed, per existing
  `seattle-indies.md`), MoPOP and Pacific Science Center (already
  `added`), every First Thursday art walk neighborhood (all already
  covered via `sources/recurring/*-artwalk.yaml`)
- ❌ Not viable: Silent Dance Alki (silentdance.org) — confirmed via plain
  fetch: GoDaddy Website Builder static shell, page text is literally
  "Coming Soon" x4, no schedule data at all despite the `investigating`
  candidate file suggesting a browser trace might help; updating that file
  to reflect this
- ❌ Not viable (Eastside, outside Seattle): Redmond Arts Festival — also
  confirmed date pattern isn't fixed (2024 2nd weekend, 2025 2nd weekend,
  2026 5th weekend of July — inconsistent, in addition to being Redmond)
- 🔍 Investigating: Greenlake Brewing — still no findable website/events
  page beyond opening-announcement news articles

**✅ Added: CID Night Market — `sources/recurring/cid-night-market.yaml`.**
Promoted the existing `investigating` candidate
(`docs/source-candidates/cid-night-market.md`) to implemented. Confirmed
the event's dedicated page (`seattlechinatownid.com/experiences/c-id-night-market`)
is distinct from the site's `/local-events` calendar already covered by the
`cidbia` ripper (checked the September 2026 `/local-events` month page
directly — it only lists the monthly Community Clean-Up, not the Night
Market). Confirmed a 3-year date history (2024 Sep 21 = 3rd Saturday, 2025
Sep 27 = 4th Saturday, 2026 Sep 26 = 4th Saturday, confirmed live) and
encoded as a `4th Saturday` September schedule with a comment noting the
2024 drift, following the same precedent as `bite-of-seattle`. `geo` uses
the existing `hing hay park` entry in `lib/geocoder.ts`'s
`KNOWN_VENUE_COORDS`. Verified via `ONLY_SOURCE=cid-night-market npm run
generate-calendars`: 1 event, 0 errors, `DTSTART` 2026-09-26T13:00
America/Los_Angeles.
