## Source discovery: pickleball/racket sports, Halloween/fall seasonal, dance socials, axe throwing, chess, theater re-checks

No open source-discovery PR to sweep (last merge was `#1565`, already on
`main`). Confirmed `origin/main` was current, no rebase needed.

Ran `skills/source-discovery/scripts/dead-sources.py` against the production
`build-errors.json`: 21 zero-event calendars (the standing
`seattle-food-trucks-*` set staged under `#1453`, `sam-*` sub-venues, and
other long-known small/empty branches — no change) and 16 external failures
(15 are `Browserbase fetch failed: HTTP 402` billing exhaustion, handled by
`skills/proxy-escalation`; `saving-great-animals` is the pre-existing
empty-response issue). No new 30+-day dead source found.

Ran fresh searches across Halloween/fall-seasonal events (timely for
late-September), roller derby, community theater, spoken-word/poetry open
mics, salsa/swing dance socials, axe throwing, wine tasting rooms, drag
brunch venues, hiking/cycling clubs, new bar/brewery openings, silent disco,
language exchange meetups, community gardens, birding/Audubon, MOHAI, and
pickleball. Nearly every lead resolved to a source already `added` (Rat City
Roller Derby, Seattle Public Theater, Triple Door, WAMU Theater, NWTheatre,
Seattle Chess Club, Fog Room, Fleet Feet Ballard, Fremont Sunday Market,
MOHAI) or a previously-evaluated candidate — caught before re-investigating
by checking `sources/` and `docs/source-candidates/` first. Re-verified
several standing `candidate`-status Squarespace/Eventbrite entries live
(Reverie Ballroom, Phinney Books, Poquitos Seattle, Uplift Climbing,
Nudibranch Coffee, Clean Greens Farm & Market, Innervisions Posters) — all
still 0 upcoming events, no change. Blade & Timber and We Rise Wines
confirmed still `notviable` (no public calendar); Axe Kickers still
`investigating` (Wix Events widget, no confirmed data endpoint); Greenlake
Brewing still `investigating` (domain parked); Seattle Central Theatres
still `investigating` (Trumba feed still 410 Gone); Visit Pioneer Square
still `blocked` (SiteGround captcha). Trap Door Brewing's new location
(Prairie View Station) turned out to be in Brush Prairie, WA — outside the
Seattle metro entirely, not worth a candidate file.

- ✅ Added: **Sideout Tsunami Pickleball Center** (Seattle's largest indoor
  pickleball facility, 26 courts, 2300 26th Ave S, Mount Baker) — found via
  a "Seattle pickleball open play schedule" search. Its `/events` page is a
  Next.js SSR page that server-renders real `schema.org/SportsEvent` JSON-LD
  directly in the HTML — no JS rendering needed. Confirmed 50 upcoming
  events (drop-in classes, leagues, clinics, tournaments) spanning today
  through mid-October, each with a stable per-occurrence id via a
  CourtReserve booking link (`reservationId=`/`resId=` query param). 🔴
  Low-confidence tier (custom JSON-LD scraper — no built-in ripper type
  fits) but high-certainty data, implemented as `sources/side_out_tsunami/`.
  "Mount Baker" was not yet a registered neighborhood tag; added to
  `city.config.ts`. Verified via `ONLY_SOURCE=side-out-tsunami npm run
  generate-calendars`: 50 events, 0 errors, 0 geocode errors. Full `npm run
  test`: 3976 tests, 227 files green. See
  `docs/source-candidates/side-out-tsunami.md`.

No other fresh lead cleared the quality gate today — everything else was
either already covered, blocked, or had 0 upcoming events on live re-check.
